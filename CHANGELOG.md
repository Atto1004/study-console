# 학습 관제탑 패치 노트

설정 화면의 「패치 노트」 카드에는 최근 항목만 보이고, 전체 기록은 이 파일에 쌓는다. 빌드 번호는 설정 하단(상단 학기 태그에 마우스를 올려도 보임)에서 확인한다. 배포 후 GitHub Pages 반영까지 최대 10분, iPad에서는 ↻ 버튼으로 새로 고친다.

## 2026-09-16

### .33 — 과목별 상단(미리)/하단(수업 후) 자료 구성 (아토 지시 → 지침 학습시스템.md §10)
- `V33 LAYER`(파일 끝, 원본 `docs/v33-layer-src.js`). 전역 `MUST`는 그대로 두고 `mustHTML`·`V32.dotsHTML`을 **목록 인자 버전**으로 다시 정의(오타 1차 권고). 과목별 스펙 `V33.SPEC[과목명] = {pre, post, rules}`.
- **하단(수업 후)**: 일물2 판서·녹음 / 공수1 판서·녹음·**과제 제출용 생성본**(주차 링크·「과제」 노트 연결 여부 → `linked`, "제출본 존재"라고 쓰지 않음) / 정역학 **교수님 필기본**·녹음 / 미적2 판서·녹음·필기 / CADD **그 주 영상 정리**(PC 날짜 폴더 `영상정리_*.md`, `weekOf(item.date)` 일치만)·과제 / 글쓰기 녹음. 스펙 없는 과목은 V32 4종.
- **상단(미리 올릴 자료)**: 회차 시트 요약 위에 `#v33Pre` — 전공책·솔루션·강의노트·수업자료를 PC 인벤토리 `common`의 **파일명 정규식**으로 판정(classify 변경 없이 구·신 JSON 호환). 없음 / PC N개 / 확인 못 함(인벤토리 없음·동명 과목).
- 과목 화면 `#cHead` 아래 「과목 규칙 · 자료 구성」 카드. 첨부 kind에 교수필기·영상정리 추가. 주차표 점 개수 = post 길이.
- `_진도커버리지.py classify`: `솔루션_`→solution, `교수필기_`→profnote, `영상정리_`→lmsvideo, `여름*`→summernote 추가(기존 분류 유지). `_영상정리`는 계속 제외(작업 폴더).
- 오타 회의: 설계 1차 RED(4) → 2차 RED(1) → 3차 GREEN. 게이트 구문 0 · 스모크 50건 × 3시점 실패 0·오류 0.
- 미결(아토 지시로 생긴 설계 항목): CADD LMS 영상 자동 정리, 정역학 LMS 영상·필기본 수집·한글 변환, 미적분 클리닉 루틴 등록.

### .32 — 주차표 · 자료 4종 업로드 여부 · 라이브러리 (아토 스프레드시트 지시)
- **주차표**(달력 탭 4번째 세그먼트): 주차×요일(월~금) 표. 셀마다 그날 수업을 `과목약칭 N일차 [출결약자]` + 자료 점 4개(🎙📘✍️🧱)로 나열, 진도 문구·그날 마감 과제 태그는 아래 줄. 글자색 = 출결(결석 빨강 A · 지각 노랑 L · 인정결석 분홍 E · 출튀 보라 G · 개큰지각 주황 VL), 색과 약자 병행. 과목 칩에 `(결석/지각)` 누계(지각 = 지각+개큰지각). 오늘 셀 테두리, 휴강 표시. 줄을 누르면 회차 기록. 폰(≤600px)은 선택 주차의 요일별 목록이 기본이고 「표로 보기」로 5열 표(가로 스크롤).
- **출결 6종**: `ATT`에 개큰지각(vlate)·인정결석(excused) 추가. `attStats` 덮어쓰기 — 6종 집계, 휴강 제외, 출석률 분자 = 출석+지각+개큰지각+출튀+인정결석, 결석 환산 = 결석 + floor((지각+개큰지각)/n). 학습 완료(`reviewed`)·복습 큐 규칙과 XP는 그대로.
- **자료 4종** `MUST` = 녹음본(rec)·강의자료(pre)·내 필기본(note, kind 필기본)·칠판 판서(board, kind 칠판판서). `mustHTML` 덮어쓰기 — 분모 `MUST.length`, 종류별 상태 have/pc/maybe/pending/unknown/none, 출처별 개수(텍스트·기기·PC) 따로 표시하고 합산 없음. PC 조회 실패해도 기기 판정은 유지하고 「PC 확인 못 함」만 덧붙임. 예전 `판서` kind 첨부는 **미분류**로 두 줄 모두에 노란 점 + 「분류하기」, 첨부 칸에서 필기본/칠판판서 셀렉트로 재분류(`idb.put`). 첨부 kind 선택지에 필기본·칠판판서·녹음 추가.
- **PC 인벤토리** `knowledge/library.json` — `study-materials/_진도커버리지.py`가 폴더 전체를 훑어 생성(파일명 접두+폴더 문맥 분류: rec/pre/note/board/summary/hw/derived/other, 공통 폴더는 `common`). **`.gitignore`로 공개 배포 제외**(파일명에 교수 실명) — atom `/study/` 안에서만 읽히고, 공개 페이지는 「PC 인벤토리 확인 못 함」으로 동작. 앱은 과목 **이름**으로 연결(과목 id는 기기마다 `uid()`).
- **라이브러리** 뷰 `#v-lib`: 종류 탭 9개(녹음본·강의자료·내 필기본·칠판 판서·정리·과제·파생 텍스트·기타·이 기기 첨부) → 과목 카드 → 날짜·N일차·파일명·크기·출처. PC 파일은 이름만(열기 미결). 진입: 학습 탭 상단 카드, 주차표 도구줄.
- **N일차** `V32.meetings(c)` = `planned(c)`에서 `c.enrolledFrom` 이전·`cancelled`·학기 밖 제외, 보강 세션 포함. 정역학 `enrolledFrom=2026-09-03`(9/7 = 1일차), 9/2 OT는 휴강 처리로 집계 제외.
- **아토 주차표 기록 반영**(`patchV32b`, 1회): 색 칸은 덮어씀 — 공수1·일물2 9/2 인정결석 · 미적2·글쓰기·CADD 9/8 인정결석 · 공수1·일물2 9/9 인정결석 · 정역학 9/9 결석 · 미적2 9/10 개큰지각 · 공수1 9/11 지각 · 창업 9/14 결석. 검정 칸은 비어 있을 때만 출석. 미적2 9/15 진도 「12.1-12.2 (위치벡터)」. 시트의 `[감상문 과제]`·`[2주차 과제]` 태그는 마감을 몰라 안 넣음.
- 오타 회의: 설계 1차 RED(6건) → 2차 GREEN(`docs/v32-design-brief*.md`, `_v32-otta-*.md`). 푸시 게이트: `vm.Script` 구문 0건 · jsdom 스모크 29건 × 시점 3개(현재·D-11·개강 전) 실패 0·오류 0. 헤드리스 캡처 900px·390px(표·목록)·라이브러리·회차 시트 확인.
- 오타 3차(구현 검수) RED 4건 → 수선: ① 타임라인 사진도 4종 집계에 포함(`V32.countAtt`), 새 타임라인 사진은 `idb.put` 래핑으로 `칠판판서` 저장, 예전 타임라인 「판서」는 첨부 칸 아래 「전부 내 필기본/칠판 판서」 일괄 버튼과 라이브러리 「이 기기 첨부」의 셀렉트로 재분류 ② 같은 이름 과목 2개면 라이브러리·PC 인벤토리 미연결 ③ 재분류 전 `captureLog`로 입력 보존 ④ 폰 목록 모드도 학기 밖 날짜 제외.
- 미결: PC 파일 열기(atom 서빙 경로), 회차 로그(`#cLog`) 점 색에 인정결석·개큰지각 색 미반영(회색), 보충 워크플로.

### 데이터 갱신 — 진도 현황·커버리지 9/16 기준 (BUILD 변동 없음)
- `knowledge/progress.json` 9/9 → 9/16: 과목별 3주차 행 추가, 9/9 이후 들어온 자료 반영(정역학 9/7 정리 · 공수1 9/11 1.5 선형 ODE 정리 · 일물2 9/11 원형 도선·원판·가우스 서론 정리 · 미적2 9/10 자료 · 공수1 2주차 과제 3종). 「당장 3개」를 2주차 과제 제출 확인 · 3주차 8회차 자료 0건 · 결석분 4회차로 교체. learn.html 「진도 현황」이 그대로 읽는다.
- `knowledge/coverage.json` 9/11 → 9/16 (`_진도커버리지.py` 재실행): 정리 11 · 자료만 3 · 빈칸 14(2주차 이전 6 + 3주차 8).
- 원본: `study-materials/_진도현황_2026-09-16.md` · `_진도커버리지.md`. index.html 변경 없음이라 푸시 게이트 대상 아님.
- (같은 날 추가) 아카데믹글쓰기 LMS 누적 PDF 68p 수신 → 9/8 결석분을 슬라이드 44장으로 재구성한 정리 + 9/15 정리(예고 슬라이드·읽기자료 신형철 칼럼 6단락 개요, 본 슬라이드는 미확보) → `progress.json` 글쓰기 2·3주차 행 교체, 「당장」 4개로 교체(공수1 3주차 과제 #23 확정 · 글쓰기 토론방 주제문 · 정역학 9/16 · 일물2 #31). `coverage.json` 빈칸 1(정역학 9/16), 글쓰기 9/8은 자료 안 옴 확정에서 제외. 라이브러리 회차 자료 90 · 공통 50.
- (같은 날 추가 2) 글쓰기 9/15 녹음 48분 수신 → 정리 ③ 완성(구성 3·4·5단, 개요 효과 5·종류·유의점, 칼럼 토론) → `progress.json` 글쓰기 3주차 행·「당장」 갱신. 라이브러리 회차 자료 91.

## 2026-09-17

### 데이터 갱신 — 미분적분학2 9/8·9/10 정리 (BUILD 변동 없음)
- 9/10 폴더의 `강의자료_Lecture21.pdf`가 실은 행렬 학습지 14p **아토 풀이본**이라 `2026-09-08/필기_…pdf`로 옮기고 9/8 결석분 정리 작성(Ex05~Ex10 검산 전부 정답, 크래머 j 표기 질문 답). 9/10 지각분은 필기 1장 + 9/15 녹음 역참조로 정리. `progress.json` 미적2 2주차 행 교체, 「당장」 정역학 항목을 보류로. `coverage.json` 미적2 9/8을 자료 안 옴 확정에서 제외 → O. 라이브러리 회차 자료 93.
- (같은 날 추가) 공학 과목 결석분 — 공수1 9/9(슬라이드 p.29~36, 1.4 적분인자 분모 해석 정정)·정역학 9/9(교수 필기본 W12) 정리 O → 자료만(-) 0회차. 공수1 Ch.3~6 슬라이드 4개 `_강의자료/` 저장 + 전체 지도. `progress.json` 공수1·정역학 2주차 행 갱신. 라이브러리 회차 자료 95 · 공통 55.
- (같은 날 추가) 일물2 9/9 결석분 — 강의노트 p.6~7로 정리 O(점전하·점전하군·쌍극자·직선 도선). 자료 안 옴 확정에서 제외. 2주차 결석분 중 남은 n = CADD 9/8·창업 9/7.
- (같은 날 추가) 미적2 9/17 판서 4장 → 정리 O(12.2 벡터 계산, 중간 범위 시작). 빈칸 = 정역학 9/16(보류)만.
- (같은 날 추가) 미적2 9/17 녹음 68분 + 아토 필기 2장 → 정리 ③⑤ 완성(개념 ①~⑤, 12.3 내적 Def·Thm 01 도입, 시험 언급 7건, 필기 부호 오류 1곳 지적). 라이브러리 회차 자료 104.

## 2026-09-15

### .31 — 공업수학1 2주차 과제 로그
- **과제 산출물 3종 등록**(학습시스템 지침 §8.3): `notes/em-hw2-guide.html`(유형 판별·힌트·접힌 검산용 답·답안 형식), `notes/em-hw2-solution.html`(제출용 해답지, 수식 전개만 + 교재 지시대로 1.5 #9·#12 특수해 그래프 SVG), `notes/em-hw2-explain.html`(완전미분·적분인자·선형 공식이 왜 그렇게 되는지 해설 + 9/11 플로차트). `NOTES` 레지스트리 3건, `V27.LOG`에 공업수학1 2주차 링크 3건(boot 패치로 `weekNotes[2].links`에 올라감).
- **오타 검증**: 1차 RED(그래프 누락) → 2차 RED(y축 범위 밖 값을 고정해 가짜 수평선) → 3차 GREEN. 답 5문제는 1차부터 대입·초기조건 검산 통과. 그래프는 함수값 좌표 그대로 두고 `clipPath`로 영역만 가리는 방식으로 수정.
- `V31 LAYER`(파일 끝): 2주차 과제 할 일 두 건의 「교재 문제 원문 미확인」 문구를 「교재 원문 확인(9/15)」로 바꾸고 마감 9/16(수) 09:00을 다시 못박음. 원본 폴더 `study-materials/공업수학1/_과제/2주차_*`(HTML + PDF).
- **버그 수정 A — 복습 채점 저장 회귀**: .25의 `V25 LAYER` `setSession` 래퍼(결석→atom 알림)가 원본 반환값을 버려 `undefined`를 돌려줬고, `gradeReview`가 `s.srs`에서 TypeError → 오늘 탭 복습 큐 채점·학습 마무리 채점이 .25부터 저장되지 않았음. 래퍼가 `s`를 반환하도록 수정. jsdom 스모크에서 같은 오류 86건 → 0건.
- **버그 수정 B**: 주간 할 일 「직접 추가」가 입력칸 없는 화면(개강 전)에서 `.value` null 접근으로 죽던 것 방지.
- 푸시 게이트: `vm.Script` 구문검사 0건 · jsdom 스모크(7뷰+과목 화면 전 버튼, 검사 26건) 오류 0건 × 시점 3개(현재·시험 D-11·개강 전). 오타 회의 GREEN 후 로컬 커밋, GitHub push는 아토 컨펌 대기.
- CHANGELOG에 .26~.30이 빠져 있던 것을 아래에 보충(PATCHNOTES 기준).

## 2026-09-11

### .30
- 주차별 수업 정리도 1주차부터 내려오게 정렬(회차 로그와 같은 순서).

### .29
- 책장 표지에 실제 교재 표지 사진(`covers/`): 공업수학1(Kreyszig 10판) · 미분적분학2(Stewart 9E) · 일반물리학2(Halliday 11판) · 정역학(Pytel & Kiusalaas 4판) · 아카데믹글쓰기. 표지가 있으면 제목 글씨 대신 사진, 아래 진도·출결은 그대로. 교재 없는 과목(CADD 등)은 색 표지 유지.

### .28
- 회차 로그를 1주차부터 내려오게(전에는 최근 주차가 위, 12주까지만 보였음).
- 회차를 열면 맨 위에 「수업 후 올릴 것」 — 녹음본·교재·필기본 3종 업로드 여부. 빠진 것은 빨갛게, 「올리기」로 바로 그 자리로. 그 아래 회차 요약(진도 범위·미완료 과제·정리 요약본 노트 칩). 휴강·학기 밖 날짜 표시.
- 첨부를 못 읽으면 「없음」이 아니라 「못 읽음」으로 구분.

### .27
- **과제 로그**(`V27 LAYER`): 과제 산출물이 과목 화면 「과제」 노트와 주차 정리 링크에 자동 등록. 공업수학1 1주차분(해답지·해설) 반영, 1주차 마감 9/11(금) 09:00 교탁으로 정정·제출 완료 처리.
- 아카데믹글쓰기 주제문 초안(《체인징 배터리》) 과제 노트 등록 · 2주차 할 일 추가.
- 공업수학1 2주차 과제 등록 — 1.5 #9·12(9/11 출제) · 1.4 #4·11·12(9/9 출제), 둘 다 9/16(수).

## 2026-09-09

### .26
- `V26 LAYER`: 학습 탭 상단에 「지식맵 · 이걸 이해하려면」 진입 버튼 — `learn.html?subject=<과목>`으로 연결(지식 그래프·학습 카드·진도 현황은 별도 파일 `knowledge/`·`learn.html`·`coverage.html`, 같은 기간 커밋).

## 2026-09-08

### .25 — 대규모 업데이트 (시험 모드 · 타이머 · atom/LMS 연동)
- **시험 모드 패키지**: 상단 「가용 공부」 게이지 = 시험 시각까지 수업·캘린더 일정·활동 블록·수면(기본 7h)·생활 여유(기본 3h)를 뺀 순수 공부 시간. 학습 탭 맨 위 「시험 모드」 보드: 과목별 D-day, 시험 범위 주차 칩(✓ 공부함 · ▤ 정리만 · 없음, 누르면 학습 화면), 필요 시간(미학습 단원 × 2h + 학습한 단원 × 0.5h) vs 가용 시간 판정, 시험 가까운 순 전략. 오늘 브리핑에 한 줄 + 오늘 목표 시간. D-14부터 켜짐(`phase()==="EXAM"`).
- **공부 타이머**: 오늘 탭 「공부 타이머」 카드 — 과목 칩 → 시작/종료 → `S.studyLog`에 `grade:"timer"`로 기록(학습 탭 누적·시험 모드 계산에 합산). 진행 중 상태는 `localStorage mc-timer`라 새로고침해도 이어짐. 1초 틱은 기존 interval에 `V25.tickTimer()`.
- **atom 연동**: atom 안(`/study/`)에서는 웹훅이 없어도 `fetchGcal`이 atom `/api/cal`로 대체되어 오늘 화면 빈 시간·가용 시간에 캘린더가 반영됨. 출결에서 「결석」을 찍으면(사전 지정 포함) `POST /api/absent` → 아톰이 캘린더 블록을 토마토로. atom 홈·브리핑은 학습앱 상태(`study_state`)의 시험 D-day·시험 모드를 그대로 씀.
- **LMS(Canvas)**: atom에 LMS 토큰이 있으면 `/api/lms/state`를 읽어 할 일 탭 「LMS 과제」 카드(미제출·마감·상태·열기 + 최근 공지), 과목 화면 「LMS」 카드(과제·이번 주 모듈 자료·공지, 과목명 자동 매칭), 오늘 브리핑 「LMS 미제출 N건」. 읽기만, 10분 캐시.
- **정책 반영**(아토 판단 기준 답, 2026-09-08): 시험 모드 중 학습 큐를 시험 가까운 과목부터(과목 가중 없음) · 중간고사 미정 과목은 8주차 가정(`exam.assumed`, CADD 10주차→8주차, 정역학만 확정) · 공개 노트에서 교수 성향 분석 제거(`notes/em-prof-analysis.html` → `Desktop\학습시스템\비공개 노트\`, 레지스트리·주차 링크에서 삭제, 1회 패치 `patchV25b`).
- 폰(atom 안): 상단 저장·새로고침·게이지를 한 줄로. 구현은 기존 함수를 감싸는 `V25 LAYER`(파일 끝)에 모아 두어 위 코드는 건드리지 않음.


### .24
- **강조색을 알파인 그린으로**(아토 지시): 라이트 `--accent:#4F9A35`(글자용 #2F6F22, 연한 배경 #E7F2E1, 선 #BFDDB3), 다크 `#5CAB3F`(글자 #8FD07A). 버튼·활성 탭·타일·게이지·즐겨찾기 강조·로고 배경이 전부 바뀐다. 브라우저 테마색(theme-color)도 같은 색.
- 파비콘 `icon.svg`와 홈 화면 아이콘 `icon-180.png` 배경을 같은 초록으로 다시 생성. 과목 표지 색(유형별 파랑·노랑·주황 등)과 상태색(ok·warn·crit·info)은 그대로.

### .23
- 사이트 이름을 「기계공학 학습 관제탑」에서 **「aTTo 학습앱」**으로 변경 — 브라우저 탭 제목, iPad 홈 화면 이름, 로고 대체 텍스트, README 제목, 노트 허브의 앱 링크 이름. 저장소 이름(study-console)과 주소는 그대로.

### .22
- **로고 교체**: 상단 마크의 글자 기호(◎)를 선형 SVG 아이콘으로 바꿈 — 펼친 책 위에 관제탑(안테나)과 신호 호 두 줄. 디자인 지침(이모지·글자 기호 대신 선형 아이콘)에 맞춤.
- 같은 아이콘으로 브라우저 탭 파비콘 `icon.svg`와 iPad 홈 화면 아이콘 `icon-180.png`(둥근 사각 배경) 추가. 홈 화면 이름 「학습 관제탑」.

### .21
- **「학습」 탭 신설** — 노트 탭을 흡수(노트 목록은 학습 탭 아래 카드로, `go("notes")`는 학습으로 연결). 앱 안에서 수업 내용을 실제로 공부할 수 있게 한 첫 버전.
- **학습 큐**: 과목 × 주차 단원을 복습 예정(FSRS due, 많이 지난 것부터) → 이번 주 → 최근 주차 → 수업 요일 순으로 나열. 「복습할 것 / 전체」 전환(선택 기억). 각 단원에 기억 %·지난 일수·정리 섹션 수·★ 시험 언급·회차 기록·노트 수·마지막 공부 시각.
- **학습 화면**(단원): 상단에 과목·주차·진도 범위·회차 칩(복습됨 표시)·한 줄 요약, 공부 시간 타이머. 본문은 채워진 것만 — ★ 시험 언급 → 5섹션 정리(교재·판서·녹음·강의자료·필기) → 회차 기록(진도·타임라인·아는/막힌/새로 이해/놓친 지점) → 정리 노트 링크(앱 안 뷰어로 열림). 「읽기 / 떠올리기」 모드 — 떠올리기는 섹션 제목만 보이고 펼쳐서 확인.
- **학습 마무리**: 학습 메모(주차 정리에 저장) + 다시/어려움/보통/쉬움 채점 → 그 주 회차들의 FSRS 카드에 반영(오늘 탭 복습 큐와 같은 주기), 공부 시간이 `S.studyLog`에 기록되어 학습 탭 상단에 오늘·이번 주 합계로 표시. 회차가 없는 주는 시간만 기록.
- 진입점: 학습 큐의 「학습」, 과목 화면 주차별 수업 정리 목록의 「학습」, 오늘 탭 복습 큐 각 행의 「학습」.
- 아직 없음: 뒤로 가기만 하면 시간이 기록되지 않음(채점해야 기록), 섹션 단위 퀴즈·빈칸 문제, 학습 타이머의 시험기간 카운트다운 연동.

### .20
- **할 일 주차별 보기 재정렬**: 각 주차 안에서 과목 묶음을 책장 순서 대신 **수업 요일 순**(월→일, 같은 요일이면 시작 시각 순)으로 배치. 수업 시간이 없는 과목은 그 뒤, 개인 공부, 공통 순. 과목 이름 옆에 요일 칩(월·수 등, 개인 공부는 「개인」).
- 주차 제목에 날짜 범위(8/31(월)~9/6(일))와 마감 지난 미완 건수 「지남 n」 배지. 과목 묶음 헤더에도 「지남 n」. 항목을 다 한 묶음은 흐리게.
- 항목 줄 정리: 마감의 요일 중복 표기 제거(「수 9/2(수)」 → 「9/2(수)」), 출처(매주 루틴·수업·주차 정리)는 흐리게, 주차 정리 과제도 다른 항목과 같은 형식(빨간 점 + 과제). 삭제 ✕는 마우스를 올렸을 때만 진하게.

## 2026-09-07

### .19
- **표지 루틴 픽토그램 세로 쌓임 수정**: 픽토그램 묶음 클래스 `.rg`가 오늘 화면의 `.rg`(세로 flex)와 이름이 겹쳐 방향이 세로로 바뀌어 있었다. 표지(`.bk-rt .rg`)와 과목 화면 루틴 띠(`.rt-strip .rg`)에 가로 방향을 명시. 이 때문에 사회봉사·미분적분학2처럼 픽토그램이 2개 이상인 표지가 아래로 길어져 .18의 제목 잘림 원인 중 하나이기도 했다.
- 표지 제목 아래 구분선 위아래 여백 8/7 → 6/5px.
- 표지 높이를 고정값에서 **최소값**으로 바꿈. 배지가 세 줄(요일·재수강·중간·기말)이라 내용이 넘치는 표지가 있으면 같은 줄의 표지들이 함께 조금 길어져 학점 줄과 게이지까지 다 보인다. 책등 보기는 고정 높이 그대로.
- 확인: 헤드리스 Chrome 스크린샷(820px·390px)으로 학교 책장·개인 책장·개인 공부 화면·할 일 주차별·사회봉사 과목 화면 점검. 휴대폰 폭에서 페이지가 가로로 넘치는 것처럼 보였던 것은 과목 현황 표가 카드 안에서 내부 스크롤되는 것이라 실제 넘침은 아님.

### .18
- **표지 제목 잘림 수정**: 표지는 높이가 고정인데 루틴 픽토그램 줄과 배지 줄(요일·재수강·중간·기말)이 두 줄로 늘어나면 제목 요소가 세로로 줄어들어 윗부분이 잘렸다(.10에서 픽토그램을 넣은 뒤부터). 엠블럼·제목·구분선·발치는 줄지 않게 하고 영문 부제가 먼저 접히도록 바꿈. 픽토그램 줄은 한 줄 고정, 휴대폰(≤420px) 표지 높이 172 → 182px.
- **할 일 탭 주차별 보기**: 주차를 1주차부터 오름차순으로 정렬해 지난 주에 안 한 항목이 이번 주 위에 보이도록. 지난 주는 미완 항목이 남은 과목 묶음만 펼쳐지고, 주차 미정은 맨 뒤.
- **사회봉사 규정 반영**(부팅 패치 `patchSvStepsV1`, 1회 적용): 과목 자료의 강의계획서에 1~3단계 규정(소양교육 → 봉사 30시간 9/2~11/29 → 소감문 보고서 포털 입력 9/9~11/30, 3,000byte 이상·띄어쓰기/공란/단락 없이·저장→수정 확인)과 체리 안내(회차 일정, 시간 인정 기준, 해시태그·SNS 조건, 결과보고서 양식 항목, 주의사항, 문의처)를 기록. 과제·할 일에 체리 1~5회차 마감(9/20·10/4·10/18·11/1·11/15, 각 콘텐츠 2건+보고서 2건), 30시간 이수(11/29), 소감문 보고서(11/30), ZOOM 평가간담회(11/26)를 추가.
- 상단 D-day 게이지 라벨에서 「(고정)」 표시 제거(「중간고사 (고정)」 → 「중간고사」). 고정 기준일 기능 자체는 그대로.

### .17
- **책장 분리**: 책장 상단 세그먼트로 「학교 수업」과 「개인 공부」 두 책장을 오간다(마지막 선택 기억). 학교 책장은 기존 그대로.
- **개인 책장**(비교과·개인 공부): 자격증·독학·프로젝트·어학·기타 종류별 표지 색. 항목마다 목표 한 줄, 목표일(D-day 배지), 진도 게이지, 매주 루틴 픽토그램이 표지에 뜬다. 「공부 추가」로 넣고, 길게 누르면 열기·즐겨찾기·완료·편집 메뉴.
- **개인 공부 화면**: 목표·진도(직접 입력 ±5/±10 또는 「단계 추가」 후 단계 완료율 자동 계산), 학습 기록(날짜·분·메모, 누적·이번 주 합계), 매주 루틴(과목과 같은 편집 시트·이번 주 없음 토글), 메모·링크.
- **할 일 연동**: 개인 루틴도 매주 월요일 할 일에 들어가되 학교 과목 다음 순서(출처 「개인 루틴」). 과목별·주차별 보기에 개인 공부 묶음이 표시되고, 「과목 화면」 버튼은 개인 공부 화면으로 연다.
- **완료 처리**: 책장·할 일에서 빠지고 「완료 n개 보기」로 다시 볼 수 있다. 되돌리면 루틴도 복귀.
- 개인 책장은 학점 계산·출결·시험·복습 큐·달력에 전혀 들어가지 않는다(저장 위치 `S.personal`, 학기와 무관).

### .16
- 정역학 9/2(수) 회차를 「수강정정 전 · 출결 제외」에서 「결석」으로 변경. 출석 인정을 요청했으나 9/7에 어렵다는 답변을 받음. 출결 위험도와 지난주 결석 보강 항목에 반영.

### .15
- 정역학 루틴 「과제 제출 ×1(수요일)」 추가. 평소에는 과제가 있으나 9/7 주는 없어서 이번 주만 건너뜀.
- 루틴마다 「이번 주 없음」 토글(과목 화면 루틴 띠의 – 버튼). 그 주만 표지 픽토그램과 할 일에서 빠지고, 다음 주에는 자동으로 돌아온다. 되돌리기는 「이번 주 있음으로」.

### .14
- 할 일 탭에 「전체 / 과제·제출 / 복습 / 예습」 선택 탭. 묶음별·과목별·주차별 세 보기 모두에 필터로 적용되고 마지막 선택을 기억. 한 묶음만 고르면 카드가 전체 폭으로 펼쳐진다.

### .13
- 공업수학1 과제 주기 확정: 금요일 출제 → 수요일 제출(루틴 라벨에 표기). 1주차 과제만 예외로 9/11(금) 수업 시간 제출 — 이번 주 할 일과 1주차 정리 ⑦의 마감을 9/9 → 9/11로 갱신.

### .12
- 학습모드 「M8 과제제출형」 추가. 과목 현황 표에서 사회봉사가 출석유지형 대신 과제제출형으로 표시.

### .11
- 과목 유형 「과제 제출형」(SUBMIT) 추가, 사회봉사에 적용. 시험 없이 매주 산출물을 내는 과목으로, 예습·복습 큐에는 들어가지 않고 루틴으로만 관리.
- 할 일 탭: **묶음별 / 과목별 / 주차별** 보기 전환(마지막 선택 기억). 과목별은 이번 주 항목을 과목 카드로, 주차별은 지난 주까지 전부 주차 → 과목 순으로 접이식 정리.
- 할 일 탭 글자 잘림 수정 — 「과제·할 일 전체」 제목 요소가 시간표용 `.tt` 클래스(grid, 최소 폭 520px)와 충돌하던 문제. 모든 할 일 텍스트에 `word-break: keep-all` 적용.
- 설정에 패치 노트 카드, 저장소에 `CHANGELOG.md`.

### .10
- **과목별 매주 루틴**: 책장 표지와 과목 화면에 픽토그램(업로드·문서·영상·복습·정리)이 주당 횟수만큼 뜬다. 안 하면 회색, 하면 채움, 마감 요일이 지나면 빨간 테두리. 픽토그램을 누르면 완료 처리.
- 루틴은 「할 일」 탭의 이번 주 항목과 양방향 동기화. 월요일마다 새 주로 리셋되고 지난 주 기록은 남는다.
- 기본값: 사회봉사 카드뉴스 업로드 ×2·보고서 제출 ×1, 창업아이디어탐색 수업 후 과제 ×1, CADD 강의영상 시청 ×1(수요일 전), 공업수학1 문제풀이 과제 제출 ×1(수요일), 미분적분학2 수업 복습 ×2, 일반물리학2 수업 내용 정리 ×2. 아카데믹글쓰기·정역학은 미정.
- 설정의 매주 규칙 중 CADD 영상·카드뉴스 규칙은 루틴으로 이관(과목 화면 → 「매주 루틴」에서 편집).

### .9
- **「할 일」 탭 신설**: 이번 주 할 일(과제·복습·예습) + 과제·할 일 전체(주차 정리 ⑦ 항목 마감순, 완료 체크, 주차 정리 열기).
- 오늘 탭은 할 일 요약 한 줄(과제 n · 복습 n · 예습 n · 임박 건수 → 할 일 탭 열기)만 남김.

### .8
- 학점 탭 졸업 계획표: 계절학기 포함 12학기(2026 겨울 ~ 2029-2). 학기마다 계획 과목을 ＋ 과목으로 추가하고 ✕로 빼며 학점은 자동 합산. 잔여 학점 대비 부족/여유와 전공 진행 표시.
- 과목 주차 보드 라벨을 「1주차 … 8주차(중간) … 16주차(기말)」로 변경.
- 사회봉사 P/F 처리: 필요 등급 표에 「P (통과)」, 평점 역산에서 제외, 등급 입력은 P/NP.

### .7
- 학점 탭에 「앞으로의 학기 · 졸업까지」 계획표(학기별 계획 학점·누적·메모, 잔여 학점 검사).

### .3 ~ .6
- 2026 여름학기 성적 입력(행복한금융생활 B+, 미분적분학2 D+), 미분적분학2 재수강 표시(최대 A0).
- 사회봉사 표지 문구를 「주2회 카드뉴스 업로드」로.
- 상단 D-day 기준일을 10/19 중간고사로 고정(설정에서 변경 가능).

### .1 ~ .2
- 매주 월요일 「이번 주 할 일」 자동 생성(① 과제·제출 ② 복습 ③ 예습·공부, 우선순위 과제 ≫ 복습 > 예습) + 설정에서 규칙 편집.
- 상단에 ↻ 새로고침 버튼(캐시 우회).
- 2026-1학기 이력 시드(9과목 20학점).
- 오늘 타임라인: 구글 캘린더 일정을 수업 사이 빈 시간에 표시(얼라이브위크 웹훅 GET `?action=events`), 수업을 일찍 끝내면 실제 종료 시각부터 빈 구간.

## 2026-09-04 ~ 09-05
- 강의 정리 노트를 앱에 통합: `notes/` 폴더, 노트 탭, 앱 내 뷰어, 과목 화면 문서 목록.
- 회차 로그를 주차 묶음 + 요일별 한 줄(주 2회 과목은 두 줄)로, 이번 주 예정 회차 포함.
- 「W1」식 표기를 「1주차」로 전부 통일.
- 주차별 수업 정리(① 교재 ② 판서 ③ 녹음 ④ 강의자료 ⑤ 필기 ⑥ ★시험 언급 ⑦ 과제→할 일) 추가 + 1주차 내용 시드.
- 정역학 강의계획서·1주차 OT, 아카데믹글쓰기 1주차 자료, 일반물리학2 슬라이드 반영. 정역학 9/2 회차는 수강정정 전이라 출결 제외.
- 문자열 줄바꿈 이스케이프 오류로 앱이 안 뜨던 문제 수정.
- 설정에 빌드 번호 표시.

## 2026-09-01 ~ 09-03
- 복습 큐 버그 수정(1·10분 뒤 재노출 제거, 다음 복습일 하루 밀림).
- 얼라이브위크 연동: 출결·수업 시각을 구글 캘린더에 자동 반영.
- CADD 중간고사를 이론 암기 위주로 반영.
- 상단바 오늘 날짜·요일, D-day를 가장 빠른 중간고사 기준으로, 게이지 라벨 정리.
- 자동 수업 시작, 휴강 사전 지정, 오늘 화면 주간 요일 스트립.
- FSRS 복습 큐 연결(srs-core 인라인 번들), 회차 심화 기록.

## 2026-08-29
- 학습 관제탑 v0.1 초기 커밋.
