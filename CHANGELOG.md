# 학습 관제탑 패치 노트

설정 화면의 「패치 노트」 카드에는 최근 항목만 보이고, 전체 기록은 이 파일에 쌓는다. 빌드 번호는 설정 하단(상단 학기 태그에 마우스를 올려도 보임)에서 확인한다. 배포 후 GitHub Pages 반영까지 최대 10분, iPad에서는 ↻ 버튼으로 새로 고친다.

## 2026-09-27

### .76 — 오타 2차 검수(RED 5건, `docs/otta/_v54-otta-2.md`) 반영
- 교실: 저장 위치 `L.pos` 를 `posOf()` 로 정수·범위 정리(문자열 `"2"+1="21"` 로 목차의 「이어서 하기」 뒤 3단계에서 챕터가 완료되던 것). 목차 재개·`#resume` 둘 다 같은 경로.
- 경로(V55): `unitOf` 가 장 번호(`num`) → 키워드(`kw`) 순으로 대조(「13.1 벡터함수」가 「벡터」로 12장에, 「Ch.8」이 「Moments」로 Ch.4에, 「29장」이 「자기장」으로 28장에 잡히던 것). units.json v2 = `num`/`kw` 분리. 지난 시험은 그 단원 안에서 회차 사이 날짜 순, 첫 회차보다 앞이면 맨 앞(전에는 사라짐). 앞으로 부분은 시험마다 [그 전 예정 회차] → [그 시험 범위(phase)의 단원] → [★] — 중간이 지나면 기말 범위 단원이 기말 앞에 온다(전에는 「기말 뒤」).
- 대비: 어두운 테마 글자 토큰(`--duo-blue-text #5AC8FA · green #7ED957 · red #FF7B7B · orange #FFB347`, `--crit #FF6B6B`)을 V54 에 두고 V55 상태색·버튼 글자가 이를 쓴다. 단원 배너 글자(어두움 55%·흰색 혼합)·진행 막대(밝음 45%·검정 채움 / 어두움 어두운 트랙 + 55%·흰색 채움) 3:1 이상.
- 검사기 `docs/tools/v55_path_check.cjs`(unitOf 우선순위 · 지난 시험 위치 · 기말 앞 단원 · 전부 보기 과목별 · 어두운 토큰) + `classroom_check` 문자열 위치 시나리오.

### .75 — 오타 사후 검수 1차(RED 7건, `docs/otta/_v54-otta-1.md`) 반영
- 교실(`classroom_tpl.html`): ① 정답 → 다시 풀기 → 정답으로 XP 가 두 번(15→30) 붙던 것 → 문제별 첫 보상 기록 `L.rew` 로 한 번만. ② 23:59에 열어 자정을 넘기면 하트·연속일이 전날 기준이던 것 → `dayReset()` 을 행동마다(채점·챕터 완료·문제 열기). ③ 재빌드로 단계 수가 줄어 저장 위치가 범위 밖이면 `#resume` 이 안 읽은 챕터를 완료로 치고 XP 를 주던 것 → 마지막 단계로 보정, 챕터 완료는 마지막 단계의 「이해했어요」(next)에서만. ④ 하트 0에서 입력형 「답 보기 → 맞았어요」가 통하던 것 → 객관식과 같이 막음.
- 빌더(`build_classroom.py`): 섹션 첫 문단의 `data-tutor` 에서 IndexError → 문단 버퍼와 함께 대사를 보관. 그 과정에서 h3 소제목이 그림·상자 단계 앞에서 유실되던 회귀를 검사기가 잡아 고침(비울 게 없을 때 소제목 초기화 금지).
- 스킨(V54)·경로(V55)·교실: 시트 뒤 `.scrim` 흐림 해제. 글자 대비 4.5:1 — 보조 글자 #707070, 초록 글자 #2E7D00, 주황 #B85C00, 빨강 #D42020, 파랑 #0A7BBF, 초록 기본 버튼은 밝은 초록 바탕에 짙은 초록 글자(7.2:1), 단원 배너는 연한 단원색 바탕 + 짙은 단원색 글자, 마디 아이콘은 짙은 단원색. 밝은 듀오링고 색은 바탕·테두리·큰 도형에만.
- 검사기 `classroom_check.cjs` 에 위 ①③④ 시나리오와 챕터 재완료 XP 불변 추가. 2차 검수 프롬프트 `otta_v55_prompt.txt`.

### .74 — 학습 경로를 단원(장)별로 (대표님 2026-09-27 "단원별로 진도 구분잘해줘")
- **`knowledge/units.json`**(새 파일): 과목별 단원 목록 — 일물2 21~32장 · 정역학 Ch.1~8(OT 슬라이드 주차표) · 공수1 1~6장 · 미적2 행렬/벡터와 공간/벡터함수/편도함수/다중적분. 항목마다 `dates`(그 단원에 속하는 회차, 아톰이 회차 정리 때 채움) · `match`(회차 제목 대조 정규식, 날짜가 없을 때) · `phase`(mid/final) · `planned`(아직 회차가 없는 단원의 근거 = 강의계획서 주차). 근거 없는 단원은 만들지 않았다.
- **V55 경로**: 회차 마디를 단원 배너(단원 n · 제목 · 따라감 n/m · 진행 막대, 단원마다 색, 지금 단원은 테두리 강조 · 완료 ✓) 아래 묶고 마디 색도 단원 색을 따른다. 날짜·제목 어디에도 안 맞는 회차는 「단원 미지정」 배너로 드러난다(units.json 에 넣으라는 뜻). 예정 회차는 다음 시험 전까지 작은 마디로 4개만(전부 보기 토글), 그 아래 「앞으로 배울 단원」(planned) → 시험 ★ → 「시험 뒤」 단원 목록. 지난 시험은 그 날짜 전에 끝난 단원 뒤에. 마디 라벨에 주차 표시. 히어로에 「단원 n · 제목」 칩. units.json 이 없는 과목은 전처럼 주차 머리말.

### .73 — 과목 화면 개편 「학습 경로」 · 김주영 스앵님 캐릭터 보완 (대표님 2026-09-27 "학습앱 메인 과목화면도 개편해줘 · 이름은 김주영 스앵님 · 캐릭터 디자인 보완")
- **`V55 LAYER`**(`docs/layers/v55-layer-src.js`): 과목 화면 = 히어로 카드(과목 · 교수 · 요일 · 출결 · 따라감/밀림/이 과목 XP/시험 D-day · 진행 막대 · [교실 열기 · 날짜] [교재] [세부정보] + 김주영 스앵님 말풍선) + **학습 경로**(회차마다 동그란 마디 — 따라감 ✓ 초록 · 진행 중 = 읽은 챕터 비율 고리 · 밀림 빨강 · 오늘 파랑 · 이어짐 · 정리만 · 정리 대기 점선 · 예정 잠김 · 시험 ★ 노랑 — 주차 제목(map.json) 아래 지그재그, 다음에 할 마디는 크게 + 「시작/이어서」 말풍선 + 옆에 스앵님, 결석 배지 · 확인 문제 점수 배지). 마디 = 교실(V53→V38.deckFor) / 회차 정리(V36) / 시험 마디 = 범위·비중 펼치기. 기존 회차 표는 「회차 표 펼치기」로 접힘(기본 접힘, `mc-v55-table`), 세부정보·기능은 그대로. 판정은 수업 따라가기(V50.session)와 같은 기준, 진행률은 `mc-lesson-<id>`, XP 는 `mc-tutor`.
- **캐릭터**: 원본 `docs/tools/tutor.svg` 하나 — 가운데 가르마로 뒤로 넘긴 머리 + 낮은 쪽머리, 진주 귀걸이, 검정 터틀넥 + 블레이저 + 금 브로치, 눈꼬리 올라간 눈(깜빡임), 진한 붉은 입술, 표정 6종. `build_classroom.py` 가 교실 24회차에 인라인하고 `notes/classroom/assets/tutor.svg` 로 복사(앱 V55 가 fetch; 처음 `_assets` 로 두었다가 GitHub Pages 가 `_` 폴더를 올리지 않아 404 → `assets`). 이름은 교실 이름표·앱 목록 전부 「김주영 스앵님」.
- 버그: 회차 표(V37)의 현재 주차 scrollIntoView 가 경로 아래로 페이지를 확 내리던 것 → 그리기 전 스크롤 위치 복원.
- 검증: 구문 0 · 레이어 · `_v53chk` 21 · `classroom_check` 24/826 0건 · 1040·390 가로 넘침 0 · 라이트/다크/폰 캡처.

### .72 — 듀오링고식 앱 스킨 (대표님 2026-09-26 "학습앱 전체 UI 디자인이 별로 → 세련되고 이쁘게 감각적으로", 지침 §23 듀오링고 톤 · 아이패드 1040 먼저)
- **`V54 LAYER`**(`docs/layers/v54-layer-src.js`, CSS 만 — 구조·기능 변화 없음): Pretendard 굵은 글꼴(본문 500·제목 800), 바탕 평평한 #F7F7F7(글래스 층 V51 의 유리·블러·번지는 빛은 토큰을 단색으로 덮어 끔), 카드·타일 2px 테두리 #E5E5E5 + 16px 모서리 + 그림자 없음, 버튼 2px 테두리 + 4px 바닥의 눌리는 3D(기본 = 초록 #58CC02, 보조 = 흰 바탕 파란 글자 #1CB0F6, 위험 = 빨강), 현재 탭 = 하늘색 바탕 + 파란 테두리, 세그먼트·입력·칩 굵게, 폰 탭바 2px 테두리 + 바닥 그림자. 어두운 테마 = 짙은 청록 바탕(#131F24 / 면 #1B2A31 / 선 #37464F).
- **설정 → 「화면」 카드**: 듀오링고 톤 ↔ 클래식(글래스) 전환(`mc-skin`, `?skin=classic`). 스킨이 꺼져 있어도 카드는 남는다.
- 검증: 구문 0 · 레이어 21 · `_v53chk` 21 · `v50_status_check` · 1040 오늘/학습/과목/책장/할 일/설정/달력/라이브러리 + 390 오늘/학습/설정/과목 가로 넘침 0 · 라이트/다크/폰 캡처 눈 확인.

## 2026-09-26

### .71 — 교실 모드 (초록 칠판 + 스앵님) · 24회차 전부
- **교실 페이지 `notes/classroom/<약칭>/<날짜>.html`** (`docs/tools/classroom_tpl.html` + `build_classroom.py`, 수업 노트와 같은 원본에서 생성 — 원본은 한 곳): 챕터(= 원본 섹션) 목차 → 한 단계 한 화면(글 2문단 · 그림 · 왜 · 비유 · 외울 것 · 함정 · 정리 · 교수님 말 · 예제)으로 초록 칠판에 판서, 그림은 칠판에 붙인 종이(교재와 같은 원본 SVG — fig_check 0건 그대로), 예제는 생각한 뒤 「풀이 보기」, 확인 문제 6개는 칠판에서 바로 채점(숫자 키). 스앵님 대사 풀 30여 종(훅·왜·예제·함정·정리 톤, 호감도 60 이상이면 따뜻한 줄) + 원본 `data-tutor` 로 문장 지정 가능.
- **스앵님 캐릭터**: 검정 정장·낮게 묶은 머리·날카로운 눈의 SVG 치비, 표정 6종(무표정·날카로움·미소·화남·놀람·흡족) + 깜빡임·숨쉬기. 이름표 「스앵님」, 교수님 인용은 「교수님 말」 상자로 분리.
- **게임 요소(최대한 심플, 저장 키 `mc-tutor` 하나)**: XP(챕터 10 · 정답 15 · 회차 완료 20)·레벨(150 XP) · 연속 학습일(어제 이어지면 +1) · 하트 3(오답 −1, 완료한 챕터 다시 읽으면 +1, 매일 3개) · 호감도 0~100(챕터 +2 · 정답 +3 · 오답 −1 · 질문 +1, 「무관심 → 지켜보는 중 → 인정 → 전적으로 믿는」). 회차 끝 결과 칠판(정답·XP·연속일·배지). 챕터·문제 기록은 `mc-lesson-<id>`에 같이 써서 수업 노트·앱 V52 판정과 한 몸.
- **앱 연결(V53)**: 회차 표 [학습하기]·수업 따라가기 [교실 열기]가 교실을 열고 [교재]가 읽기용 노트를 연다. 과목 화면 노트 목록에 「교실 · 날짜 · 제목」. 교실의 [교재 보기]·[질문할게요]·[앱으로]는 postMessage(mc-open · mc-ask · mc-close, notes/ 아래 경로만). **질문 패널 문맥이 수업 노트·교실에서도 붙는다** — V45.deckOf 가 `notes/<이름>-slides.html` 만 알아 노트·교실의 문맥이 채택되지 않던 것 고침. 이어서 학습하기 카드는 「CH n/m」.
- **버그**: 빌더의 `inner()`가 요소 앞 글자를 풀린 채(`&lt;` → `<`) 내보내 \[r<R …\] 의 `<R` 이 브라우저에서 태그로 읽혀 수식이 사라졌다(일물2 9/16 CH3) → 이스케이프. `figs.py text()` 도 라벨의 `< > &` 이스케이프(「r<a: …」 라벨이 `<a` 태그로 깨지던 것, 같은 회차 그림). 24회차 원본·노트·교실 전부 재생성.
- 검증: `classroom_check.cjs`(24회차 826단계 전부 렌더 · 문제 흐름 · 원본 글 유실 0 · 저장 키) · `_v53chk.cjs` 21건 · fig_check 0 · KaTeX 0 · 아이패드 1040·폰 390 캡처 눈 확인. 레퍼런스 정리 `Desktop\학습시스템\레퍼런스\2026-09-26_학습지·교육앱 레퍼런스.md`.

### .70 — 그림 품질 0건 (대표님 "학습자료 그림은 수식 오류·깨진 글자·글자/선 겹침이 절대 없어야")
- **검사기 `docs/tools/fig_check.cjs`**: 헤드리스 크롬으로 24회차 118개 SVG 그림을 실제 폰트로 렌더해 글자 상자 겹침(tt)·선 관통(tl)·획 관통(tp)·캔버스 밖(out)·결합 문자(glyph)를 잡는다. 첫 실행 218건(중복 제외) → 전부 수정 → **0건**. KaTeX 오류 0.
- 공통 수정(`figs.py`): 작은 원의 부호 크기 자동, 평행판 기호 크기 자동, 흐름도 상자 글자 자동 축소, 3D 축 x 라벨 위치, 결합 화살표·모자(a⃗·n̂) → 굵은 글자+위첨자, 글자 흰 테두리(halo), 사루스·지우기 대각선은 글자를 관통하는 선 대신 반투명 띠(`band`), 구 안의 전하 기호는 동심원 배치.
- 개별 수정 60여 곳(라벨 위치·캔버스 크기·축 길이). 템플릿 주석의 자리표시 글자가 치환돼 본문이 두 번 들어가던 문제 고침(파일 절반).
- 지침 학습시스템 §22·§23(교실 모드 확정) 추가.

### .69 — 미분적분학2 수업 노트 7회차 · 중간고사 4과목 24회차 완성
- **미분적분학2 9/1·9/3·9/8·9/10·9/15·9/17·9/22**: 행렬의 정의·5종·학습지 지도(9/1) → 연산·곱셈 조건·행렬식 \(ad-bc\)·사루스·예제 3·4(9/3) → 여인수 전개·2×2/3×3 역행렬(adj)·연립방정식·크래머 Ex05~Ex10(9/8, 결석분 학습지+아토 풀이 검산본) → 12.1 좌표계 3종·원주좌표·곡선/곡면·점 찍기(9/10, 지각분 판서 1장+9/15 언급) → 곡면 그리기 규칙·거리·구면 완전제곱·벡터 기하 표현·위치벡터(9/15) → 두 점 벡터·크기·연산 닫힘·표준기저 i j k·단위벡터·Ex01~04·내적 Def/Thm 도입(9/17) → Thm 01 증명(지각 구간 교재 보충)·방향각·방향코사인·Ex04·외적 정의·유도(9/22). `figs.py`에 행렬 그림 조각 `mat`(대괄호/세로줄·강조 칸).
- 이로써 **중간고사 4과목(일물2 6 · 정역학 4 · 공수1 7 · 미적2 7) 24회차 수업 노트** 완성. 회차마다 그림·비유·외울 것 상자, 확인 문제 6, KaTeX 오류 0, 교수 실명 0.
- 템플릿: 폰 폭(≤640px)에서 그림을 축소하지 않고(글자 7px) 가로 스크롤로 본다(`figure.fig` overflow-x, svg min-width 500px). 24회차 전부 재빌드.
- 오타 2차 검수(채점 고침·정역학·공수1·미적2)는 Codex 한도 복귀(9/27 01:41) 뒤.

### .68 — 공업수학1 수업 노트 7회차
- **공업수학1 9/2·9/4·9/9·9/11·9/16·9/18·9/23**: 모델화 3단계·ODE 분류(9/2, 결석분 슬라이드 재구성) → 초기값 문제·방사능 붕괴·변수분리·토리첼리·동차형 \(y=ux\)(9/4) → 완전미방 판별·Case 1/2·적분인자 정리 1·2(p→q 정정 반영)·Ex.5(9/9, 결석분) → 선형 표준형·해 공식·판별 플로차트·예제 1·연습 3제(역수 기술)·RL 회로·베르누이 예고(9/11) → 베르누이·치환 연습 3제·1장 해법 총정리·2.1 중첩 원리·IVP·1차독립(교수 정정)·차수축소법(9/16) → Ex.7 마무리·2.2 특성방정식 3경우·Case II/III 유도·Ex.2/4/5·2.3 미분연산자(9/18, 결석분) → 2.5 오일러-코시 3경우·예제 3·2.6 론스키안(9/23). 회차마다 그림 4~6·확인 문제 6, KaTeX 오류 0, 교수 실명 0.
- `figs.py`: `polyline`·`fplot`(함수 그래프)·`fbox`·`diamond`(흐름도) 추가. 오타 2차 검수는 Codex 한도(9/27 01:41 복귀) 뒤 사후.
- 남은 과목: 미분적분학2(9/1~9/22 7회차).

### .67 — 오타 검수(v53 1차) RED 반영 · 정역학 수업 노트 4회차
- **확인 문제 채점 고침**: 템플릿 `$$`가 범위(el)를 무시해 모든 보기가 마지막 문제에 묶였다 → 문제 카드마다 보기를 따로 묶음. 회차 끝 안내에 정답 수와 따라감 기준(문제 60% 정답) 표시. 검사 `docs/tools/lesson_tpl_check.cjs`(jsdom: 클릭 범위·다시 풀기·mc-slides-last·완료 문구).
- **이어서 학습하기(V43)**: 수업 노트의 중첩 경로(`notes/lessons/…`)를 과목에 연결하고 「섹션 n/m」으로 표시(전에는 7/6장). 템플릿 idx = 읽은 섹션 − 1. 검사 `docs/tools/v43_lesson_check.cjs`.
- **일반물리학2 노트 정정·보강**: 9/4 예제 02 거리 7.0×10⁻¹⁰ m(슬라이드 원문, 아톰이 5.0으로 잘못 옮김) → 2e 검산 일치. 9/16 "9/9의 적분과 같다" → 무한 도선(옆)과 유한 도선(연장선)은 다른 배치. 여섯 회차 전 섹션에 그림·비유·외울 것 상자, 9/9·9/23 본문 보강.
- **정역학 4회차**: 9/7 역학 정의·단위·뉴턴 법칙·벡터 표기·덧셈·단위벡터 · 9/9 성분·위치벡터·방향여현·내적 정의(결석분, 필기본+LMS 영상) · 9/14 내적 성질·사이각·정사영·외적·행렬식 · 9/16 혼합삼중적·Ch.3 힘의 종류·접촉력·장력·도르래·스프링·평형·자유물체도. `figs.py`에 3D 축·각도 호·블록 조각 추가. KaTeX 오류 0, 교수 실명 0.

### .66 — 수업 노트(회차 학습 페이지) · 일반물리학2 6회차 (대표님 2026-09-26 "공부하는 화면이 공부하기 좋은 화면이 아니다 · 지난 수업 진도정리 · 교수님 설명 과정대로 그림·원리·흥미")
- 대표님 결정 4건(질문 도구): ① 개념은 스크롤 교재, 문제는 카드 ② 아톰 SVG 그림만 공개, 판서 사진은 atom 안에서만(`notes/lessons/_private/` gitignore) ③ 원리 먼저·그림·비유 많이·회차당 15~20분 ④ 순서 일물2 → 정역학 → 공수1 → 미적2.
- **수업 노트 파이프라인**: 원본 `study-materials/<과목>/_수업노트/<날짜>.html`(생성기 `docs/tools/lessons/<약칭>_<날짜>.py`, 그림은 `docs/tools/figs.py`) → `docs/tools/build_lesson.py` → `notes/lessons/<약칭>/<날짜>.html`(템플릿 `docs/tools/lesson_tpl.html`) + `knowledge/lessons.json`. 페이지 = 상단 고정 진행(읽음·문제·%) · 회차 도입 · 섹션(왜 그런가/교수님 말/비유/외울 것/함정 상자 · 인라인 SVG · 판서 사진(atom 안) · 예제 접기) · 확인 문제 카드(보기 즉시 채점·답 입력형 자가 채점·틀린 문제 다시). 읽음 = 섹션이 화면에 머물면 자동. 저장 `mc-lesson-<id>` + 「이어서 학습하기」 키. 앱 질문 패널 문맥(mc-slide) 전송.
- **일반물리학2 6회차**: 9/4 전하·쿨롱·전기장 정의 · 9/9 전기장 계산 ①~④(결석분 재구성) · 9/11 원형 도선·원판·가우스 서론 · 9/16 가우스 3종·도체구·부도체구·범위별 문제 · 9/18 전위·증명 3제·25장 도입 · 9/23 축전기 3종·유전체. 회차마다 그림 3~5 · 확인 문제 6. KaTeX 오류 0.
- `V52 LAYER`: 수업 따라가기(V50)·회차 표 [학습하기](V38)·과목 노트 목록이 수업 노트를 먼저 연다(완료 = 섹션 전부 읽음 + 문제 90% 풀고 60% 정답). 노트 없는 회차는 덱 파트 그대로. 검사 `_v52chk.cjs` 11항목.
- 다음: 정역학 → 공업수학1 → 미분적분학2 순으로 회차 노트 제작(대표님 순서).

## 2026-09-25

### .65 — 오타 재검수(v52 2차) RED 3건 반영
- **공유 파트는 한 번만 판정**: 여러 회차에 걸친 덱 파트는 마지막 회차(judgeAt)에서만 완료를 판정. 앞 회차는 그 파트가 끝났으면 「따라감」, 아니면 새 상태 「이어짐」(밀림 집계 제외, "○/○에서 판정" 표시). 회차별 필수 슬라이드 분리는 정리노트를 회차 단위로 나눌 때.
- **오늘 회차**: 진행 중이어도 「오늘 수업」(밀림 아님). 오늘 탭 브리핑도 「밀린 회차 N개 · 오늘 수업 M회차 · 가장 오래된 밀림 …」으로 분리. 검사 `docs/tools/v50_today_check.cjs`(today=9/23 시뮬레이션) 추가, `v50_status_check.cjs` 기대값 갱신.
- 블러 미지원 폴백에 `.card .card` 추가(특이성 일치).

### .64 — 오타 사후 검수(v52 브리프: 수업 따라가기 · 글래스) 반영 · RED 3건 수정
- 오타 판정 `docs/otta/_v52-otta-1.md`: 해석 GREEN / 회차 연결·상태 판정·글래스 폴백 RED.
- **상태 판정(V50)**: 완료 = 모든 파트가 「개념·암기 장 전부 읽음(건너뜀 제외) + 문제 90% 이상 풀고 60% 이상 정답」. 평균 90%·건너뜀 100%·전부 오답 100%로 「따라감」이 되던 것 수정. 「문제만」(#quiz) 정답도 인정. 오늘 수업은 밀림과 따로 집계. 정리 없는 회차엔 「회차 정리 전 — 덱은 예상 진도」, 여러 회차가 공유하는 파트엔 「(9/9·9/14 공유)」 표시. 검사 `_v50chk2.cjs` 8항목 추가.
- **회차 연결(deck_index)**: 파트 날짜를 달력 유효성·coverage.json(수업 없는 날·휴강)과 대조해 걸러내고 WARN 출력, 날짜 없는 파트 WARN. 현재 9덱 40파트 WARN 0. 미결: 한 파트가 두 회차에 걸칠 때 회차별 학습 범위 분리(정리노트를 회차 단위로 다시 나눠야 함 — 다음 정리 때).
- **글래스**: 중첩 카드는 블러 없이 더 불투명하게(탁해짐·성능), 라이트 보조 글자 --ink-2/--ink-3 를 유리 위에서도 4.5:1 이상으로(#5B6A60/#66736B), 블러 미지원 폴백에 뷰어 헤더·연동 배너·세그먼트·아이콘 버튼 추가. 덱 템플릿 폴백에 풀이판·필기판·입력·판정 추가 → 덱 9개 재빌드(검사 결과 동일).
- 다크 실측 대비·아이패드 블러 지연은 캡처로만 확인(수치 측정 없음) — 대표님 실사용 후 조정.

### .63 — 오타 사후 검수(v51 브리프) 반영 · RED 2건 수정
- 오타 판정 `docs/otta/_v51-otta-1.md`: 저장 분리·D-14 관계 GREEN / 범위 집계·미적2 선택지 RED. 계산 문항 16개 검산은 전부 일치.
- **범위 집계**: 정역학 중간 범위가 "Ch.3~5"(단원 표기)라 `V25.examWeeks`가 1주차부터 잡아 OT·Ch.2가 범위·이해도에 섞였다 → V49가 `examWeeks`를 감싸 단원 시작 주차(정역학 3주차, 9/16 "여기서부터 중간고사 범위")부터로 보정. V47 이해도·V25 시험 보드도 같은 함수를 쓰므로 함께 맞는다. 허브 범위 줄에 「범위 주차 3~4 — 이유」 표시, 주차 미공지 과목은 「지금까지 1~4주차 기준」 표시.
- **미적2 벡터 덱** 기초 1-1·1-2·2-3: 정답과 같은 값(√9=3 · √8=2√2 · √98=7√2)이 보기에 있어 단일정답이 아니었다 → 다른 값(2√2 · √6 · 5√2)으로 교체, "근호 정리" 교훈은 해설로. 덱 재빌드(56장 · 검사 0).

## 2026-09-24

### .62 — 글래스 스킨 (대표님 2026-09-24 "학습앱도 글래스모피즘 형식으로 세련되게, 가능한 심플하고 깔끔하게")
- `V51 LAYER`(CSS만): 바탕 = 부드러운 그러데이션 + 번지는 빛 2개(고정) · 유리 패널 = 카드·상단바·PC 레일·시트·토스트(반투명 + 블러 + 흰 헤어라인 + 옅은 그림자) · 안쪽 행(과목 행·덱 행·회차 행·주차 행)은 더 옅은 유리 · 버튼·세그먼트·칩·입력도 유리, 행동 버튼은 알파인 그린 그대로. 글자 토큰(--ink)은 안 바꿔 대비 유지. 다크 = 어두운 유리 + 흰 글자(atom 메인 글래스 시안과 같은 규칙). `backdrop-filter` 미지원은 불투명 폴백. 마크업·배치 변경 없음.
- 덱 템플릿 `V6F`(`docs/tools/patch_tpl_v6f.py`): 같은 바탕 + 무대·상단·하단 바·보기 카드·버튼·개념 상자·시험 언급·풀이 판을 유리로. 테두리 두께·여백은 그대로라 1024×768 배치 검사 결과 불변. 덱 9개(문제 덱 5 + 임시 덱 4) 재생성.
- 오늘 탭 브리핑 줄의 긴 글이 폰(390)에서 가로로 넘치던 것(예전부터) `.step>*{min-width:0}`로 수정.
- 캡처 확인: 학습·오늘·할 일·과목 탭 1024 라이트/다크 · 폰 390 학습·오늘.

### .61 — 수업 따라가기 (대표님 2026-09-24 "이해를 돕는 「학교 수업진도 따라가기」 구현이 시급")
- `V50 LAYER` **학습 탭 첫 카드 「수업 따라가기」**: 과목마다 지금까지의 회차(주차×요일)를 점으로 — 따라감(초록)·진행 중(노랑)·밀림(빨강)·정리만 있음(파랑)·정리 대기(점선)·다음 수업(빈 원), 결석 회차는 테두리. 「여기까지 · 다음 따라갈 회차 [따라가기] · 다음 수업(예정 제목)」 세 줄, 펼치면 회차 목록(상태 칩·제목·덱 파트 진행·문제 정답).
- **「따라가기」** = 그 회차와 이어진 덱 파트의 표지(`#at=pN-cover`)에서 시작 → 개념 → 암기 vs 이해 → 기초 → 응용(지침 §11-6 루틴). 회차 ↔ 파트는 `knowledge/decks.json partList.dates`(덱 표지의 날짜, `deck_index.py`가 파싱)로 잇는다. 문제 덱이 없는 회차는 임시 덱 파트, 그것도 없으면 「회차 정리」. 정리도 없으면 「정리 대기(아톰이 정리하면 열림)」.
- 오늘 탭: 상태 칸 아래 **「수업 따라가기」 카드**(과목별 다음 따라갈 회차 오래된 순 4개 + 따라가기 버튼) + 브리핑 첫 줄 「밀린 회차 n개 · 가장 오래된 것」.
- 회차 색인 `knowledge/sessions.json` ← `docs/tools/session_index.py`(study-materials 회차 정리 파일의 제목·진도 한 줄, 강의 내용만). 새 회차 정리가 생기면 실행.
- 검증: 구문 0 · 레이어 20/20 · V50 jsdom 15항목(카드 위치·점 상태·따라가기 링크·펼치기·오늘 줄) · V49 11 · 스모크.

### .60 — 중간고사 대비 허브 · 「문제만 풀기」 · 시험 범위 문제 덱 4개 (대표님 2026-09-24 "학습앱에서 시험공부할 수 있게 세팅")
- `V49 LAYER` **「중간고사 대비」 허브**: 학습 탭 맨 위(시험 모드 보드 아래 · 커리큘럼 카드 위), D-14 시험 모드와 별개로 지금(D-25)부터. 과목 행 = D-day(가정 표시) · 범위(exam.scope) · 범위 안 진도(map.json 주차 제목) · 학습 덱(문제 덱 우선, 「학습」#resume / 「문제만」#quiz, 진행·정답률) · 이해도(V47 노드). 「오늘」 = 시험 가까운 과목부터(지침 §4), 같은 날이면 이해도 낮은 과목. 데이터 `knowledge/decks.json`(`docs/tools/deck_index.py` 생성) — 못 읽으면 원인 한 줄 + 덱 없이 행 표시.
- 덱 템플릿 `V6E` **「문제만 풀기」(#quiz)**: 문제 장만 + 시작·전체 결과, 저장 키 `mc-slides-<deck>-quiz` 분리(학습 진행·이어서 학습 위치 보존). `docs/tools/patch_tpl_v6e.py`.
- **시험 범위 문제 덱 4개**(정리노트 v2 → build_slides, 1024×768 오버플로 0 · 자기완결 0): 정역학 `statics-mid`(Ch.2 벡터·Ch.3 힘·평형, 5파트 63장 31문제) · 일반물리학2 `phys2-mid`(21~25장, 5파트 70장 34문제) · 공업수학1 `em1-mid`(1장·2.1~2.3·2.5, 6파트 81장 36문제) · 미분적분학2 `calc2-vectors`(12.1~12.4, 5파트 56장 25문제). 원본 정리노트는 `study-materials/<과목>/_정리노트/2026-09-24_*_중간대비_*.html`. 행렬 덱(calc2-matrix)은 새 템플릿으로 재생성(89장 41문제).
- 일반물리학2 중간 범위를 교수 발언(9/18 녹음 40:48 "27장 RC 회로까지, 28장 제외" · 증명 3제 중 1문제 20점 · 도체구/부도체구 100%)대로 갱신(boot 패치 1회). 다른 과목 범위는 미공지라 손대지 않음.
- `check_q_refs.py` 확장: 물리·정역학 기호("전기장 \(E\)", "장력 \(T\)")를 정의로 인정, `div.q data-def="E,V"` 선언 지원, "전위 "·"단위 "의 "위 "를 참조로 오인하지 않게 한글 경계 적용.
- 검증: 구문 0 · 레이어 18/18 · 스모크(기존 CADD 9/15 1건 제외) · V49 jsdom 11항목(D-day 순·오늘 줄·문제만 버튼은 문제 덱만·정답률 표시·27장 갱신·decks.json 없을 때 안내) · 덱 4개 #quiz 모드 슬라이드 수 = 문제 수 + 2.
- 오타: Codex 사용 한도(9/25 00:07 복귀)로 브리프 `docs/briefs/v51-exam-prep-brief.md` 사전 회의 못 함 → 지침 §13대로 진행, 사후 검수(설계 + 덱 문제 검산) 예정.

### .59 — 연동 끊김 배너 · 유실된 레이어 복구 (대표님 2026-09-24)
- **연동 상시 점검**: PC 예약 작업 **「아톰_연동점검」 매시간**(`기술실/integrations/check_integrations.ps1`) — 아톰 크롬 · 클로바노트 · 한양 LMS · 체리스쿨 · iCloud 사진 · 구글 드라이브 · 수집 작업 4개. 로그인 여부는 기존 수집기로만 읽고(로그인 동작 없음), 탭이 없으면 열어 세션을 유지, 꺼진 수집 작업은 다시 켠다. 결과 `knowledge/integrations.json`(.gitignore) · `study-materials/_연동상태.md` · `_연동상태_기록.txt`.
- `V48 LAYER`: 오늘 탭 맨 위 **연동 끊김 배너**(무엇이 · 무엇이 멈췄는지 · 조치), 점검이 3시간 넘게 안 돌아도 알림, 전부 정상이면 안 보임.
- **버그 복구**: `insert_v*.py` 17개의 레이어 교체 정규식이 **뒤따르는 레이어까지 지워** .57(출결 수정 때 V44 재삽입)에서 **V45 질문 패널·V46 글쓰기 활동③이 빠진 채** .57·.58이 커밋됐다. 스크립트를 「자기 블록만 제자리 교체」로 전부 고치고 V45·V46 재삽입. 재발 방지 검사 `docs/tools/layers_present.cjs`(V32~V48 헤더 각 1개) — 앞으로 커밋 전 게이트에 포함.
- 검증: 구문 0 · 레이어 17/17 · 스모크 · v43/v44/v45 · V46 · V47 · 마인드맵 · V48 배너 전부 통과.

### .58 — 개념 마인드맵 (아토 §16 ③ "마인드맵도 잘 그리고 연결시켜줘야해" · "연결해")
- `learn.html` 에 **개념 마인드맵** 카드(패치 `docs/tools/patch_learn_mindmap.py`, 멱등): SVG 그래프 — 노드 = `graph.json` 개념, **선 = prereq(선수지식) 연결**, 층(중등→고교→대학기초→전공) 행 배치 + 무게중심 3회 정렬로 선 꼬임 감소, 노드 색 = 이해 상태(안다/흔들림/모름/미평가), 가로 스크롤. 「선택한 주차」 모드면 그 주차 개념만 점선 테두리 + 나머지 흐리게 + 연결선 강조. 노드 클릭 = 기존 카드 시트(설명·자가평가·퀴즈). 정역학 45노드·63연결 / 미적2 37·51 실측.
- `V47` 커리큘럼 카드 → 과목 행 **「마인드맵」**, 펼친 주차 줄 **「이 주차 맵」** 링크(`learn.html?subject=…&week=n&map=1`, 열리면 맵으로 스크롤).
- 그래프 정리: 내가 3주차에 넣은 `mech.forces_types` 가 기존 `mech.force_types`(힘의 종류)와 같은 개념이라 **삭제하고 기존 노드에 근거·설명 병합**(117노드), `map.json` 참조 교체. `check_graph.py --all` 0건.

## 2026-09-23

### .57 — 뷰어 「새 탭」 버튼 제거 (아토)
- `#ntvOpen` 를 마크업에서 삭제(.51의 CSS 숨김 대신). `openNote` 의 href 설정 줄과 V45 의 숨김 규칙도 함께 제거. 뷰어 헤더 = `← 닫기 / 제목 / 질문`.
- 과목 행 출결: 「출석 3/4」 제거 → **「지각 n회 · 결석 n회」**(V44.attTxt). 0회면 아예 표시 안 함, 둘 다 0이면 아무것도 안 붙음. 지각 = late + vlate(개큰지각), 결석 = absent(인정결석·출튀 제외 — V32 6종 기준). 검사 `v44_check.cjs` 52건.
- 회귀 검사 갱신: `v43_check.cjs` 의 「과목 카드가 회차 표 위」 를 .56 이후 순서(이어서 학습하기 → 커리큘럼 → 회차 표)에 맞춰 인덱스 비교로 수정.

## 2026-09-22

### .56 — 커리큘럼 · 지식 이원화(시험 범위 vs 이해) (아토 §16 ②③)
- **지식 ② = 시험 범위·수업 내용**: `knowledge/map.json` 3주차 확정(회차 정리.md 진도 줄 근거) · 4주차 planned(예고·강의계획서) · 2주차 노드 보정 · **아카데믹글쓰기 과목 추가**. `knowledge/graph.json` 노드 95 → **118**(공수1 2계 ODE 중첩·기저·차수축소·상수계수 / 미적2 3D 좌표·거리·구면·위치벡터·표준기저·내적 / 일물2 가우스·응용·전기위치에너지·전위·연속분포 전위·전기용량 / 정역학 정사영·삼중적·힘의 종류·FBD / 글쓰기 주제·구성·개요·문장단락·맞춤법). `check_graph.py --all` 0건(접두어 `wr.` 추가). 갱신 스크립트 `docs/tools/update_curriculum_w34.py`(멱등).
- **지식 ① = 이해 확인**: mastery(atom `/api/mastery`, 페이지에서는 `atto.mastery`) — learn.html·server.py 와 같은 점수식(퀴즈 0.5 + 자가 0.2, 감쇠 60일).
- `V47 LAYER`(원본 `docs/layers/v47-layer-src.js`): 학습 탭 첫 카드 「커리큘럼 · 시험 범위 vs 이해」 — 과목 행(범위 노드 N · 이해 확인 M · 흔들림 · 비어 있음 + 막대) → 펼치면 주차별 목차(범위 주차 「범위」 배지, 예정 주차 「예정」, 이번 주 표시)와 개념 노드 칩(초록 이해 확인 · 노랑 흔들림 · 빨강 모름 · 회색 미평가), 「다음 확인할 것」 4개 + 지식맵 링크. 과목 화면 회차 표 위에도 그 과목 카드. 범위 주차 = 다가오는 시험 scope `n~m주차`(V25.examWeeks), 없으면 1~현재 주차. 이해도 못 읽으면 「이해도 못 읽음」 표시.
- 검증: 구문 0 · 스모크 · jsdom(6과목 행, 미적2 범위 14 · 이해 1, 펼침 4주차, 과목 카드) · 캡처 `~/.claude/scratch/sc_test/_v56/study.png`. 오타 검수 보류(Codex 한도 18:57).
- 다음(.57): 핵심 개념 **마인드맵**(graph.json 과목별 SVG, 선수지식 연결, 상태색) · 김주영 스앵님 서버 적용.

### .55 — 아카데믹글쓰기 글쓰기 활동 ③ 등록 (아토 슬라이드 사진 12:19)
- `V46 LAYER`(원본 `docs/layers/v46-layer-src.js`): 아카데믹글쓰기 4주차 weekNote에 할 일(한글 맞춤법·띄어쓰기 LMS 토론방, 최대 2~3개 고치고 이유, ~9/27 일, 참여도 1점) + ★시험 언급(기말 출제 예정, 정답 공개 후 회수) + 자료 줄 + 주차 범위(문장과 단락 쓰기). 로컬 원본: `study-materials/아카데믹글쓰기/2026-09-22/정리.md` ⑥⑦ + 사진, `_할일.md`.

## 2026-09-21

### .54 — 덱 본문 서체 교체: 나눔펜 → 고운돋움 (아토 "손글씨체 말고 더 이쁜 대안 있으면 적용")
- 덱 템플릿 **v6d**(`docs/tools/patch_tpl_v6d.py [폰트명]`): 본문·보기·제목 = **Gowun Dodum**(Google Fonts, display=swap), 제목 700, 행간 1.55, 글자 크기 한 단계 축소(x-height 큼). UI Pretendard·수식 KaTeX 그대로. 글꼴 경고도 고운돋움 기준. 다른 후보(Gowun Batang·Noto Serif KR·IBM Plex Sans KR·Pretendard)는 같은 스크립트에 이름만 넣으면 교체. 지침 학습게임_미적분2 §1 「손글씨」는 아토 허용으로 대체(학습시스템 §15 ④ 갱신).

### .53 — 덱 여백 제거 (아토 "학습하기 여백이 너무 많고 디자인 수정이 제일 시급")
- 덱 템플릿 **v6c**(`docs/tools/patch_tpl_v6c.py`): `#stage` 16:10 고정 → `100vw×100dvh`(뷰어 안 회색 띠 제거, ≥1480px 만 1400 제한) · 글자 크기 `clamp(…vh…)` · 문제 장 `--qs` 스케일(≥900×680 1.15 · ≥1000×760 1.3 · ≥1100×900 1.45: 제목·본문·보기 카드·간격) + 세로 가운데(`safe center`) · 개념 장 `--cs`(1.12/1.22) + 세로 가운데 + ≥1300px 콘텐츠 폭 1180 · 폰(≤700px) 보기 1열·본문 스크롤·하단 버튼 nowrap·힌트 문구/파트명 숨김. 재빌드 배치 검사(1024×768, 스케일 1.15·1.12 적용 상태) 89장 실패 0. 캡처 `~/.claude/scratch/sc_test/_v53/`.

### .52 — 김주영 스앵님: 학습앱 전용 AI (아토 "학습앱에서는 김주영 스앵님이라는 새 정체성으로 대화")
- 정체성 원본 `본사/atom/personas/kimjuyoung/CLAUDE.md`(v0.1): 진단관·설계자·감독관·전략가, 단호·구체·숫자 근거·위로 없음, **지식 이원화**(① 아토가 이해한 지식 = mastery / ② 스앵님이 아는 지식 = 시험 범위·수업 내용 전부 = map.json·graph.json·정리.md·덱) — 목표는 ①이 ②를 덮는 것, 섞지 않음. 답 형식 결론 → 원리 → 확인 문제 1개. 파일 읽기만.
- 서버 패치 `본사/atom/apply_study_persona.py`(멱등, 복사본에서 구문 확인): FIXED_CHATS 에 `("study","김주영 스앵님")` · `_claude_once/run_claude` 에 `cwd` 인자 → 학습 대화는 `personas/kimjuyoung/` 에서 `claude -p`(그 폴더 CLAUDE.md 만 읽힘, 아톰 헌법 비적용) · `_send` 에서 `kind=="study"` → `_study_flow`(스앵님 한 턴, 오타 검증 없음). **아직 적용·재시작 안 함** — 다른 세션이 server.py 를 미커밋 작업 중(layers). 그 세션이 끝나면 `python apply_study_persona.py` → `restart-atom.ps1`.
- 앱(V45): `kind==="study"` 대화 우선, 없으면 group 폴백(헤더에 「서버 갱신 전」 표시). study 모드 프롬프트는 `[학습 질문]
질문: …` + 문맥만(아톰·오타 역할 지시 없음). 화자 라벨 「스앵님」. 검사 49건.
- 지침 학습시스템 §16(아토 지시 5건 원문 요지 + 순서). 남은 것 → **.53**: 지식 이원화 대시보드(시험 범위 노드 vs 이해 노드) · 과목별 커리큘럼(목차·진도, map.json 3~4주차 갱신 필요 — 지금 1~2주차만) · 핵심 개념 마인드맵(graph.json 과목별 SVG, learn.html 확장).

### .51 — 덱 조작부 재설계 · 결과 장 · 새 탭 제거 (아토 "학습하기 문제·보기 버튼 개망 → 실제 서비스처럼")
- 덱 템플릿 **v6**(`docs/tools/patch_tpl_v6.py`, CSS 덮어쓰기만 — 마크업·JS 무변경): Pretendard 로드, 상단 점 → **분절 진행 막대**(완료 초록·현재 연초록·시험 언급 핑크), 보기 = 흰 카드 r14 + 번호 배지(선택 초록 테두리 · 정답 연초록 · 오답 연빨강 · 미선택 흐림), 버튼 = 정자 평면 r10(주버튼 초록 1개, 미완료 다음은 초록 아웃라인), 개념 상자 = 좌측 3px 파란 바 + 연파랑 배경(두꺼운 테두리 제거), 암기/이해 상자·힌트·필기·풀이 판·출처 썸네일·시작 목록·글꼴 경고 전부 둥근 평면. 본문 손글씨·5색은 그대로(§15 ④).
- 덱 템플릿 **v6b**(`patch_tpl_v6b.py`): 결과 장 = 정답·오답·안 푼 3칸 + 통과 기준 숫자(`기초 4문제 전부 정답 + 응용 2문제 중 2문제 이상`) + 틀린 문제 목록(누르면 이동) + **주버튼 하나**(오답>0 「틀린 문제 복습」 › 안 푼>0 「안 푼 문제 풀기」 › 전부 정답 「다음 파트」/마지막 「전체 결과」), 보조 「이 파트 처음으로」·「안 푼 문제 n개」. 글꼴 경고는 3초 뒤 재확인해 로드되면 지움.
- 뷰어 「새 탭」 버튼 제거(아토: 불필요). 덱 5개 동일 적용, 재빌드 배치 검사 89장 실패 0. 오타 검수는 Codex 한도로 보류(사후).

### .50 — 학습 중 질문: 아톰·오타 동시 호출 (아토 "학습할 때 오타랑 아톰 같이 불러와서 학습하면서 질문")
- 설계: `docs/briefs/v50-study-chat-brief-r3.md` — 오타 설계 회의 1차 RED(5) → 2차 RED(3) → 3차 RED(1) → 4차 GREEN. 서버(`본사/atom/server.py`)는 다른 세션이 미커밋 수정 중이라 **서버 수정 없이 기존 API만**: `GET /api/conversations`(kind group) · `GET /api/conversations/<id>` · `POST /api/send`(SSE) · `EventSource /api/watch`(재조회 트리거만).
- `V45 LAYER`(원본 `docs/layers/v45-layer-src.js`, 삽입 `~/.claude/scratch/sc_test/insert_v45.py`): 덱/노트 뷰어 헤더 [질문] → PC 오른쪽 380px 열 / 폰 아래 60dvh 시트. 문맥 칩(덱 · 파트 › 위치 · 정오) + 「이 문제 문맥 붙이기」 체크. 프롬프트 = `[학습 질문 — 설명·검증만, 후속 실행 요청 없음] 아톰: 학생 눈높이 1,500자 / 오타: 수학·논리 정확성만 판정` + 질문 + 참고 문맥(≤1,500자). 상태: 보내는 중 → 아톰이 답하는 중 · 경과 → 오타가 검토 중 → … → **기록 동기화됨 · 완료 미확정**(스트림에 완료 신호가 없어 완료를 주장하지 않음, .51 서버 `flow_end` 뒤) / 연결 끊김 / 다른 창 답변 중(초안 보존) / 로그인 필요 / 서버 오류. 입력 잠금 해제 = 스트림 EOF. 실시간 말풍선은 내 스트림만(provisional) → EOF·watch 때 기록 재조회로 서버 메시지 id 와 교체(중복 없음). 저장된 아토 메시지는 질문만 보이고 문맥은 접힘. 깃허브 페이지(비호스팅)에서는 미리보기만, 전송은 atom `/study/` 안에서.
- 덱 템플릿 **v5b**(`patch_tpl_v5b.py`, 덱 5개 동일): 앱이 `{type:"mc-ctx?",gen}` 을 주면 이후 `render()` 마다 `{type:"mc-slide",v:1,gen,deck,title,sid,stype,part,partTitle,label,q{qn,kind,text≤600,choices≤6,my,revealed,correct,answer}}` postMessage(origin 지정, `#check` 제외). 앱은 origin·source·type·v·**gen**·**deck** 6조건 + 필드 상한으로 채택, revealed=false 면 정오·정답 미전송.
- 발견 수정: `.ntv-h .tt` 가 시간표 격자 `.tt{min-width:520px}` 와 이름 충돌 → 폰에서 뷰어 헤더 「새 탭」·「질문」 버튼이 화면 밖으로 밀리던 것(이전 빌드부터) 수정. 뷰어 열림 중 하단 탭바 숨김(`body.ntv-on`).
- 검증: 구문검사 0 · 스모크 138건 · v43(18)·v44(47) 회귀 · **v45_check.cjs 46건**(SSE 파서 청크·UTF-8 경계 · 5턴 흐름·재조회 교체 · error→done{} · locked · dropped · 401/500/비SSE/그룹 없음 · 문맥 6조건·상한·gen 왕복·덱 전환 · XSS · 한글 조합 Enter · 닫기/재열기) · http 서빙으로 실제 덱 iframe postMessage 확인(file:// 는 origin null 이라 불가) · 390 넘침 0 · 캡처 PC/폰.
- **미검증**: atom 실서버 실전송(그룹 대화에 기록 남음 — 아토가 직접 1회) · `/api/watch` 실동작. **오타 구현 검수 보류**: Codex 한도(16:53 복귀) — 지침 [[otta-absent-rule]]대로 진행, 복귀 후 사후 검수.
- 알려진 한계(.51): 폰 세로에서 덱 자체가 16:10 이라 읽기 어려움(덱 반응형은 덱 조작부 재설계와 함께) · 전용 「학습」 그룹 대화·flow_end·질문 기록 회수는 서버 작업.

### .49 — 디자인 재설계 1차: 평면 셸 · 과목 행 목록 · 오늘 탭 · 5탭 (아토 "디자인이 개망 · 메가스터디·이투스 참고")
- 기준: 브리프 `docs/briefs/v49-design-brief-r3.md`(인강 앱 4종 앱스토어 42장 실측 → 오타 2·3·4차 GREEN), 아토 결정 4건(학습시스템 지침 §15): 색 제한 지침에서 제외 · 표지 사진 → 행 칩 · 5탭 · 덱 본문 손글씨 유지.
- `V44 LAYER`(원본 `docs/layers/v44-layer-src.js`, 삽입 `~/.claude/scratch/sc_test/insert_v44.py`): 연회색 바탕 `#F4F6F5` + 평면 흰 카드 · 헤더 한 줄(게이지 숨김 → `D-28 중간고사` 칩, 폰은 ↻·◐ 숨김) · 탭 5개(오늘·할 일·학습·과목·설정; 달력 = 오늘 탭 「달력」 버튼 + 달력 화면 「← 오늘」, 학점·졸업 = 과목/학점 화면 세그먼트; cal/grade/course 에서는 오늘/과목 탭이 켜짐) · `renderShelf` 래핑: `.book` → `.crow` 행(44px 색 칩 약자 · 과목명·재수강 · 이수/학점/요일/출석 · 시험 D-day ≤21 · 덱 진도 `학습 n%` / `덱 k/n 진행` / `학습 시작 전` / `덱 준비 중` · ⋯ 44px 메뉴). 이벤트는 원본 `.book` 블록 이관(짧게 = go course · 520ms 길게 = bookMenu · contextmenu · Enter/Space, 자식 버튼 키 입력 무시). 표지/책등 세그먼트 숨김, 개인 공부 세그먼트 유지 · `renderToday` 래핑: `#v44TodayStats`(D-day · 이번 주 회차 ended/total, 시험기간엔 오늘 학습 분) + `#dayBooks` 안을 `.drow`(3px 과목색 바 · 시작/끝 · 과목 · 장소 · 출결 배지 · 덱 있는 회차 [학습]=openStudy)로 · `markSave(state)` 3상태(dirty/saved/failed, cloudSave 실패 경로 2곳 `markSave("failed")`) · 영문 UI 라벨 스크럽(.hs/.kicker/.kbdhint/.rail-sec 중 대문자만, V43 RESUME 포함) · 저장 버튼은 보조(아웃라인), 할 일 요약 버튼도 텍스트형.
- 과목 팔레트 `V44.COURSE_COLORS`(bucket 기준 8색, 저채도). 기존 `typeA` 면색은 표시 안 함(데이터 유지).
- **V41 버그 수정**: 임시 덱 4개의 NOTES 등록이 `patchV41b` 플래그 뒤에 있어 두 번째 로드부터 학습 탭 목록·덱 판정에서 빠지던 것 → `V44.ensureDeckNotes` 매 부팅 등록 (허용 목록 밖 — 아토 승인 조건).
- 검증: 구문검사 0 · 스모크 138건×3시점(V34 표지 검사 → 행 검사로 교체, 기존 CADD 9/15 검사 1건 제외) · `v44_check.cjs` 47건 · 390/768/1180 `documentElement.scrollWidth ≤ innerWidth` 전 뷰(390은 `_shot_m.html` iframe 호스트 + `--allow-file-access-from-files`, 크롬 헤드리스 최소 폭 500) · 캡처 라이트/다크/폰. 오타 5차 RED 5건(자식 버튼 Enter · 44px · dirty 전이 · 덱 분모 · 허용 목록) → 6차 GREEN(`docs/otta/_v44-otta-2.md`).
- .50 예정: 과목 화면 회차 행 · 할 일 단일 목록 · 덱 조작부(Pretendard·연속 진행바·결과 장 주버튼).

### .48 — 덱 즉시 채점 · 정답이면 풀이 판 생략 · 「이어서 학습하기」
- 덱 템플릿 v5a(`docs/tools/slides_tpl.html`, 빌드된 덱 5개에 같은 치환 — `~/.claude/scratch/sc_test/patch_tpl_v5a.py` 멱등): 객관식은 보기를 누르는 순간 제출·채점(「답 확인」 버튼 없음) · 정답이면 답·풀이 판을 자동으로 띄우지 않음(「답 · 풀이 다시 보기」로 열람, 오답·주관식 자기채점 전에는 그대로 뜸) · 저장 때마다 `mc-slides-last`·`mc-slides-last-<deck>` 에 위치 기록 `{deck,title,href,idx,n,sid,part,partTitle,where,qDone,qN,ts}` · `#resume` = 저장 위치에서 열기 · `#check`(빌드 검사) 중에는 저장 안 함.
- `V43 LAYER`(원본 `docs/layers/v43-layer-src.js`): 오늘 탭 「오늘 수업」 아래 + 그 과목 화면 회차 표 위에 「이어서 학습하기」 카드(과목 칩 · 덱 제목 · 파트 › 위치 · n/N장 · 문제 진행 · 몇 시간 전). 버튼은 덱을 `#resume`으로 앱 안 뷰어에 연다(`#at=`은 파트 시작으로 되돌리므로 안 씀). 뷰어 닫기·`storage` 이벤트로 카드 갱신. 저장값 숫자 칸은 숫자로 강제, 문자열은 esc.
- 오타 회의: 1차 RED 2건(숫자 칸 esc 누락 · `#check` 중 pagehide 저장) → 수정 → 2차 GREEN (`docs/otta/_v43-otta-2.md`). 검증: 구문검사 0 · 스모크 138건(기존 CADD 9/15 검사 1건 제외 통과) · `v43_check.cjs` 18건 · 재빌드 배치검사 89장 0 · 헤드리스 캡처(오늘·과목·덱 정답 상태).

### .47 — 정역학 LMS 영상 정리 3건 · 과제 API
- `V33.SPEC["정역학"].post` 에 `VIDEO`(영상 정리·그 주) 추가 → 회차 카드가 `knowledge/library.json` 의 `lmsvideo`(파일명 `영상정리_*.md`)를 같은 주차로 세어 표시. 정역학 영상정리 3건: `2026-09-09/영상정리_2주차_W2-2_성분과내적정의.md` · `2026-09-14/…W3-1_내적과외적.md` · `2026-09-16/…W3-2_삼중적과힘.md` (원료: LMS 영상 15편 faster-whisper 영어 전사 + 교수 필기 한글 변환, ★시험 언급 절은 교수 발언과 아톰 판단을 구분).
- atom `lms_sync.mjs` 5단계: `/api/v1/courses/{id}/assignments?include[]=submission` (GET) → `state._assignments`(submission 누락은 `"unknown"` 보존, 미제출 집계 제외) · 새 과제는 `_lms/new_assignments.txt`. 단계 전체 try/catch 로 기존 save·종료 경로 보호. 실측: 정역학 0건, CADD 2건(Week3 due 9/21 23:59 제출 완료).
- 오타 회의: 1차 RED(시험 언급 단정 3곳 · submission 누락 처리 · 실패 경로) → 수정 반영. 재판정은 아토 지시("오타 없으니 일단 만들고 오면 피드백")로 뒤로 미룸.
## 2026-09-16

### .33 — 과목별 상단(미리)/하단(수업 후) 자료 구성 (아토 지시 → 지침 학습시스템.md §10)
- `V33 LAYER`(파일 끝, 원본 `docs/v33-layer-src.js`). 전역 `MUST`는 그대로 두고 `mustHTML`·`V32.dotsHTML`을 **목록 인자 버전**으로 다시 정의(오타 1차 권고). 과목별 스펙 `V33.SPEC[과목명] = {pre, post, rules}`.
- **하단(수업 후)**: 일물2 판서·녹음 / 공수1 판서·녹음·**과제 제출용 생성본**(주차 링크·「과제」 노트 연결 여부 → `linked`, "제출본 존재"라고 쓰지 않음) / 정역학 **교수님 필기본**·녹음 / 미적2 판서·녹음·필기 / CADD **그 주 영상 정리**(PC 날짜 폴더 `영상정리_*.md`, `weekOf(item.date)` 일치만)·과제 / 글쓰기 녹음. 스펙 없는 과목은 V32 4종.
- **상단(미리 올릴 자료)**: 회차 시트 요약 위에 `#v33Pre` — 전공책·솔루션·강의노트·수업자료를 PC 인벤토리 `common`의 **파일명 정규식**으로 판정(classify 변경 없이 구·신 JSON 호환). 없음 / PC N개 / 확인 못 함(인벤토리 없음·동명 과목).
- 과목 화면 `#cHead` 아래 「과목 규칙 · 자료 구성」 카드. 첨부 kind에 교수필기·영상정리 추가. 주차표 점 개수 = post 길이.
- `_진도커버리지.py classify`: `솔루션_`→solution, `교수필기_`→profnote, `영상정리_`→lmsvideo, `여름*`→summernote 추가(기존 분류 유지). `_영상정리`는 계속 제외(작업 폴더).
- 오타 회의: 설계 1차 RED(4) → 2차 RED(1) → 3차 GREEN. 게이트 구문 0 · 스모크 50건 × 3시점 실패 0·오류 0.
- 미결(아토 지시로 생긴 설계 항목): CADD LMS 영상 자동 정리, 정역학 LMS 영상·필기본 수집·한글 변환, 미적분 클리닉 루틴 등록.

### .32 — 주차표 · 자료 4종 업로드 여부 · 라이브러리 (아토 스프레드시트 지시)
- **주차표**(달력 탭 4번째 세그먼트): 주차×요일(월~금) 표. 셀마다 그날 수업을 `과목약칭 N일차 [출결약자]` + 자료 점 4개(🎙📘✍️🧱)로 나열, 진도 문구·그날 마감 과제 태그는 아래 줄. 글자색 = 출결(결석 빨강 A · 지각 노랑 L · 인정결석 분홍 E · 출튀 보라 G · 개큰지각 주황 VL), 색과 약자 병행. 과목 칩에 `(결석/지각)` 누계(지각 = 지각+개큰지각). 오늘 셀 테두리, 휴강 표시. 줄을 누르면 회차 기록. 폰(≤600px)은 선택 주차의 요일별 목록이 기본이고 「표로 보기」로 5열 표(가로 스크롤).
- **출결 6종**: `ATT`에 개큰지각(vlate)·인정결석(excused) 추가. `attStats` 덮어쓰기 — 6종 집계, 휴강 제외, 출석률 분자 = 출석+지각+개큰지각+출튀+인정결석, 결석 환산 = 결석 + floor((지각+개큰지각)/n). 학습 완료(`reviewed`)·복습 큐 규칙과 XP는 그대로.
- **자료 4종** `MUST` = 녹음본(rec)·강의자료(pre)·내 필기본(note, kind 필기본)·칠판 판서(board, kind 칠판판서). `mustHTML` 덮어쓰기 — 분모 `MUST.length`, 종류별 상태 have/pc/maybe/pending/unknown/none, 출처별 개수(텍스트·기기·PC) 따로 표시하고 합산 없음. PC 조회 실패해도 기기 판정은 유지하고 「PC 확인 못 함」만 덧붙임. 예전 `판서` kind 첨부는 **미분류**로 두 줄 모두에 노란 점 + 「분류하기」, 첨부 칸에서 필기본/칠판판서 셀렉트로 재분류(`idb.put`). 첨부 kind 선택지에 필기본·칠판판서·녹음 추가.
- **PC 인벤토리** `knowledge/library.json` — `study-materials/_진도커버리지.py`가 폴더 전체를 훑어 생성(파일명 접두+폴더 문맥 분류: rec/pre/note/board/summary/hw/derived/other, 공통 폴더는 `common`). **`.gitignore`로 공개 배포 제외**(파일명에 교수 실명) — atom `/study/` 안에서만 읽히고, 공개 페이지는 「PC 인벤토리 확인 못 함」으로 동작. 앱은 과목 **이름**으로 연결(과목 id는 기기마다 `uid()`).
- **라이브러리** 뷰 `#v-lib`: 종류 탭 9개(녹음본·강의자료·내 필기본·칠판 판서·정리·과제·파생 텍스트·기타·이 기기 첨부) → 과목 카드 → 날짜·N일차·파일명·크기·출처. PC 파일은 이름만(열기 미결). 진입: 학습 탭 상단 카드, 주차표 도구줄.
- **N일차** `V32.meetings(c)` = `planned(c)`에서 `c.enrolledFrom` 이전·`cancelled`·학기 밖 제외, 보강 세션 포함. 정역학 `enrolledFrom=2026-09-03`(9/7 = 1일차), 9/2 OT는 휴강 처리로 집계 제외.
- **아토 주차표 기록 반영**(`patchV32b`, 1회): 색 칸은 덮어씀 — 공수1·일물2 9/2 인정결석 · 미적2·글쓰기·CADD 9/8 인정결석 · 공수1·일물2 9/9 인정결석 · 정역학 9/9 결석 · 미적2 9/10 개큰지각 · 공수1 9/11 지각 · 창업 9/14 결석. 검정 칸은 비어 있을 때만 출석. 미적2 9/15 진도 「12.1-12.2 (위치벡터)」. 시트의 `[감상문 과제]`·`[2주차 과제]` 태그는 마감을 몰라 안 넣음.
- 오타 회의: 설계 1차 RED(6건) → 2차 GREEN(`docs/v32-design-brief*.md`, `_v32-otta-*.md`). 푸시 게이트: `vm.Script` 구문 0건 · jsdom 스모크 29건 × 시점 3개(현재·D-11·개강 전) 실패 0·오류 0. 헤드리스 캡처 900px·390px(표·목록)·라이브러리·회차 시트 확인.
- 오타 3차(구현 검수) RED 4건 → 수선: ① 타임라인 사진도 4종 집계에 포함(`V32.countAtt`), 새 타임라인 사진은 `idb.put` 래핑으로 `칠판판서` 저장, 예전 타임라인 「판서」는 첨부 칸 아래 「전부 내 필기본/칠판 판서」 일괄 버튼과 라이브러리 「이 기기 첨부」의 셀렉트로 재분류 ② 같은 이름 과목 2개면 라이브러리·PC 인벤토리 미연결 ③ 재분류 전 `captureLog`로 입력 보존 ④ 폰 목록 모드도 학기 밖 날짜 제외.
- 미결: PC 파일 열기(atom 서빙 경로), 회차 로그(`#cLog`) 점 색에 인정결석·개큰지각 색 미반영(회색), 보충 워크플로.

### 데이터 갱신 — 진도 현황·커버리지 9/16 기준 (BUILD 변동 없음)
- `knowledge/progress.json` 9/9 → 9/16: 과목별 3주차 행 추가, 9/9 이후 들어온 자료 반영(정역학 9/7 정리 · 공수1 9/11 1.5 선형 ODE 정리 · 일물2 9/11 원형 도선·원판·가우스 서론 정리 · 미적2 9/10 자료 · 공수1 2주차 과제 3종). 「당장 3개」를 2주차 과제 제출 확인 · 3주차 8회차 자료 0건 · 결석분 4회차로 교체. learn.html 「진도 현황」이 그대로 읽는다.
- `knowledge/coverage.json` 9/11 → 9/16 (`_진도커버리지.py` 재실행): 정리 11 · 자료만 3 · 빈칸 14(2주차 이전 6 + 3주차 8).
- 원본: `study-materials/_진도현황_2026-09-16.md` · `_진도커버리지.md`. index.html 변경 없음이라 푸시 게이트 대상 아님.
- (같은 날 추가) 아카데믹글쓰기 LMS 누적 PDF 68p 수신 → 9/8 결석분을 슬라이드 44장으로 재구성한 정리 + 9/15 정리(예고 슬라이드·읽기자료 신형철 칼럼 6단락 개요, 본 슬라이드는 미확보) → `progress.json` 글쓰기 2·3주차 행 교체, 「당장」 4개로 교체(공수1 3주차 과제 #23 확정 · 글쓰기 토론방 주제문 · 정역학 9/16 · 일물2 #31). `coverage.json` 빈칸 1(정역학 9/16), 글쓰기 9/8은 자료 안 옴 확정에서 제외. 라이브러리 회차 자료 90 · 공통 50.
- (같은 날 추가 2) 글쓰기 9/15 녹음 48분 수신 → 정리 ③ 완성(구성 3·4·5단, 개요 효과 5·종류·유의점, 칼럼 토론) → `progress.json` 글쓰기 3주차 행·「당장」 갱신. 라이브러리 회차 자료 91.

## 2026-09-18

### .46 — 공업수학1 3주차 과제 해답지 등록 (아토 "학습앱 등록은 자동으로")
- `notes/em-hw3-solution.html` + `.pdf`(4장). `V42 LAYER`: NOTES 과제 항목, 3주차 정리 links(HTML·PDF), 3주차 과제 task(9/23 수 09:00 ❓), 9/18 회차 links. 오타 2차 GREEN(`_과제/_hw3b-otta-1·2.md`). 앞으로 과제 산출물은 지시 없이 자동 등록(원본 = NOTES + weekNotes.links/tasks).
- 스모크 138건 × 3시점 실패 0.

### .45 — 일반물리학2 9/18 회차 반영
- 녹음 43분(24장 전위: 점전하·여러 점전하·쌍극자·직선 도선·원형(교재에 없는 이유)·원판 + 25장 축전기 도입) → `study-materials/일반물리학2/2026-09-18/정리.md`. 시험 언급: 증명 3제(쌍극자·직선·원판) 중 1문제 20점 주관식, 여러 점전하 전위 계산 출제, 1 μF = 10⁻⁶ F, **중간고사 범위 27장 RC 회로까지(28장 제외)**.
- 임시 덱 `phys2-w1-3` 88장으로 재빌드(파트 5 = 9/18). V41 DECKS에 9/18 → 파트 5, 패치 플래그 `patchV41b`로 재적용. 커버리지 재생성(회차 자료 106). 스모크 135건 × 3시점 실패 0.
- 같은 날 공업수학1 9/18 결석: 캘린더 토마토·변동 로그·커버리지 `n`·할 일(보강 정리). 앱 출결은 아토 기기에서.

## 2026-09-17

### .44 — 나머지 과목 임시 학습 덱 (아토 "나머지 과목들도 일단 임시로 정리해줘")
- `docs/build_md_note.py`: 과목 폴더의 회차 `정리.md`(7섹션)를 v2 노트 HTML로 자동 변환 — 파트 = 회차, 내용 순서는 지침 §14대로 ② 판서 → ③ 녹음 → ④ 강의자료 → ⑤ 필기 → ① 교재, ⑥ 시험 언급은 `aside.exam`(강조), ⑦ 과제는 제외. 문제는 없다(개념 장만). 코드펜스 6줄·표 2행씩 잘라 장 분할이 되게.
- 덱 4개: 공수1 84장(9/9·9/11·9/16) · 일물2 66장(9/4·9/9·9/11·9/16) · 정역학 43장(9/7·9/9·9/14) · 글쓰기 53장(9/8·9/15). `--limit=400 --soft`로 빌드 — 넘친 장(공수1 6·일물2 6·정역학 3)은 스크롤 허용 표시(임시 덱 전용, 정식 덱은 여전히 실패 처리).
- `V41 LAYER`(원본 `docs/v41-layer-src.js`): 회차 정리 links에 `#at=pN-cover` 덱 링크(주 1회 글쓰기는 주차 정리에), NOTES 학습 4건. 회차 표 [학습하기]가 전 과목에서 켜진다. 1회 패치 `patchV41a`.
- build_slides: 시험 언급 3개 이상이면 전용 장(2개씩) · `--limit=N` 분할 한계 · `--soft`. 행렬 덱 89장으로 재빌드(실패 0).
- 스모크 134건 × 3시점 실패 0.

### .43 — 회차별 학습하기 → 덱의 그 파트에서 시작 (아토 "학습하기 소행렬식부터, 이전까지는 완료 처리")
- `V40 LAYER`(원본 `docs/v40-layer-src.js`). 미적2 회차 링크에 해시: 9/1 `#at=p1-cover`(이동만) · 9/3 `#at=p2-cover` · 9/8 `#done=p4-cover`(파트 1~3을 완료로 표시하고 소행렬식부터). 1회 패치 `patchV40a`.
- 덱 `startFrom(id, mode)`: `#at=` 이동만 · `#from=` 앞은 건너뜀(채점 제외) · `#done=` 앞은 완료. 일차별 진도 목차 장 제거(과목 페이지 회차 표가 목차). 표지 제목의 ★ 중복 제거.
- 스모크 130건 × 3시점 실패 0. 캡처: `#done=p4-cover`로 열면 파트 1~3 초록, 파트 4 표지.

### .42 — 메뉴 레일 폭 축소 · 공부 타이머는 시험기간만 (아토 지시)
- `V39 LAYER`(원본 `docs/v39-layer-src.js`). 좌측 레일 184 → 128px(≥861px에서만, 폰은 원래 숨김) — 항목이 2~3자라 오른쪽에 남던 여백 제거. 본문 폭이 그만큼 넓어짐.
- 공부 타이머: `V25.renderTimer` 스왑 — `phase()==="EXAM"`이 아니면 오늘 탭 타이머 카드를 만들지 않고 있으면 지운다. 학습 화면(studyunit)의 시간 칩·안내와 `tickStudy`도 시험기간에만. 진행 중 타이머 저장값(`mc-timer`)은 그대로 둠.
- 스모크 127건 × 3시점 실패 0. 캡처: 오늘 탭(1024px) 레일 축소·타이머 카드 없음.

- (같은 날 추가) **학습 슬라이드 답 입력 칸** (아토 "문제 보고 답 입력하는 칸이 없음"): 문제 페이지에 「내 답」 textarea. 비어 있으면 「답 확인」 버튼이 눌리지 않고, 확인 뒤에는 내 답(점선 상자)과 답·풀이가 나란히 보인 다음 맞았어요/틀렸어요. 입력값은 `ST.answer[id]`에 저장(Enter = 답 확인, Shift+Enter 줄바꿈). 자동 채점은 아직 아님 — 문제마다 검증된 정답 문자열이 없어서(지침 §0 "검증되지 않은 정답 금지"), 학습게임 지침 재작성 때 정답 필드를 붙인다. `docs/slides_tpl.html` 갱신, 덱 재생성(58장).

- (같은 날 추가 2) **풀이 메모장 — 손글씨 캔버스** (아토 "문제 보고 풀이할 수 있는 메모장, 애플펜슬"): 문제 페이지에서 문제와 「내 답」 사이에 모눈 캔버스. 펜/지우개/되돌리기/지우기/손가락도/크게. 애플펜슬(pointerType pen)과 마우스는 항상 그리고, 손가락은 「손가락도」를 켰을 때만(손바닥 오인 방지). 필압으로 굵기. 획은 `ST.ink[문제id]`에 캔버스 폭 기준 비율 좌표로 저장 → 화면 크기가 바뀌어도 같은 모양. 답 확인 뒤에도 남아 풀이와 비교. 「크게」는 문제 글자를 줄이고 답 칸을 한 줄로 해 캔버스를 넓힘. 저장 용량: 문제당 수십 KB, 37문제 다 써도 1~2MB(localStorage 5MB 안).

- (같은 날 추가 3) **문제 자기완결 수정 + 빌드 검사** (아토 "기초 1-3에서 B, C가 없는데 어딜 보고 풀라는 거야, 다시는 없게"): 정리노트 v2의 문제 7개가 다른 문제의 행렬을 "위의 B, C" 식으로 참조하고 있었고, 슬라이드는 한 장에 문제 하나라 그 행렬이 보이지 않았다. 기초 1-3 · 응용 1-2 · 응용 2-1 · 기초 4-2 · 4-3 · 5-2 · 6-2를 필요한 행렬·식을 문제 안에 다 적는 형태로 고침(답 재검산 완료). `docs/check_q_refs.py` 신설 — 문제마다 정의 없는 행렬 기호·"위의/앞의/기초 N-N" 참조 문구를 잡고, `build_slides.py`가 빌드 전에 실행해 **위반이면 덱을 만들지 않는다**(옛 노트로 검증: 7건 차단). 노트 HTML·PDF·앱 사본(`notes/calc2-matrix-w1-2.html`)·덱 재생성. 문제 페이지 여백을 줄여 iPad 가로(1024×768)에서 세로 스크롤 없이 문제·메모장·답 칸이 다 보이게 하고, 답 칸 자동 포커스가 화면을 밀어 올리던 것도 막음(preventScroll).

- (같은 날 추가 4) **출처 썸네일 · 보기 버튼 문제 · 잠금 해제** (아토 지시 3건):
  - **출처 발췌**: 슬라이드 오른쪽 아래에 카메라 갤러리처럼 작은 정사각 썸네일(1~3장). 누르면 그 페이지의 근거가 된 자료 영역(교수님 학습지 p.N 해당 부분 · 학습지 위 내 풀이본)이 크게 뜨고, 사진을 누르면 190% 확대·드래그 이동, 이전/다음 출처. 영역은 `<노트>.sources.json`(슬라이드 id → PDF 페이지·좌표)에 적고, `build_slides.py`가 pymupdf로 잘라 `notes/src/<deck>/`에 JPEG(130dpi, 총 2.1MB, 70장)로 넣는다. 판서 사진이 있는 회차는 같은 방식으로 사진 파일을 넣으면 된다(행렬 단원은 판서 사진이 없어 학습지·풀이본만).
  - **보기 버튼**: 기초 문제 18개는 4지선다 버튼(정리노트의 `ol.choices`, 정답 `li[data-ok]`, `docs/add_choices_calc2_matrix.py`). 누르고 「답 확인」 → 자동 채점(정답 초록/오답 빨강 + 풀이) → 「다시 풀기」. 답이 여러 개인 기초 4-1(소행렬식 5개)과 응용 문제는 텍스트 답 그대로. 오답 선택지는 흔한 실수(부호·순서·성분곱)로 만들었고 정답은 재검산.
  - **잠금 해제**: 페이지를 안 끝내도 → 로 넘어간다. 안 끝낸 페이지의 화살표는 점선, 끝내면 실선·펄스. 결과 페이지 기준은 그대로.
  - 답 확인 뒤에는 메모장을 접어(96px) 풀이가 한 화면에 들어오게. 시작 페이지 안내 문구 갱신.

- (같은 날 추가 5) **학습지 전체 대조** (아토 "행렬은 학습지가 전부, 학습지 내용은 다 들어가야"): 아토가 다시 올린 풀이본 PDF(14p)로 학습지 Lecture 2-1 항목을 노트와 1:1 대조. 빠졌던 것 보강 — 정의02 (ii) 단위행렬=항등행렬 \(I_n(E_n)\) 이름, Ex01 (1)(4) \(A^t, D^t\)(기초 1-4 신설, 보기), Ex05를 학습지대로 **소행렬식 9개·여인수 9개 전부**(기초 4-1·4-2, 답 여러 개라 텍스트형으로 전환). 나머지(정의01, (2) ①~⑤ 거듭제곱 법칙, (3) 2·3차 사루스, (4) 전개, (5) 2×2·3×3 역행렬·수반행렬, (6) Ex9, (7) 크래머 (i)~(iii)·아토 여백 질문 "j에 1 못 넣나?" 답)는 이미 있음. 덱 59장(출처 발췌 72장), `docs/fill_handout_calc2_matrix.py`.

- (같은 날 추가 6) **암기 vs 이해 구분** (아토 "암기해야 하는 거 이해해야 하는 거 구분!"): 파트 6개마다 한 줄 요약 아래 「암기할 것(노랑) / 이해할 것(파랑)」 상자. 암기 = 공식·정의·부호판·수순(교수 시험 언급 ★ 포함), 이해 = 왜 그런지(행렬=계수 상자, 교환법칙 ✗, det=0의 뜻, j열 교체 이유 등). 덱은 개념 장 다음에 「암기 vs 이해」 장(type mu, 출처는 개념 장과 공유) — 65장. 노트 맨 위 범례 "시험 직전엔 노랑만, 막히면 파랑". `docs/add_mu_calc2_matrix.py`. 지침 §14에 규칙.

- (같은 날 추가 7) **시작 위치 점프 + 곱셈 보충 4장** (아토 "2-1 예제 2-3부터 이해 안 돼, 그 앞은 학습 안 해도 돼"): ① 덱 3번째 장 「어디서부터 할까?」 — 파트별 개념·암기/이해·문제 목록에서 하나를 누르면 그 앞은 전부 「건너뜀」(done+skip, 채점 제외)이 되고 거기서 시작. 「건너뜀 N장 되돌리기」. URL `#from=<슬라이드id>`로도 가능(앱 링크용). ② 막히는 지점 Ex02 (3)(4)(5) 앞에 보충 4장(div.extra → 문제 사이 개념 장): ① A−2B 두 단계(실수배→뺄셈) · ②-1 AB^t 꼴부터(전치, (3×2)(2×3)=3×3) · ②-2 한 칸씩 표(아토 풀이본 p.4 여백 계산 6+8=14, 12+12=24, 10+16=26 그대로) · ③ A²=AA 같은 규칙. 덱 70장, 출처 80장. 표 셀 여백 축소로 iPad 가로 스크롤 없음. 노트 PDF 갱신.
- 셸 함정: Bash 히어독 안에서 `\`가 `\`로 뭉개져 보충 장의 행렬 행 구분이 깨졌던 것 복구(`docs/`엔 복구본). 앞으로 백슬래시 든 스크립트는 파일로 쓴다.

- (같은 날 추가 8) **슬라이드 템플릿 v4 — 오타 디자인 검토 반영** (아토 "글씨체 및 디자인 오타한테 수정받아"): 1차 RED 14건(`docs/_slides-v4-otta-1.md`) → 재작성 → 2차 RED 7건(`docs/_slides-v4-otta-2.md`) → 6건 추가 반영. 지침 학습게임_미적분2.md 기준: 본문 나눔손글씨(Nanum Pen Script, display=block) + 수식·숫자 KaTeX 정자(MathJax → KaTeX 0.16.11), UI 숫자도 정자(`.num`) · 글자 크기 3단계(31/22/17px) · 5색 고정(노랑 암기·빨강 주의·파랑 개념·핑크 시험 언급·초록 풀이) + 무채색 · 16:10 · 상단 진행점(파트 안 단계, 시험 언급 핑크)·챕터명·n/N · 하단 [← 이전][힌트] 출처 [동작][다음 →] · 힌트 = 파트 한 줄 요약 · 시험 언급 상자(`aside.exam` data-level/when, 9/3 녹음 원문 4건: 파트 2 배제, 파트 3 확정 2·강조 1, 응용 3-1 확정) · 애니메이션 제거 · 답 공개 뒤 메모장 접고 답·풀이는 전용 판 · 개념 장 자동 분할(글자량·표·행렬 무게·시험 상자 포함) · **빌드 게이트**: `build_slides.py`가 헤드리스 크롬 `#check`로 77장 × (기본·답공개·힌트) 넘침·raw LaTeX 검사, 1건이라도 있으면 빌드 실패(현재 0). 덱 77장.
  - 미반영·보류: ⑬ localStorage(아토 저장 방식 결정 대기) · ③ 폰트 로드 실패 시 화면 차단(경고 배너로 대체 — 지침 fallback 체인은 비상 경로라고 봄, 오타와 이견) · ② 자유 입력 한글/수식 분리(입력란은 정자 폰트만) · ⑪ 지원 화면 크기 전수(1024×768만) · 시작 위치 목록 장은 스크롤 허용(학습 내용 아님).

- (같은 날 추가 9) **필기 영역은 「+ 필기」를 눌렀을 때만** (아토): 문제 장 본문에서 메모장을 빼고, 하단 [+ 필기] 버튼으로 문제 아래쪽을 덮는 판(`#padPanel`)을 여닫는다. 획은 그대로 문제별 저장, 닫기/다시 열기 가능. 배치 검사 77장 실패 0.

- (같은 날 추가 10) **파트 4·5·6 재작성** (아토 "소행렬식~행렬 끝까지, 13쪽부터 쭉 다시"): 채팅에서 통한 순서 그대로. 파트 4 = 지운다(소행렬식) → 부호 붙인다(여인수·부호판, "부호판의 −와 M 값의 −는 별개") → 곱해 더한다(전개), 기초 5문제(M₂₂ 하나·9개·A₁₂ 하나·9개·1행 전개) + 응용 3. 파트 5 = 나눗셈 대신·2×2 공식(Ex07 검산까지)·3×3 4단계(det → 여인수 9 → 전치 adj → ÷det, "전치 빼먹는 실수"), 기초 4(Ex07·검산·2×2·Ex8 부품 A₂₃) + 응용 3(Ex8 4단계 전개). 파트 6 = A·X·B 선언 → 역행렬(Ex9 전개) · 크래머(j열 교체 이유, j=1 질문, det=0) + 보충 「Ex10 행렬식 4번 표」, 기초 3 + 응용 3. 시험 언급 aside(9/3 "여기서 중간고사 문제 나옵니다") 파트 4 개념. 답 전부 재검산(Ex8 A⁻¹, Ex10 20/9·−1/3·22/9, 응용 5-3 B⁻¹ 검산). 개념 장 분할 무게를 수식 고정 무게로 보정(한계 440). 덱 86장, 출처 86장, 배치 검사 실패 0.

### .41 — 학습하기 = 바로 학습 화면 · 미적2 클리닉 규칙 제거 (아토 지시)
- `V38 LAYER`(파일 끝, 원본 `docs/v38-layer-src.js`). `openStudy(cid,w,date)` 스왑: 그 회차 정리에 학습 슬라이드(`*slides.html`)가 연결돼 있으면 수업 내용 화면(studyunit)을 거치지 않고 앱 내 뷰어(`openNote`)로 바로 연다. 회차 단위는 **그 회차 links만** 본다(9/10 벡터 회차에 행렬 슬라이드가 붙지 않도록). 주 단위는 주 정리 links → NOTES(type 학습, 같은 과목·주차).
- 회차 표(V37) [학습하기]: 슬라이드 있는 회차만 활성. 없는 회차는 비활성 「학습 준비 중」 — 수업 내용 화면으로 가는 길을 없앰. 현재 활성 = 미적2 9/1·9/3·9/8(행렬). 9/10·9/15·9/17(벡터)은 벡터 슬라이드가 생기면 자동 활성.
- `V33.SPEC["미분적분학2"].rules`에서 「미적분 클리닉 매주 가야 한다(가능하면 매일)」 삭제(원본 줄 수정 + 레이어에서 필터).
- 오타 없이 진행(지침 §13). 게이트 0 · 스모크 124건 × 3시점 실패 0. 캡처: 과목 화면 회차 표(1024px) · 학습하기 클릭 → 슬라이드 뷰어.
- 스모크 하네스 `_shot.html`에 `cl=<selector>`(클릭) 파라미터, 맨 앞 `<meta charset>` 추가(python http.server는 charset을 안 보내서 깨짐).


### .40 — 행렬 단원 학습 슬라이드 (아토: 가로 PPT형, 한 페이지 완료 → 다음 화살표)
- `notes/calc2-matrix-slides.html` — `docs/build_slides.py`가 v2 정리노트(파트 → 개념/기초/응용)에서 자동 생성(템플릿 `docs/slides_tpl.html`). 16:9 무대, 진행 막대, 파트 탭, 페이지 유형: 시작·목차·파트 표지·개념(읽었어요)·문제(답 보기 → 맞았어요/틀렸어요)·파트 결과(기초 전부 + 응용 2/3)·전체 결과(틀린 문제 다시). **완료 전에는 → 잠김**, 완료 시 → 깜빡임. 키보드·스와이프. 진행·정답률 `localStorage mc-slides-calc2-matrix`. 세로면 가로 안내.
- NOTES(type 학습) + 미적2 9/1·9/3·9/8 회차 정리 링크 맨 위(`V36.patchSlides`, 1회). 지침 §14(판서·녹음 최우선, 가로 슬라이드 학습) 근거.

### .39 — 행렬 노트 v2 (아토: 일차별 목차 + 개념 → 기초 → 응용, 행렬만)
- `notes/calc2-matrix-w1-2.html` 교체(15쪽 PDF): 0 일차별 진도 목차(1~6일차) · 파트 1~6 각각 개념 상자 → 기초(학습지 Ex 그대로, 19문제) → 응용(시험 유형 변형, 18문제) · 완료 기준표. 12.1은 벡터 노트로.

### .38 — 행렬 1~2주차 개념 학습 노트 (아토 "1~2주차 개념 학습할 수 있게")
- `notes/calc2-matrix-w1-2.html`(원본 `study-materials/미분적분학2/_정리노트/2026-09-17_행렬_1-2주차_개념학습.html|pdf`, 8쪽) — 학습지 14p + 9/1·9/3 녹음 + 아토 풀이본 + 9/10 필기 합본. 12단계(STEP) · 「왜」·「한 줄」 · 스스로 6문제 · 클리닉 전 체크 · 교육과정 결손표 · 용어 팝업. 교수 강조 = 형광펜, 감점 = 빨간 글씨, 시험 언급 = ★. 1차 정리본(아토 컨펌 대기).
- NOTES 목록·9/3·9/8 회차 정리 링크(`V36.patchLinks`, 1회). 정리노트 PDF는 `#print`로 열어 답 펼침(details 닫힘 상태로는 인쇄 안 됨 — summary 숨김 CSS 제거).

### .36·.37 — 회차 단위 정리 + 과목 화면 재구성 (아토 2026-09-17, 지침 §11·§12) — 오타 회의 대기(Codex 한도 16:30)
- `V36 LAYER`: `c.dayNotes[date]`(주차 정리와 같은 7섹션). 주 2회 이상 과목은 회차별로 편집(`V36.daySheet` = weekSheet를 동기 스왑으로 재사용)·학습(`openStudy(cid,w,date)`). `allTasks`·`weekNoteHas`·`V33.hwState`가 day 데이터 포함. 미적2 V35 주 단위 시드를 9/1·9/3·9/8·9/10·9/15·9/17로 이관(시드 문장과 같은 것만 이동, 1회 `patchV36a`).
- `V37 LAYER`: 과목 화면 「주차별 수업 정리」가 **회차 표** — 주차 × 수업 요일 칸(화 | 목)에 출결 칩·진도·과제·정리 상태 + [학습하기][정리][기록]. 휴강 회차도 칸으로. 8주차 중간·16주차 기말 라벨, 과목 시험일 라벨. 녹음 요약 줄 제거. 상단 한 줄 `(재)과목명 (학점) · 교수님 [요일]` + 출석·지각·결석·시험 D-day 칩. 학습 처방전·주차 타임라인·회차 로그·커버리지·출결·과목 자료·과목 규칙·버튼 묶음·루틴 띠는 「세부정보」 토글 안으로. 회차 표는 화면 전체 폭(아이패드 우선), 폰(≤599px)은 칸 세로.
- 게이트 0 · 스모크 118건 × 3시점 실패 0 · 헤드리스 1024px 캡처 확인(헤드리스 크롬 최소 뷰포트 500px이라 390px 캡처는 잘려 보임 — 레이아웃 overflow 아님, 측정으로 확인).

### .35 — 미분적분학2 1~3주차 주차 정리 시드 (아토 "미적분 1·2·3주차 정리해줘")
- `V35 LAYER`(데이터 시드, 원본 `docs/v35-layer-src.js`). 부팅 후 1회(`term().patchV35a`): 미적2 1주차 빈 칸(교재·판서·필기·할 일 1) 보강, **2주차**(9/8 학습지 (4)~(7) 소행렬식·여인수·역행렬·크래머 = 아토 풀이본 검산, 9/10 12.1 도입) · **3주차**(9/15 곡면·거리·구·12.2 시작, 9/17 벡터 계산·12.3 내적 도입) 7섹션 전부 + 시험 언급 10건 + 할 일 7건 + 링크. 회차 진도 4건(빈 칸만).
- 기존 입력 보존: 문자열 필드는 비어 있을 때만, 배열은 같은 항목(quote/text/url)이 없을 때만 추가. **비정상 기존 값(비배열·null 원소·비문자열·회차 없음)은 건너뛰고 완료 플래그를 보류**(콘솔 경고, 다음 부팅 재시도). 저장 실패 시 플래그 되돌림. 오타 1~3차 RED → 4차 GREEN(`docs/_v35-otta-1..4.md`).
- 정리 노트에 「벡터 정리노트 12.1~12.3 도입」(`notes/calc2-vectors-0917.html`) 추가 — 과목 자료·3주차 링크·NOTES.
- 게이트 0 · 스모크 93건 × 3시점 실패 0.

### .34 — 아토 패치 노트 9/17 (책장·예습 규칙·과목 주차 목록·정리)
- `V34 LAYER`(파일 끝, 원본 `docs/v34-layer-src.js`). 오타 설계 회의 1~4차 RED → 5차 GREEN(`docs/v34-design-brief*.md`, `_v34-otta-1..5.md`).
- **예습 생성 조건**: `generateWeekly` 뒤 `V34.pruneStudy` — 그 과목의 끝난 회차(휴강·결석·인정결석·출튀 제외) 중 `reviewed`가 아닌 것이 하나라도 있으면 `study:` 항목 제거(기록 없는 회차 = 미복습). `gradeReview`·`captureLog` 뒤 재생성. 주 2회 과목은 두 회차 모두 복습해야 예습이 뜬다.
- **할 일 초기화 = 앱 안 결재**: 할 일 탭 상단 배너에 「자동 N건 제거 · 수동 M건 보존 · 재생성 예상 K건」을 먼저 보여 주고 아토가 [실행]을 눌러야 실행. 이번 주 entry만 대상, 백업을 `term().patchV34a.backup`에 두고 설정 → 패치 노트에서 「초기화 되돌리기」. 재실행 없음.
- **책장**: 책등 보기 = 세로 글자만(엠블럼·게이지·배지·표지 사진 숨김), 첫/마지막 책 `margin-inline auto`로 중앙 정렬(넘치면 왼쪽부터 스크롤). 표지 사진 위 상단 스크림 + 한글 과목명(즐겨찾기 별 오른쪽). 「시간표 등록」「과목 추가」 버튼 숨김. 오늘 탭 복습 큐 카드 숨김(FSRS 채점은 학습 마무리에서만).
- **과목 화면 주차 정리 목록 1~16주**: 상태 = 수업 없음 / 휴강 / 미수강(회색) / 결석·보강 필요 / 기록 없음 / 진행 중 / 정리 없음 / 요약. 8주차 「중간고사 주간」·16주차 「기말고사 주간」 라벨 + 과목 `exams` 날짜가 속한 주에 「중간 M/D」 배지(CADD 10주차). 휴강·결석·미기록 배지. 열기·학습 버튼은 지난·이번 주만.
- **주차표 셀 날짜**(M/D, 휴강·학기 밖 셀 포함) — `V32.renderGrid` 래핑 후 DOM에 삽입. **UI 이모지 → 한 글자 라벨**(녹·자·필·판·정·과·영), `MUST`·`V33.SPEC`·`V32.TYPES` icon 치환 + 렌더 후 텍스트 노드 스크럽. 검증은 렌더된 DOM에서 U+1F300~ 0개.
- 게이트 구문 0 · 스모크 82건 × 4시점(현재·9/17·개강 전 8/25·10/20) 실패 0·오류 0 · 헤드리스 캡처(표지·책등 900/390px·과목 16주·주차표·할 일 배너) 확인.

### 데이터 갱신 — 미분적분학2 9/8·9/10 정리 (BUILD 변동 없음)
- 9/10 폴더의 `강의자료_Lecture21.pdf`가 실은 행렬 학습지 14p **아토 풀이본**이라 `2026-09-08/필기_…pdf`로 옮기고 9/8 결석분 정리 작성(Ex05~Ex10 검산 전부 정답, 크래머 j 표기 질문 답). 9/10 지각분은 필기 1장 + 9/15 녹음 역참조로 정리. `progress.json` 미적2 2주차 행 교체, 「당장」 정역학 항목을 보류로. `coverage.json` 미적2 9/8을 자료 안 옴 확정에서 제외 → O. 라이브러리 회차 자료 93.
- (같은 날 추가) 공학 과목 결석분 — 공수1 9/9(슬라이드 p.29~36, 1.4 적분인자 분모 해석 정정)·정역학 9/9(교수 필기본 W12) 정리 O → 자료만(-) 0회차. 공수1 Ch.3~6 슬라이드 4개 `_강의자료/` 저장 + 전체 지도. `progress.json` 공수1·정역학 2주차 행 갱신. 라이브러리 회차 자료 95 · 공통 55.
- (같은 날 추가) 일물2 9/9 결석분 — 강의노트 p.6~7로 정리 O(점전하·점전하군·쌍극자·직선 도선). 자료 안 옴 확정에서 제외. 2주차 결석분 중 남은 n = CADD 9/8·창업 9/7.
- (같은 날 추가) 미적2 9/17 판서 4장 → 정리 O(12.2 벡터 계산, 중간 범위 시작). 빈칸 = 정역학 9/16(보류)만.
- (같은 날 추가) 미적2 9/17 녹음 68분 + 아토 필기 2장 → 정리 ③⑤ 완성(개념 ①~⑤, 12.3 내적 Def·Thm 01 도입, 시험 언급 7건, 필기 부호 오류 1곳 지적). 라이브러리 회차 자료 104.

## 2026-09-15

### .31 — 공업수학1 2주차 과제 로그
- **과제 산출물 3종 등록**(학습시스템 지침 §8.3): `notes/em-hw2-guide.html`(유형 판별·힌트·접힌 검산용 답·답안 형식), `notes/em-hw2-solution.html`(제출용 해답지, 수식 전개만 + 교재 지시대로 1.5 #9·#12 특수해 그래프 SVG), `notes/em-hw2-explain.html`(완전미분·적분인자·선형 공식이 왜 그렇게 되는지 해설 + 9/11 플로차트). `NOTES` 레지스트리 3건, `V27.LOG`에 공업수학1 2주차 링크 3건(boot 패치로 `weekNotes[2].links`에 올라감).
- **오타 검증**: 1차 RED(그래프 누락) → 2차 RED(y축 범위 밖 값을 고정해 가짜 수평선) → 3차 GREEN. 답 5문제는 1차부터 대입·초기조건 검산 통과. 그래프는 함수값 좌표 그대로 두고 `clipPath`로 영역만 가리는 방식으로 수정.
- `V31 LAYER`(파일 끝): 2주차 과제 할 일 두 건의 「교재 문제 원문 미확인」 문구를 「교재 원문 확인(9/15)」로 바꾸고 마감 9/16(수) 09:00을 다시 못박음. 원본 폴더 `study-materials/공업수학1/_과제/2주차_*`(HTML + PDF).
- **버그 수정 A — 복습 채점 저장 회귀**: .25의 `V25 LAYER` `setSession` 래퍼(결석→atom 알림)가 원본 반환값을 버려 `undefined`를 돌려줬고, `gradeReview`가 `s.srs`에서 TypeError → 오늘 탭 복습 큐 채점·학습 마무리 채점이 .25부터 저장되지 않았음. 래퍼가 `s`를 반환하도록 수정. jsdom 스모크에서 같은 오류 86건 → 0건.
- **버그 수정 B**: 주간 할 일 「직접 추가」가 입력칸 없는 화면(개강 전)에서 `.value` null 접근으로 죽던 것 방지.
- 푸시 게이트: `vm.Script` 구문검사 0건 · jsdom 스모크(7뷰+과목 화면 전 버튼, 검사 26건) 오류 0건 × 시점 3개(현재·시험 D-11·개강 전). 오타 회의 GREEN 후 로컬 커밋, GitHub push는 아토 컨펌 대기.
- CHANGELOG에 .26~.30이 빠져 있던 것을 아래에 보충(PATCHNOTES 기준).

## 2026-09-11

### .30
- 주차별 수업 정리도 1주차부터 내려오게 정렬(회차 로그와 같은 순서).

### .29
- 책장 표지에 실제 교재 표지 사진(`covers/`): 공업수학1(Kreyszig 10판) · 미분적분학2(Stewart 9E) · 일반물리학2(Halliday 11판) · 정역학(Pytel & Kiusalaas 4판) · 아카데믹글쓰기. 표지가 있으면 제목 글씨 대신 사진, 아래 진도·출결은 그대로. 교재 없는 과목(CADD 등)은 색 표지 유지.

### .28
- 회차 로그를 1주차부터 내려오게(전에는 최근 주차가 위, 12주까지만 보였음).
- 회차를 열면 맨 위에 「수업 후 올릴 것」 — 녹음본·교재·필기본 3종 업로드 여부. 빠진 것은 빨갛게, 「올리기」로 바로 그 자리로. 그 아래 회차 요약(진도 범위·미완료 과제·정리 요약본 노트 칩). 휴강·학기 밖 날짜 표시.
- 첨부를 못 읽으면 「없음」이 아니라 「못 읽음」으로 구분.

### .27
- **과제 로그**(`V27 LAYER`): 과제 산출물이 과목 화면 「과제」 노트와 주차 정리 링크에 자동 등록. 공업수학1 1주차분(해답지·해설) 반영, 1주차 마감 9/11(금) 09:00 교탁으로 정정·제출 완료 처리.
- 아카데믹글쓰기 주제문 초안(《체인징 배터리》) 과제 노트 등록 · 2주차 할 일 추가.
- 공업수학1 2주차 과제 등록 — 1.5 #9·12(9/11 출제) · 1.4 #4·11·12(9/9 출제), 둘 다 9/16(수).

## 2026-09-09

### .26
- `V26 LAYER`: 학습 탭 상단에 「지식맵 · 이걸 이해하려면」 진입 버튼 — `learn.html?subject=<과목>`으로 연결(지식 그래프·학습 카드·진도 현황은 별도 파일 `knowledge/`·`learn.html`·`coverage.html`, 같은 기간 커밋).

## 2026-09-08

### .25 — 대규모 업데이트 (시험 모드 · 타이머 · atom/LMS 연동)
- **시험 모드 패키지**: 상단 「가용 공부」 게이지 = 시험 시각까지 수업·캘린더 일정·활동 블록·수면(기본 7h)·생활 여유(기본 3h)를 뺀 순수 공부 시간. 학습 탭 맨 위 「시험 모드」 보드: 과목별 D-day, 시험 범위 주차 칩(✓ 공부함 · ▤ 정리만 · 없음, 누르면 학습 화면), 필요 시간(미학습 단원 × 2h + 학습한 단원 × 0.5h) vs 가용 시간 판정, 시험 가까운 순 전략. 오늘 브리핑에 한 줄 + 오늘 목표 시간. D-14부터 켜짐(`phase()==="EXAM"`).
- **공부 타이머**: 오늘 탭 「공부 타이머」 카드 — 과목 칩 → 시작/종료 → `S.studyLog`에 `grade:"timer"`로 기록(학습 탭 누적·시험 모드 계산에 합산). 진행 중 상태는 `localStorage mc-timer`라 새로고침해도 이어짐. 1초 틱은 기존 interval에 `V25.tickTimer()`.
- **atom 연동**: atom 안(`/study/`)에서는 웹훅이 없어도 `fetchGcal`이 atom `/api/cal`로 대체되어 오늘 화면 빈 시간·가용 시간에 캘린더가 반영됨. 출결에서 「결석」을 찍으면(사전 지정 포함) `POST /api/absent` → 아톰이 캘린더 블록을 토마토로. atom 홈·브리핑은 학습앱 상태(`study_state`)의 시험 D-day·시험 모드를 그대로 씀.
- **LMS(Canvas)**: atom에 LMS 토큰이 있으면 `/api/lms/state`를 읽어 할 일 탭 「LMS 과제」 카드(미제출·마감·상태·열기 + 최근 공지), 과목 화면 「LMS」 카드(과제·이번 주 모듈 자료·공지, 과목명 자동 매칭), 오늘 브리핑 「LMS 미제출 N건」. 읽기만, 10분 캐시.
- **정책 반영**(아토 판단 기준 답, 2026-09-08): 시험 모드 중 학습 큐를 시험 가까운 과목부터(과목 가중 없음) · 중간고사 미정 과목은 8주차 가정(`exam.assumed`, CADD 10주차→8주차, 정역학만 확정) · 공개 노트에서 교수 성향 분석 제거(`notes/em-prof-analysis.html` → `Desktop\학습시스템\비공개 노트\`, 레지스트리·주차 링크에서 삭제, 1회 패치 `patchV25b`).
- 폰(atom 안): 상단 저장·새로고침·게이지를 한 줄로. 구현은 기존 함수를 감싸는 `V25 LAYER`(파일 끝)에 모아 두어 위 코드는 건드리지 않음.


### .24
- **강조색을 알파인 그린으로**(아토 지시): 라이트 `--accent:#4F9A35`(글자용 #2F6F22, 연한 배경 #E7F2E1, 선 #BFDDB3), 다크 `#5CAB3F`(글자 #8FD07A). 버튼·활성 탭·타일·게이지·즐겨찾기 강조·로고 배경이 전부 바뀐다. 브라우저 테마색(theme-color)도 같은 색.
- 파비콘 `icon.svg`와 홈 화면 아이콘 `icon-180.png` 배경을 같은 초록으로 다시 생성. 과목 표지 색(유형별 파랑·노랑·주황 등)과 상태색(ok·warn·crit·info)은 그대로.

### .23
- 사이트 이름을 「기계공학 학습 관제탑」에서 **「aTTo 학습앱」**으로 변경 — 브라우저 탭 제목, iPad 홈 화면 이름, 로고 대체 텍스트, README 제목, 노트 허브의 앱 링크 이름. 저장소 이름(study-console)과 주소는 그대로.

### .22
- **로고 교체**: 상단 마크의 글자 기호(◎)를 선형 SVG 아이콘으로 바꿈 — 펼친 책 위에 관제탑(안테나)과 신호 호 두 줄. 디자인 지침(이모지·글자 기호 대신 선형 아이콘)에 맞춤.
- 같은 아이콘으로 브라우저 탭 파비콘 `icon.svg`와 iPad 홈 화면 아이콘 `icon-180.png`(둥근 사각 배경) 추가. 홈 화면 이름 「학습 관제탑」.

### .21
- **「학습」 탭 신설** — 노트 탭을 흡수(노트 목록은 학습 탭 아래 카드로, `go("notes")`는 학습으로 연결). 앱 안에서 수업 내용을 실제로 공부할 수 있게 한 첫 버전.
- **학습 큐**: 과목 × 주차 단원을 복습 예정(FSRS due, 많이 지난 것부터) → 이번 주 → 최근 주차 → 수업 요일 순으로 나열. 「복습할 것 / 전체」 전환(선택 기억). 각 단원에 기억 %·지난 일수·정리 섹션 수·★ 시험 언급·회차 기록·노트 수·마지막 공부 시각.
- **학습 화면**(단원): 상단에 과목·주차·진도 범위·회차 칩(복습됨 표시)·한 줄 요약, 공부 시간 타이머. 본문은 채워진 것만 — ★ 시험 언급 → 5섹션 정리(교재·판서·녹음·강의자료·필기) → 회차 기록(진도·타임라인·아는/막힌/새로 이해/놓친 지점) → 정리 노트 링크(앱 안 뷰어로 열림). 「읽기 / 떠올리기」 모드 — 떠올리기는 섹션 제목만 보이고 펼쳐서 확인.
- **학습 마무리**: 학습 메모(주차 정리에 저장) + 다시/어려움/보통/쉬움 채점 → 그 주 회차들의 FSRS 카드에 반영(오늘 탭 복습 큐와 같은 주기), 공부 시간이 `S.studyLog`에 기록되어 학습 탭 상단에 오늘·이번 주 합계로 표시. 회차가 없는 주는 시간만 기록.
- 진입점: 학습 큐의 「학습」, 과목 화면 주차별 수업 정리 목록의 「학습」, 오늘 탭 복습 큐 각 행의 「학습」.
- 아직 없음: 뒤로 가기만 하면 시간이 기록되지 않음(채점해야 기록), 섹션 단위 퀴즈·빈칸 문제, 학습 타이머의 시험기간 카운트다운 연동.

### .20
- **할 일 주차별 보기 재정렬**: 각 주차 안에서 과목 묶음을 책장 순서 대신 **수업 요일 순**(월→일, 같은 요일이면 시작 시각 순)으로 배치. 수업 시간이 없는 과목은 그 뒤, 개인 공부, 공통 순. 과목 이름 옆에 요일 칩(월·수 등, 개인 공부는 「개인」).
- 주차 제목에 날짜 범위(8/31(월)~9/6(일))와 마감 지난 미완 건수 「지남 n」 배지. 과목 묶음 헤더에도 「지남 n」. 항목을 다 한 묶음은 흐리게.
- 항목 줄 정리: 마감의 요일 중복 표기 제거(「수 9/2(수)」 → 「9/2(수)」), 출처(매주 루틴·수업·주차 정리)는 흐리게, 주차 정리 과제도 다른 항목과 같은 형식(빨간 점 + 과제). 삭제 ✕는 마우스를 올렸을 때만 진하게.

## 2026-09-07

### .19
- **표지 루틴 픽토그램 세로 쌓임 수정**: 픽토그램 묶음 클래스 `.rg`가 오늘 화면의 `.rg`(세로 flex)와 이름이 겹쳐 방향이 세로로 바뀌어 있었다. 표지(`.bk-rt .rg`)와 과목 화면 루틴 띠(`.rt-strip .rg`)에 가로 방향을 명시. 이 때문에 사회봉사·미분적분학2처럼 픽토그램이 2개 이상인 표지가 아래로 길어져 .18의 제목 잘림 원인 중 하나이기도 했다.
- 표지 제목 아래 구분선 위아래 여백 8/7 → 6/5px.
- 표지 높이를 고정값에서 **최소값**으로 바꿈. 배지가 세 줄(요일·재수강·중간·기말)이라 내용이 넘치는 표지가 있으면 같은 줄의 표지들이 함께 조금 길어져 학점 줄과 게이지까지 다 보인다. 책등 보기는 고정 높이 그대로.
- 확인: 헤드리스 Chrome 스크린샷(820px·390px)으로 학교 책장·개인 책장·개인 공부 화면·할 일 주차별·사회봉사 과목 화면 점검. 휴대폰 폭에서 페이지가 가로로 넘치는 것처럼 보였던 것은 과목 현황 표가 카드 안에서 내부 스크롤되는 것이라 실제 넘침은 아님.

### .18
- **표지 제목 잘림 수정**: 표지는 높이가 고정인데 루틴 픽토그램 줄과 배지 줄(요일·재수강·중간·기말)이 두 줄로 늘어나면 제목 요소가 세로로 줄어들어 윗부분이 잘렸다(.10에서 픽토그램을 넣은 뒤부터). 엠블럼·제목·구분선·발치는 줄지 않게 하고 영문 부제가 먼저 접히도록 바꿈. 픽토그램 줄은 한 줄 고정, 휴대폰(≤420px) 표지 높이 172 → 182px.
- **할 일 탭 주차별 보기**: 주차를 1주차부터 오름차순으로 정렬해 지난 주에 안 한 항목이 이번 주 위에 보이도록. 지난 주는 미완 항목이 남은 과목 묶음만 펼쳐지고, 주차 미정은 맨 뒤.
- **사회봉사 규정 반영**(부팅 패치 `patchSvStepsV1`, 1회 적용): 과목 자료의 강의계획서에 1~3단계 규정(소양교육 → 봉사 30시간 9/2~11/29 → 소감문 보고서 포털 입력 9/9~11/30, 3,000byte 이상·띄어쓰기/공란/단락 없이·저장→수정 확인)과 체리 안내(회차 일정, 시간 인정 기준, 해시태그·SNS 조건, 결과보고서 양식 항목, 주의사항, 문의처)를 기록. 과제·할 일에 체리 1~5회차 마감(9/20·10/4·10/18·11/1·11/15, 각 콘텐츠 2건+보고서 2건), 30시간 이수(11/29), 소감문 보고서(11/30), ZOOM 평가간담회(11/26)를 추가.
- 상단 D-day 게이지 라벨에서 「(고정)」 표시 제거(「중간고사 (고정)」 → 「중간고사」). 고정 기준일 기능 자체는 그대로.

### .17
- **책장 분리**: 책장 상단 세그먼트로 「학교 수업」과 「개인 공부」 두 책장을 오간다(마지막 선택 기억). 학교 책장은 기존 그대로.
- **개인 책장**(비교과·개인 공부): 자격증·독학·프로젝트·어학·기타 종류별 표지 색. 항목마다 목표 한 줄, 목표일(D-day 배지), 진도 게이지, 매주 루틴 픽토그램이 표지에 뜬다. 「공부 추가」로 넣고, 길게 누르면 열기·즐겨찾기·완료·편집 메뉴.
- **개인 공부 화면**: 목표·진도(직접 입력 ±5/±10 또는 「단계 추가」 후 단계 완료율 자동 계산), 학습 기록(날짜·분·메모, 누적·이번 주 합계), 매주 루틴(과목과 같은 편집 시트·이번 주 없음 토글), 메모·링크.
- **할 일 연동**: 개인 루틴도 매주 월요일 할 일에 들어가되 학교 과목 다음 순서(출처 「개인 루틴」). 과목별·주차별 보기에 개인 공부 묶음이 표시되고, 「과목 화면」 버튼은 개인 공부 화면으로 연다.
- **완료 처리**: 책장·할 일에서 빠지고 「완료 n개 보기」로 다시 볼 수 있다. 되돌리면 루틴도 복귀.
- 개인 책장은 학점 계산·출결·시험·복습 큐·달력에 전혀 들어가지 않는다(저장 위치 `S.personal`, 학기와 무관).

### .16
- 정역학 9/2(수) 회차를 「수강정정 전 · 출결 제외」에서 「결석」으로 변경. 출석 인정을 요청했으나 9/7에 어렵다는 답변을 받음. 출결 위험도와 지난주 결석 보강 항목에 반영.

### .15
- 정역학 루틴 「과제 제출 ×1(수요일)」 추가. 평소에는 과제가 있으나 9/7 주는 없어서 이번 주만 건너뜀.
- 루틴마다 「이번 주 없음」 토글(과목 화면 루틴 띠의 – 버튼). 그 주만 표지 픽토그램과 할 일에서 빠지고, 다음 주에는 자동으로 돌아온다. 되돌리기는 「이번 주 있음으로」.

### .14
- 할 일 탭에 「전체 / 과제·제출 / 복습 / 예습」 선택 탭. 묶음별·과목별·주차별 세 보기 모두에 필터로 적용되고 마지막 선택을 기억. 한 묶음만 고르면 카드가 전체 폭으로 펼쳐진다.

### .13
- 공업수학1 과제 주기 확정: 금요일 출제 → 수요일 제출(루틴 라벨에 표기). 1주차 과제만 예외로 9/11(금) 수업 시간 제출 — 이번 주 할 일과 1주차 정리 ⑦의 마감을 9/9 → 9/11로 갱신.

### .12
- 학습모드 「M8 과제제출형」 추가. 과목 현황 표에서 사회봉사가 출석유지형 대신 과제제출형으로 표시.

### .11
- 과목 유형 「과제 제출형」(SUBMIT) 추가, 사회봉사에 적용. 시험 없이 매주 산출물을 내는 과목으로, 예습·복습 큐에는 들어가지 않고 루틴으로만 관리.
- 할 일 탭: **묶음별 / 과목별 / 주차별** 보기 전환(마지막 선택 기억). 과목별은 이번 주 항목을 과목 카드로, 주차별은 지난 주까지 전부 주차 → 과목 순으로 접이식 정리.
- 할 일 탭 글자 잘림 수정 — 「과제·할 일 전체」 제목 요소가 시간표용 `.tt` 클래스(grid, 최소 폭 520px)와 충돌하던 문제. 모든 할 일 텍스트에 `word-break: keep-all` 적용.
- 설정에 패치 노트 카드, 저장소에 `CHANGELOG.md`.

### .10
- **과목별 매주 루틴**: 책장 표지와 과목 화면에 픽토그램(업로드·문서·영상·복습·정리)이 주당 횟수만큼 뜬다. 안 하면 회색, 하면 채움, 마감 요일이 지나면 빨간 테두리. 픽토그램을 누르면 완료 처리.
- 루틴은 「할 일」 탭의 이번 주 항목과 양방향 동기화. 월요일마다 새 주로 리셋되고 지난 주 기록은 남는다.
- 기본값: 사회봉사 카드뉴스 업로드 ×2·보고서 제출 ×1, 창업아이디어탐색 수업 후 과제 ×1, CADD 강의영상 시청 ×1(수요일 전), 공업수학1 문제풀이 과제 제출 ×1(수요일), 미분적분학2 수업 복습 ×2, 일반물리학2 수업 내용 정리 ×2. 아카데믹글쓰기·정역학은 미정.
- 설정의 매주 규칙 중 CADD 영상·카드뉴스 규칙은 루틴으로 이관(과목 화면 → 「매주 루틴」에서 편집).

### .9
- **「할 일」 탭 신설**: 이번 주 할 일(과제·복습·예습) + 과제·할 일 전체(주차 정리 ⑦ 항목 마감순, 완료 체크, 주차 정리 열기).
- 오늘 탭은 할 일 요약 한 줄(과제 n · 복습 n · 예습 n · 임박 건수 → 할 일 탭 열기)만 남김.

### .8
- 학점 탭 졸업 계획표: 계절학기 포함 12학기(2026 겨울 ~ 2029-2). 학기마다 계획 과목을 ＋ 과목으로 추가하고 ✕로 빼며 학점은 자동 합산. 잔여 학점 대비 부족/여유와 전공 진행 표시.
- 과목 주차 보드 라벨을 「1주차 … 8주차(중간) … 16주차(기말)」로 변경.
- 사회봉사 P/F 처리: 필요 등급 표에 「P (통과)」, 평점 역산에서 제외, 등급 입력은 P/NP.

### .7
- 학점 탭에 「앞으로의 학기 · 졸업까지」 계획표(학기별 계획 학점·누적·메모, 잔여 학점 검사).

### .3 ~ .6
- 2026 여름학기 성적 입력(행복한금융생활 B+, 미분적분학2 D+), 미분적분학2 재수강 표시(최대 A0).
- 사회봉사 표지 문구를 「주2회 카드뉴스 업로드」로.
- 상단 D-day 기준일을 10/19 중간고사로 고정(설정에서 변경 가능).

### .1 ~ .2
- 매주 월요일 「이번 주 할 일」 자동 생성(① 과제·제출 ② 복습 ③ 예습·공부, 우선순위 과제 ≫ 복습 > 예습) + 설정에서 규칙 편집.
- 상단에 ↻ 새로고침 버튼(캐시 우회).
- 2026-1학기 이력 시드(9과목 20학점).
- 오늘 타임라인: 구글 캘린더 일정을 수업 사이 빈 시간에 표시(얼라이브위크 웹훅 GET `?action=events`), 수업을 일찍 끝내면 실제 종료 시각부터 빈 구간.

## 2026-09-04 ~ 09-05
- 강의 정리 노트를 앱에 통합: `notes/` 폴더, 노트 탭, 앱 내 뷰어, 과목 화면 문서 목록.
- 회차 로그를 주차 묶음 + 요일별 한 줄(주 2회 과목은 두 줄)로, 이번 주 예정 회차 포함.
- 「W1」식 표기를 「1주차」로 전부 통일.
- 주차별 수업 정리(① 교재 ② 판서 ③ 녹음 ④ 강의자료 ⑤ 필기 ⑥ ★시험 언급 ⑦ 과제→할 일) 추가 + 1주차 내용 시드.
- 정역학 강의계획서·1주차 OT, 아카데믹글쓰기 1주차 자료, 일반물리학2 슬라이드 반영. 정역학 9/2 회차는 수강정정 전이라 출결 제외.
- 문자열 줄바꿈 이스케이프 오류로 앱이 안 뜨던 문제 수정.
- 설정에 빌드 번호 표시.

## 2026-09-01 ~ 09-03
- 복습 큐 버그 수정(1·10분 뒤 재노출 제거, 다음 복습일 하루 밀림).
- 얼라이브위크 연동: 출결·수업 시각을 구글 캘린더에 자동 반영.
- CADD 중간고사를 이론 암기 위주로 반영.
- 상단바 오늘 날짜·요일, D-day를 가장 빠른 중간고사 기준으로, 게이지 라벨 정리.
- 자동 수업 시작, 휴강 사전 지정, 오늘 화면 주간 요일 스트립.
- FSRS 복습 큐 연결(srs-core 인라인 번들), 회차 심화 기록.

## 2026-08-29
- 학습 관제탑 v0.1 초기 커밋.
