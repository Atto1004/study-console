# v52 브리프 — 수업 따라가기(.61) · 글래스 스킨(.62) 사후 검수용 (대표님 2026-09-24 밤 지시, 오타 부재로 먼저 배포)

## A. 수업 따라가기 (V50 LAYER, BUILD .61)
- 대표님 원문: "특히 이해를 돕는 '학교 수업진도 따라가기' 이거 구현이 시급해". 앱·기획서·지침 어디에도 정의 없음 → 아톰 해석: **학교가 나간 회차(주차×요일)를 순서대로 따라가며 이해**. 시험 대비(V49)는 단원별 총정리, 이것은 회차별 따라잡기.
- 화면: 학습 탭 첫 카드 + 오늘 탭 카드(#v50Today, 과목별 가장 오래된 밀린 회차 4개) + 브리핑 첫 줄(#v50Step).
- 회차 목록 = `V32.meetings(c)`(과목 슬롯 × 학기 주차, 휴강·취소 제외). 회차 제목 = `knowledge/sessions.json`(`docs/tools/session_index.py`가 study-materials/<과목>/<날짜>/{정리.md|수업요약_*|영상정리_*|자료요약_*}의 H1에서 과목·날짜·주차 칸을 뺀 것; 없으면 앱 진도 기록 → 덱 파트 제목).
- 회차 ↔ 덱 파트: `knowledge/decks.json partList[n].dates` = 덱 표지 `no` 문자열("파트 1 · 9/7·9/9", "9/16 → 9/21·23")에서 `deck_index.py`가 날짜를 파싱. 문제 덱(kind exam) 우선, 없으면 임시 덱(temp).
- 상태: 파트 진행률 = `mc-slides-<deck>` ST.done ∩ partList.sids. 여러 파트면 평균. ≥.9 따라감 · >0 진행 중 · 0이면 (오늘이면 오늘 수업, 아니면 밀림) · 파트 없고 정리 있으면 정리만 · 둘 다 없으면 정리 대기.
- 「따라가기」 = `openNote(file+"#at=pN-cover")` — ratio<.9 인 첫 파트. 「회차 정리」 = `openStudy(cid,w,date)`.
- 검사: `_v50chk.cjs` 15항목.

## B. 글래스 스킨 (V51 LAYER + 덱 템플릿 V6F, BUILD .62)
- 대표님 원문: "학습앱도 글래스모피즘 형식으로 디자인 세련되게 싹 검토, 가능한 심플하고 깔끔하게".
- CSS만: 바탕(그러데이션 + 번지는 빛 2, fixed) · 유리 패널(.card .tile .modebar .topbar .rail .sheet .toast: rgba 백 + blur(20px) saturate(140%) + 흰 헤어라인 + 옅은 그림자) · 안쪽 행(.lrow .crow .drow .v47-wk .v49-deck .v50-s: rgba(255,255,255,.38)) · 조작부(.btn .seg .chip .input). 글자 토큰 --ink 유지. 다크 = 어두운 유리. `@supports not (backdrop-filter)` 폴백.
- 덱 V6F: 무대·상단·하단·보기·버튼·개념 상자·시험 언급·풀이판 유리. 테두리 두께·여백 불변 → 1024×768 검사 9덱 결과 동일.
- 캡처: 학습·오늘·할 일·과목·설정 1024 라이트/다크, 폰 390 학습·오늘(가로 넘침 0), 덱 문제·개념·표지.

## C. 오타에게 묻는 것 (GREEN/RED)
1. A의 「수업진도 따라가기」 해석이 대표님 문장과 어긋나는 점이 있는가(대안 해석이 있으면 제시).
2. 회차↔파트를 표지 날짜 문자열 파싱으로 잇는 방식의 구멍(휴강 날짜, 한 회차가 두 파트, 파트가 두 회차, 표지에 날짜 누락) — 실제 decks.json·sessions.json 으로 확인.
3. 상태 판정(90%/평균/오늘 회차 구분)의 허점 — 예: 파트 하나만 끝낸 두 파트 회차, 문제만 푼 경우.
4. 글래스 CSS의 대비·가독성(다크 --ink-2/--ink-3 on 어두운 유리), 성능(카드마다 backdrop-filter — 아이패드에서 층 수), 폴백 누락 선택자.
