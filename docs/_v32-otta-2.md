GREEN — 1차 지적 6건의 설계상 대응을 확인했습니다. 구현으로 진행해도 됩니다. 구현 완료 판정은 아닙니다.

1. 출결 집계: 닫혔습니다. r2 §1에서 6종 집계·휴강 제외·`late+vlate` 표시를 명시했습니다. 학습 완료는 `reviewed`로 유지하고 추가 XP를 지급하지 않습니다.
2. 자료 병합·기존 필기: 닫혔습니다. 출처별 개수와 PC 열기 제한을 구분했습니다. 기존 `판서`는 미분류로 보존합니다. `index.html:6155·6288`의 두 저장 경로를 모두 반영했습니다.
3. 자료 목록·분류: 닫혔습니다. 상대경로 식별자·생성시각·접근 여부를 추가했습니다. 공통 자료는 회차 판정에서 제외합니다. 확장자·폴더 문맥과 전체 파일 수집을 반영하고 공개 배포에서 제외합니다.
4. 모바일 화면: 닫혔습니다. 요일별 목록을 기본으로 둡니다. 표는 열당 최소 140px와 가로 스크롤을 적용합니다. 출결 약자로 색상 구분을 보완합니다. 실제 가독성은 구현 후 확인해야 합니다.
5. 일차 계산: 닫혔습니다. 수강 시작일·휴강을 제외하고 보강을 포함하는 공통 목록으로 계산합니다. `index.html:7177–7180`에서 정역학 9/2의 수강 전 처리 근거를 확인했습니다. 사진과의 일치는 직접 확인 못 했습니다.
6. 분모·과제 누락: 닫혔습니다. `MUST.length`로 분모와 완료 조건을 통일합니다. 모든 주차의 과제를 마감일로 검색합니다. 기존 `index.html:6004–6006`의 고정 분모를 교체하는 안입니다.

1-b: 철회합니다. 보충 후에만 복습하도록 강제한 요구는 이번 기록·파악 범위를 넘었습니다. `index.html:5866`은 결석·휴강·종료 전 회차를 제외합니다. 보충 표시와 기존 복습 흐름 유지에 동의합니다.

3-a: `courseId` 필수 요구를 철회합니다. `index.html:2280·4931`의 `uid()` 생성이 근거입니다. 이름 연결과 미연결 보존을 수용합니다. 다만 과목명 자체가 고유하다는 보장은 없으므로 중복 이름도 미연결로 남겨야 합니다.

구현 시 r2 §7(c)의 “모든 줄 unknown”은 정정해야 합니다. PC 조회가 실패해도 이미 확인한 기기 자료의 `have`는 유지하고, PC 확인 실패를 별도로 표시하십시오.

상태: 설계 검토 완료. 파일 변경·실행 검증 없음. 아토가 하실 일은 없습니다.