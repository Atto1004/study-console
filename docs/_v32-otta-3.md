**RED** — 2차 합의 조건 누락과 입력 소실을 재현했습니다.

1. PC 실패: 통과입니다. `index.html:7829`의 기기 자료 우선 판정으로 `have`가 유지됩니다. `mustHTML`도 PC 확인 실패를 별도 표시합니다. 텍스트·첨부 경로 모두 확인했습니다.
2. 출결: 통과입니다. 6종 각 1건과 휴강 결석 1건을 넣어 `held=6`, `rate=83`, `lateN=2`, `eff=1`을 재현했습니다. 휴강은 집계에서 제외됩니다.
3. 기존 판서: 일반 첨부는 미분류 표시·재분류가 됩니다. 그러나 `tl`이 있는 판서는 `6170·6229·7993`에서 제외되어 미분류 표시와 분류 선택지가 모두 없습니다. 새 타임라인 사진도 `6295`에서 여전히 `판서`로 저장됩니다. GREEN 조건은 기존 타임라인 사진의 재분류 경로와 새 저장 종류·집계 반영입니다.
4. 인벤토리: `_진도커버리지.py`의 분류·전체 순회·공통 자료 분리는 확인했습니다. `library.json`은 Git 제외·미추적입니다. 다만 같은 이름의 과목 2개를 넣으면 `8090`이 마지막 과목에 연결합니다. 2차 조건대로 중복 이름은 미연결로 남겨야 GREEN입니다.
5. 회차: `meetings()`의 수강 시작일·휴강·학기 밖 제외는 확인했습니다. 정규 일정은 `planned():3207`, 추가 세션은 `7933`에서 학기 범위를 제한합니다. 정역학 9/7=1일차 검사도 통과했습니다.
6. 출결 이관: `patchV32b`는 색 칸 11건을 덮어쓰고 검정 칸의 기존 상태·진도를 보존했습니다. 범위 밖 상태도 유지됐습니다. 아토 시트 원본은 제공되지 않아 사진과의 일치는 확인 못 했습니다.
7. 추가 결함: 진도 입력 후 재분류하면 `7907`의 시트 재열기로 미저장 진도가 사라집니다. 입력 보존이 GREEN 조건입니다. 모바일 1주차에는 학기 밖 8/31 수업 1건도 표시됩니다(`8063`). PC와 같은 날짜 제한이 필요합니다.

구문 3개 오류 0, 스모크 29건×3시점 실패·오류 0을 재확인했습니다. 구현 원본과 앱 삽입부도 동일합니다.

아토가 하실 일: 아톰에게 위 수선 조건을 전달해 주세요.
상태: 구현 검수 완료. 파일 변경·외부 기록 없음.