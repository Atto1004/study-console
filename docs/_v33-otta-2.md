RED
1. MUST 교체 — 설계상 닫혔습니다. r2:5의 배열 참조 유지·동기 호출·finally 복원은 1차 수선 조건과 일치합니다. 구현 후에는 길이뿐 아니라 원래 항목·순서와 예외 발생 시 복원도 확인해야 합니다.
2. hw·상단·CADD 판정 — 일부 닫혔습니다. hw의 `linked` 표시와 상단 `unknown` 분리는 타당합니다. 다만 r2:9는 CADD 영상을 `LIB.items`에서 찾고, r2:12는 `_영상정리`를 `COMMON_DIRS`에 넣습니다. 생산 위치와 조회 위치가 다릅니다.
3. classify 호환 — 설계상 닫혔습니다. 기존 `pre`를 유지하고 상단은 파일명으로 판정하므로, 1차의 `pre↔material` 불일치 경로가 제거됩니다. 구·신 JSON 실제 검사는 아직 확인 못 했습니다.
4. 입력·탐색 경로 — 일부 닫혔습니다. hw 전용 판정과 `KINDS_ATT` 추가로 예외·선택지 누락을 해소하며, 점 개수도 `post.length`로 정했습니다. 그러나 영상 탐색 결과가 주차 판정까지 연결되지 않습니다.

새 치명 근거는 `_진도커버리지.py:228–233`입니다. 날짜 폴더만 `items`에 `date`와 함께 저장하고, 공통 폴더는 `common`에 날짜 없이 저장합니다. 수정안대로 `_영상정리/영상정리_2026-09-15.md`를 수집해도 CADD의 `items` 검색에는 잡히지 않습니다.

GREEN 조건: 영상의 대상 수업일을 명시적으로 추출하여 생산·조회 위치를 통일하고, 해당 주만 충족되는지 검증해야 합니다. 수정 시각을 수업일로 대신하면 안 됩니다.

상태: 설계 4건 대조, 2건 닫힘·2건 부분 해소. 파일 변경 없음.