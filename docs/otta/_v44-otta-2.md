# .49 V44 구현 오타 검수 (2026-09-21) — 5차 RED(5) → 6차 GREEN(아토 승인 조건부: ensureDeckNotes)

## 1차
**판정 — RED.** 확인된 결함 5건입니다. 파일은 수정하지 않았습니다.

1. **RED — 내부 버튼 키보드 동작.** `index.html:9597,9652`에서 자식 버튼의 Enter·Space도 과목 이동으로 처리합니다. 메뉴 버튼 Enter를 메모리 실행해 `go("course","c1")` 호출을 확인했습니다. 행 자체에 초점이 있을 때만 처리해야 합니다.

2. **RED — 터치 목표 미달.** `index.html:9716`의 `.crow-more`는 36×44px입니다. 브리프 §1의 최소 44×44px에 맞춰야 합니다.

3. **RED — 저장 상태 전이 누락.** `index.html:9537`에서 `markSave("dirty")`가 `ui.dirty`를 켜지 않습니다. 저장된 상태에서 호출하니 `dirty=false`가 유지됐습니다. 명시적 dirty 호출은 변경 상태로 전환해야 합니다.

4. **RED — 덱 진도 분모 누락.** `deckProgress`는 `last.n`이 있는 덱만 합산합니다. 두 덱 중 하나만 열어 완료하면, 미열람 덱을 제외하고 100%가 됩니다. 전체 덱 분모를 확보하거나 미확인 상태로 표시해야 합니다.

5. **RED — 변경 허용 목록 이탈.** `ensureDeckNotes`의 NOTES 등록은 §5 밖의 데이터 변경입니다. 별도 보고만으로 허용되지는 않습니다. 이번 빌드에서 분리하거나 변경 범위 승인이 필요합니다.

- **GREEN — 시각 방향.** 첨부 화면에서 행 칩·평면 카드·5탭·시간순 수업 행이 확인됩니다. 색 제한과 .50 예정인 덱 조작부는 결함으로 세지 않았습니다.
- **GREEN — 정적 회귀 구조.** 기존 래핑 호출과 `#dayBooks` 삽입 기준을 유지합니다. 교체된 `.book`의 직접 리스너가 새 `.crow`에 중복 연결되지는 않습니다.
- 전체 회귀·XSS·다크 화면은 **판정 불가**입니다. 보고된 검사 결과를 독립 재실행하지 않았으며, 입력 검증과 실제 화면 증거가 더 필요합니다.

전체 판정: RED(5건)
40,291

## 2차
**판정 — GREEN.** 제공 코드 기준, 아토 승인 조건부입니다.

- (1) `.crow`·`.drow` 모두 자식 키 입력을 무시합니다. 자식 버튼 클릭도 과목 이동에서 제외됩니다.
- (2) `.crow-more`는 44×44px로 수정됐습니다.
- (3) `"dirty"`·`true` 모두 `ui.dirty=true`를 설정합니다.
- (4) 미열람 덱이 섞이면 `덱 k/n 진행`을 표시합니다. 전체 분모가 확인될 때만 %를 표시하며, 완료 수는 덱별 `n`으로 제한합니다.
- (5) `ensureDeckNotes`는 허용 목록 밖 변경으로 남습니다. push 확인 때 아토의 별도 승인이 필요합니다.

47건 통과·구문검사·스모크 결과는 직접 재현하지 않았습니다. 파일 변경 없음.

전체 판정: GREEN
28,118

