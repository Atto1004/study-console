**판정 — RED.** 소스 기준 14건 중 GREEN 6건, RED 7건, 보류 1건입니다. 실제 77장·빌드 코드·브라우저 검사 결과는 확인 못 했습니다. 아래 재현 조건은 제공된 코드 기준입니다.

1. **GREEN — 본문 서체.** 기본 본문을 나눔손글씨로 교체했습니다. 실제 글리프 누락과 폰트 로딩은 3번에서 별도로 판단합니다.

2. **RED — 수식·숫자·기호 분리.** `파트 '+s.part`, 시작 화면의 문제 수, 결과의 백분율은 여전히 손글씨를 상속합니다. 구분자 없는 숫자는 `typeset()`이 처리하지 않습니다. 숫자·기호를 정자 요소로 분리하고, 자유 입력에는 한글 설명과 수학 답의 구분 또는 입력 제한을 적용해야 합니다.

3. **RED — 폰트 로딩 실패 처리.** `document.fonts.ready` 이후 실제 폰트 성공 여부를 확인하지 않습니다. 본문 폰트 요청이 실패하고 KaTeX만 로드되면 대체 서체로 화면을 공개합니다. 필요한 서체의 로드 성공과 사용 문자 지원을 검사하고, 실패하면 학습 화면 공개를 중단해야 합니다. 로컬 포함은 권장안이며 지침상 필수는 아닙니다.

4. **RED — 글자 크기 3단계.** `.src-th .src-n`에 `font-size:14px`가 남았습니다. `var(--fs-note)`로 교체해야 합니다. `s.html`의 제목 요소도 확인해야 합니다. 현재 `h3`는 초기화에 포함됐지만 크기를 지정하지 않아 브라우저 기본 크기를 사용합니다.

5. **GREEN — 색상 값.** 의미색 5개가 지정값과 일치합니다. 흰색·검정만 허용한다는 1차 해석은 철회합니다. 지침에는 회색 선·배경까지 금지하는 근거가 없습니다.

6. **GREEN — 색상 의미.** 개념·암기·주의·시험·풀이의 배정과 무채색 조작부가 소스에서 확인됩니다. 실제 삽입 콘텐츠의 색상 사용은 확인 못 했습니다.

7. **RED — 시험 언급 표시.** `mu` 분기는 `examHTML(s)`를 호출하지 않습니다. `mu`에 시험 언급을 넣으면 진행점만 핑크색이고 인용 상자는 누락됩니다. 시험 상자를 공통 렌더 경로로 옮기고, 강도·원문 누락을 검사해야 합니다. 녹음과 인용의 일치 여부는 확인 못 했습니다.

8. **GREEN — 화면 구조.** `16/10`과 상단·작업 영역·하단의 공통 격자가 확인됩니다. 화면 크기에 따른 넘침은 11번의 문제입니다.

9. **GREEN — 힌트 조작.** 문제 장에서 파트 요약을 표시하고, 요약이 없으면 비활성화합니다. 힌트 안 수식 처리는 14번에서 수정해야 합니다.

10. **RED — 공통 초기화.** `:where(...)`에 `pre`, `dl`, `dd` 등이 빠졌습니다. 예를 들어 `s.html`에 정의 목록을 넣으면 `dd`의 브라우저 기본 들여쓰기가 남습니다. 허용 콘텐츠 요소 전체를 초기화하거나, 허용하지 않는 태그를 빌드에서 차단해야 합니다. 목록 들여쓰기는 이후 명시적으로 적용하면 됩니다.

11. **RED — 넘침 검사와 실패 처리.** 검사에는 다음 누락이 있습니다.
   - 초기 상태에서는 답·풀이를 펼친 문제 상태를 검사하지 않습니다.
   - `#body`만 검사하므로 하단 버튼·힌트 및 내부에서 잘린 내용은 놓칠 수 있습니다.
   - `+1` 허용과 `jump` 제외는 지침의 전수 검사 조건과 다릅니다.
   - 실패를 화면·변수에 기록할 뿐, 빌드 실패로 연결하는 코드는 제공되지 않았습니다.

   미응답·답 공개·힌트 상태와 지원 화면 크기를 검사해야 합니다. 목록도 페이지로 나누고, 실패 시 빌드를 중단해야 합니다. 글자량 분할만으로 실제 배치 검사를 대체할 수 없습니다.

12. **GREEN — 애니메이션.** 제공된 코드에 장식 애니메이션과 전환 효과가 없습니다. 지연 호출은 그 자체로 시각적 애니메이션이 아닙니다.

13. 보류 — 브라우저 저장소. 요청대로 아토 결정 사항으로 분리합니다. `localStorage` 읽기·쓰기는 남아 있으며, 금지 조항을 충족한 상태는 아닙니다.

14. **RED — 원시 수식 노출.** `typeset()`은 원시 수식을 발견해도 `data-ready="true"`로 공개합니다. `throwOnError:false`와 예외 무시도 남았습니다. 구분자 없는 `\vec`는 검사 정규식에서 빠지고, 힌트는 아예 `typeset()`을 거치지 않습니다. 렌더 오류·미처리 수식 발견 시 공개를 막고, 힌트까지 동일한 검사 경로에 포함해야 합니다.

전체 판정: RED(7건)