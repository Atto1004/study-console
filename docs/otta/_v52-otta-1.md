**판정 — RED.** §C 1번은 GREEN, 2·3·4번은 RED입니다.

**근거**

1. **GREEN — 해석.** 회차별 개념→문제로 학교 진도를 따라잡는 해석은 요청과 부합합니다. 다만 “현재 수업 중 이해를 돕기”도 가능한 해석입니다. 현재 구현은 복습 중심이며, 아토가 이 정의를 확정했는지는 확인 못 함입니다.

2. **RED — 회차 연결.** 실제 9덱·40파트 모두 날짜가 있습니다. 일반물리학2 9/11은 두 파트에, 정역학 파트5는 9/16·21·23에 연결됩니다. 다대다 연결 자체는 됩니다. 그러나 정역학 9/21·23은 sessions.json에 없는데 연결되므로 실제 진도 일치는 확인 못 함입니다. `deck_index.py:33`은 날짜 없는 표지를 빈 배열로 처리하고 `2/31`도 허용합니다. 휴강은 V50에서 제외하지만 색인 생성 때 검증하지 않습니다. 수정: 표지 문구와 별개로 회차 날짜를 명시하고, 달력 유효성·수업일·취소 여부·누락을 검사하십시오. 여러 회차가 공유하는 파트는 회차별 학습 범위를 지정하십시오.

3. **RED — 상태.** V50 원문 함수를 메모리에서 재현했습니다. 정역학 9/9 두 파트 중 하나만 완료하면 50% ‘진행 중’으로 정상입니다. 그러나 100%+80%는 평균 90%라 ‘따라감’입니다(`v50-layer-src.js:52`). 전부 건너뛰어도, 개념 확인 후 문제를 전부 틀려도 100%입니다. `#quiz` 성적은 별도 저장키라 반영되지 않습니다. 오늘 회차도 밀림 집계에 포함됩니다(:67). 수정: 평균은 표시용으로 두고 모든 필수 파트의 기준 충족을 요구하십시오. `skip`을 제외하고 열람·정답·이해 상태를 분리하십시오. 오늘 수업과 밀림도 별도 집계하십시오.

4. **RED — 글래스.** V51 폴백(:65)에 `.ntv-h`, `.v48`이 빠졌습니다. `.card .card`는 선택자 우선순위 때문에 반투명 배경이 남습니다. 덱 템플릿(:310)의 `#anslb`, `#padPanel`도 빠졌습니다. 수정: 해당 표면을 불투명하게 덮고 중첩 카드의 블러를 해제하십시오. 다크 대비와 아이패드 지연은 실측 못 함입니다. 글자 토큰 유지로 대비가 보장되지는 않습니다. 최종 합성 배경에서 작은 글자 대비를 측정하고, 블러는 상위 패널 중심으로 제한하십시오.

**아토가 하실 일** — 없음. 아톰이 위 수정과 재현 결과를 제출하면 재검수합니다. index.html의 V50·V51은 원본과 일치합니다. 파일 변경·외부 전송 없음.

전체 판정 — **RED.** 완료 오판과 연결 검증·폴백 누락을 수정해야 합니다.