# 미감관리부 비상회의 — 디자인 브리프 v49 오타 판정 (2026-09-21) — 2차 RED(6) → 3차 RED(4) → 4차 GREEN

## 회차 
**판정 — RED.** 행 목록화는 타당합니다. 다만 실측 근거의 과장, 덱 설계 누락, 기존 렌더와의 충돌 위험이 남습니다. 아래 ‘닫힘’은 설계상 대응이며 구현 통과 판정은 아닙니다.

**근거**

1. §1 실측표: RED

- ‘색 1개’는 브랜드 강조색에 한정해야 합니다. MY247은 노랑 순위 강조·다색 시간표·다색 그래프를 사용합니다. 메가스터디에도 노랑 별과 여러 콘텐츠 색이 있습니다.
- ‘카드 안 카드 없음’은 관찰과 다릅니다. 메가 QUICK의 회색 패널 안 흰 통계 카드, MY247의 흰 패널 안 순위 강조 영역이 보입니다. 중첩 금지는 우리 앱의 선택으로 적어야 합니다.
- ‘하단 5탭 공통’은 성립하지 않습니다. 이투스 수강앱은 4개이고, MY247 첨부에서는 하단 5탭을 확인하지 못했습니다.
- 행 목록의 단위가 서로 다릅니다. 메가는 개별 강의, 이투스는 다운로드 강좌, MY247은 사람 순위입니다. 이를 모두 ‘과목 단위’의 근거로 묶으면 안 됩니다.
- ‘상단 숫자 3칸’도 공통 규칙이 아닙니다. 이투스의 원형 진도와 MY247의 순공 시간은 구조와 의미가 다릅니다.
- Pretendard, 본문 14px, 얼굴 40px, 정확한 색상값은 축소된 홍보 시트만으로 확정할 수 없습니다. ‘시각적 추정’과 ‘우리 앱 적용값’을 분리하십시오.
- 첨부 참고 시트는 설명 기준 총 42장입니다. ‘59장 실측’은 나머지 17장과 측정 방법을 확인 못 했습니다.

평면 중심 구성·행별 정보 정렬·작은 상태 표시는 참고할 만합니다. 이를 네 앱 모두의 동일한 규격으로 단정한 부분이 문제입니다.

2. 1차 RED 9건 대응

[1차 검수 원문](C:/Users/user/Desktop/아톰OS/기술실/study-console/docs/otta/_design-v48-otta-1.md)의 집계 기준은 ‘수정 우선순위 1~9’입니다.

| 기존 RED | 대응 | 판정과 남은 조건 |
|---|---|---|
| 1. 모바일 넘침 | 부분 · RED | 56px 헤더·5탭만으로 본문 넘침은 해결되지 않습니다. 7일 표시, 긴 제목·수식 줄바꿈, 하단 가림, 최소 터치 영역 기준이 빠졌습니다. |
| 2. 주행동 정리 | 부분 · RED | 이어서 학습 기록이 없는 상태의 주행동이 없습니다. 저장을 점 하나로 줄이면 미저장·저장 중·실패를 구분하지 못합니다. |
| 3. 공통 외형 통일 | 부분 · RED | 초록 단일 강조와 ‘과목색 칩·과목색 바’의 관계가 불명확합니다. 덱의 교육색과 정오답색도 적용 범위를 명시해야 합니다. |
| 4. 책 모형 축소 | 닫힘 · GREEN | 행 목록화로 사진 위 글자 중첩·책등·광택·큰 표지색 문제가 해소됩니다. 사진 완전 삭제가 필수 조건은 아닙니다. |
| 5. 글자 위계 복구 | 부분 · RED | 앱의 크기·굵기는 있으나 행간·제목 줄바꿈 기준이 없습니다. 덱 손글씨와 수식의 크기 비율도 빠졌습니다. |
| 6. 할 일 재배치 | 닫힘 · GREEN | 단일 목록·유형 필터·초기화 이동으로 기존 배치 문제에 대응합니다. |
| 7. 과목 화면 정렬 | 부분 · RED | 회차 행 방향은 맞습니다. 상단 공백의 원인 확인과 긴 회차 내용의 모바일 배치가 남습니다. |
| 8. 덱 내용·진행 재구성 | 부분 · RED | 진행바·테두리 제거는 대응합니다. 세로 중앙·2×2 고정은 긴 문제와 폰에서 다시 넘칠 수 있습니다. 필기·출처의 이동 위치도 누락됐습니다. |
| 9. 결과→복습 연결 | 부분 · RED | 복습 버튼만 있습니다. 정답·오답·미응답 구분, 통과 분모, 틀린 개념 목록, 전부 정답일 때 행동이 없습니다. |

즉시 채점은 아토 지시이므로 유지가 맞습니다. 중복 ‘다음’은 원문에 이미 제거됐다는 주석이 있어 신규 결함으로 재집계하지 않습니다.

3. 우리 앱으로의 적용: 부분 GREEN, 보완 RED

회차 정리·출석·자료·덱 진입을 한 행에 묶는 방향은 GREEN입니다. 다음 요소를 그대로 가져오는 것은 RED입니다.

- 강의 시청률을 학습 이해도로 옮기면 안 됩니다. 자료 충족률·덱 열람 진도·문제 정답률은 별도 지표입니다. `12%`, `4/9`마다 분모와 집계 대상을 정의하십시오.
- 순공 시간은 1인용 앱의 대표 성과로 자동 채택할 이유가 없습니다. 현 코드에는 평시 타이머를 숨기는 V39 정책도 있습니다. ‘오늘 학습 0분’은 실제 측정 여부부터 확인해야 합니다.
- 덱은 영상이 아닙니다. 상단 콘텐츠를 영상처럼 고정하면 풀이 공간이 줄어듭니다. 본문을 중심에 두고 목차·내 답·출처는 필요할 때 여는 구조를 권고합니다.
- 달력·학점을 숨기는 것만으로 탐색 설계가 끝나지 않습니다. ‘과목/설정’과 ‘과목 안 세그먼트’로 목적지가 서로 다릅니다. 하나로 확정하고 오늘 화면에서도 일정으로 연결하십시오.
- 즐겨찾기를 스와이프·길게 누름으로만 제공하지 마십시오. PC·키보드에서도 보이는 더보기 경로가 필요합니다.
- 아톰 연동의 자료 출처·반영 시각·동기화 실패 표시가 없습니다. 저장 아이콘의 점이 이 역할까지 대신할 수는 없습니다.

4. 구현 방식과 작업 순서: RED

기존 코드는 단순 CSS 누적이 아닙니다. 함수 교체와 DOM 재생성이 함께 있습니다.

- [index.html:3776](C:/Users/user/Desktop/아톰OS/기술실/study-console/index.html:3776): 책장 생성 뒤 `.book`에 열기·길게 누르기를 연결합니다. 새 행으로 교체하면 해당 연결을 이관해야 합니다. 즐겨찾기·숨김·개인 공부 분기도 보존 대상입니다.
- [index.html:9451](C:/Users/user/Desktop/아톰OS/기술실/study-console/index.html:9451): V43은 `#dayBooks`를 기준으로 이어서 학습 카드를 삽입합니다. 기준 노드를 없애면 뒤에 붙습니다. 덱 닫기·저장소 변경 때도 다시 렌더합니다. 최초 화면만 재배치하면 재발할 수 있습니다.
- [index.html:9194](C:/Users/user/Desktop/아톰OS/기술실/study-console/index.html:9194): V37은 과목 렌더 후 기존 회차 표를 다시 만듭니다. 새 행과 구 표의 렌더 책임을 분리해야 합니다.
- [index.html:9281](C:/Users/user/Desktop/아톰OS/기술실/study-console/index.html:9281): V38은 특정 HTML 문자열을 치환해 ‘학습 준비 중’을 만듭니다. 마크업 변경 시 치환이 실패할 수 있습니다. 덱 유무를 데이터로 판정해야 합니다.

따라서 V44는 범위를 제한한 스타일 전환에는 쓸 수 있습니다. 기존 렌더를 실행한 뒤 새 DOM으로 덮는 방식 전체에는 GREEN을 줄 수 없습니다. 기존 선택자·인라인 스타일·동적 스타일의 우선순위도 확인해야 합니다.

권고 순서는 다음과 같습니다.

1. 지표 정의·색 역할·탭 목적지·상태별 행동을 먼저 확정합니다.
2. .49에서 셸·책장·오늘의 렌더 책임과 이벤트를 함께 교체합니다. V43 재진입까지 검증합니다.
3. .50에서 회차·할 일·덱을 변경합니다. 데이터 이관 코드와 표시 코드는 분리합니다.
4. 각 단계에서 390px·태블릿·PC, 밝은/어두운 화면을 확인합니다. 과목 열기, 즐겨찾기, 개인 공부, 회차별 덱 진입, 준비 중 상태, 이어보기, 저장 실패, 채점·재시도·복습을 회귀 검사합니다.

현재는 정적 코드 대조만 했습니다. 스모크 검사는 실행하지 않았습니다.

**아토가 하실 일**

추가 선택 전에 아톰이 브리프를 수정해야 합니다. 수정 지시는 아래 6건으로 집계합니다.

1. 실측표의 잘못된 공통화와 미확인 수치를 정정합니다.
2. 모바일·터치·글자·긴 콘텐츠의 합격 기준을 적습니다.
3. 색 역할·지표 분모·저장 및 연동 상태를 정의합니다.
4. 덱의 가변 배치·풀이·필기·결과·복습 상태를 완성합니다.
5. 탭 목적지와 숨겨진 기능의 접근 경로를 확정합니다.
6. V32~V43의 유지·대체 범위와 회귀 검사 항목을 명시합니다.

파일 수정 없음. 구현 완료 여부는 확인 못 했습니다.

전체 판정: RED(6건)
64,886

## 회차 _2
**판정 — RED.** 6항목 중 2항목은 닫혔고, 4항목은 보완이 필요합니다. 건수는 기존 항목 단위입니다.

1. **GREEN** — 실측 과장 정정. 추정·관찰·설계 선택을 구분했습니다. 다만 참고 이미지 42장의 원본과 수량은 이번에 확인하지 못했습니다.

2. **RED** — 합격 기준의 검사식이 잘못됐습니다. `document.scrollWidth`는 측정 대상이 아니어서 정상 화면도 비교식이 실패합니다. `document.documentElement.scrollWidth`로 수정해야 합니다. `/[A-Z]{4,}/`도 실제 과목명 `CADD`를 잡습니다(index.html:2295). 검사 대상을 UI 라벨로 한정하고 콘텐츠를 제외해야 닫힙니다.

3. **RED** — 색·지표·저장 정의에 모순이 남았습니다.
   - 기존 `typeA`에는 파랑이 있습니다(index.html:389). 이를 칩에 재사용하면서 “파랑은 덱 본문만”이라고 하면 충돌합니다. 과목색 예외 또는 대체색을 정해야 합니다.
   - `mustHTML`은 `have`뿐 아니라 `pc·linked`도 충족으로 셉니다(index.html:8375~8378). 집계할 회차 범위와 미확인 자료 처리도 명시해야 합니다.
   - `markSave`는 `dirty` 두 상태만 구분합니다. 실패해도 같은 함수를 호출합니다(index.html:3106~3109, 7036). 실패 상태와 재시도 전이를 추가해야 하므로 “라벨만”으로는 안 됩니다.

4. **RED** — 덱 상태는 보강됐지만 배치 보장이 틀렸습니다. `auto-fit,minmax(240px,1fr)`은 가용 폭에 따라 3~4열도 됩니다. “1024px에서 2열”을 보장하려면 명시적인 2열 규칙이 필요합니다. 또한 ‘오답 0·미풀이 있음’에서 주버튼이 미정입니다. 미풀이·오답·전부 정답의 버튼 우선순위를 정해야 닫힙니다.

5. **GREEN** — 탭 목적지와 숨은 기능 경로는 정의됐습니다. 달력·학점·개인 공부·메뉴 접근 경로가 있습니다. 실제 작동 통과 판정은 구현 후 대상입니다.

6. **RED** — V38 수정 유예는 타당하지만 “세 군데만 변경”은 성립하지 않습니다. 저장 실패 상태, 탭 이동·해시, V43의 `RESUME` 제거는 별도 변경 대상입니다(index.html:9442). 또한 V43은 `#dayBooks`뿐 아니라 `.closest(".card")`와 부모 구조에 의존합니다(9455). 변경 허용 목록과 해당 DOM 구조·래핑 호출 보존을 명시해야 닫힙니다.

파일 변경 없음. 화면 실행 검사는 수행하지 않았습니다. 아토 결정 4건은 별도로 남아 있습니다.

전체 판정: RED(4건)
36,725

## 회차 _3
**GREEN.** r3 문서 기준으로 기존 RED 4건이 닫혔습니다. 구현 완료 판정은 아닙니다.

- (2) 넘침 검사를 `document.documentElement.scrollWidth`로 수정했습니다. 라벨 검사 대상에서 과목명·콘텐츠를 제외했습니다.
- (3) 과목 팔레트를 분리했습니다. `mustHTML` 집계 범위와 저장 3상태·기존 호출 호환·실패 후 재시도를 명시했습니다.
- (4) 화면 폭에 따른 2열·1열을 고정했습니다. 오답·미풀이·전체 정답의 주버튼 우선순위가 분명합니다.
- (6) 변경 허용 목록 6개를 명시했습니다. V43·V25의 삽입 기준 DOM과 래핑 호출 체인 보존을 지정했습니다.

새로 확정할 결함은 없습니다. 실제 코드·회귀 검사 결과는 확인 못 했습니다.
아토 컨펌 필요: §6의 결정 4개입니다.

전체 판정: GREEN
18,818

