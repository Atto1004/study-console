# 미감관리부 비상회의 — 학습앱·덱 디자인 오타 검수 1차 (2026-09-21, 아토 "디자인이 개망")

캡처 9장(`~/.claude/scratch/sc_test/_design/`) + DESIGN.md·아토 규칙·학습게임 지침을 기준으로 검수. 아톰 주석: 8번 캡처의 「다음」 중복은 .48 커밋 전 이미 제거됨 · 「보기 누르면 바로 채점」은 아토 지시(2026-09-21)라 오타 이견은 기록만.

**판정 — RED.** 핵심은 장식의 양보다 규칙의 충돌입니다. 앱은 책장 모형·관리 화면·메신저 양식이 섞였고, 덱은 학습 내용보다 테두리와 조작부가 강합니다. 모바일에는 실제 잘림도 보입니다.

판정 근거는 첨부 9장과 [A]~[C]입니다. 코드·실제 클릭 영역·계산된 글꼴은 확인 못 했습니다. 아래 선택자는 구현에 사용할 제안 이름이며, 기존 코드에서 확인한 이름은 아닙니다.

**근거 — 캡처별 상위 3개**

| 캡처 | 어느 요소가 문제인가 | 못생겨 보이는 이유·위반 기준 |
|---|---|---|
| **1 책장** | ① 표지 위 제목·별·배지·학점·진행률 | 사진의 글자와 UI 글자가 겹칩니다. 작은 영역에서 정보끼리 경쟁합니다. [A]의 본문 중심 위계 위반입니다. |
| | ② 책등·접힌 모서리·강한 그림자·입체 표지 | 콘텐츠 이미지 주변에 UI 장식까지 중첩됩니다. [A]의 평면·1px 경계 원칙과 충돌합니다. **표지 사진 자체가 금지인 것은 아닙니다.** |
| | ③ 노랑·파랑 표지와 여러 색 배지 | 과목 구분·상태·행동이 모두 색을 요구합니다. 특히 생성한 파랑 표지는 [B]와 충돌합니다. |
| **2 오늘** | ① 수업 카드 2개가 넓은 패널 왼쪽에만 배치 | 오른쪽 공간은 비지만 제목은 말줄임됩니다. 공간이 부족한 것이 아니라 배분이 잘못됐습니다. |
| | ② 노랑·파랑 입체 책과 초록 외곽선 | 수업 정보보다 표지 장식이 먼저 보입니다. [A]의 평면 경계와 [B]의 파랑 금지 위반입니다. |
| | ③ 저장·이어서 학습·할 일 열기의 초록 버튼 | 모두 주행동처럼 보입니다. [A]의 화면당 주버튼 하나 원칙을 깨고 다음 행동을 흐립니다. |
| **3 과목** | ① 화면 위 큰 공백과 보이지 않는 과목 제목 | 현재 과목과 화면의 시작점을 파악하기 어렵습니다. 다만 공백의 원인이 레이아웃인지 캡처 과정인지는 확인 못 했습니다. |
| | ② 주차·요일·진도·과제·정리의 촘촘한 반복 | 반복 라벨과 긴 내용이 비슷한 크기로 나열됩니다. 행 간격보다 정보 위계가 먼저 부족합니다. |
| | ③ 결석 카드 전체 빨간 테두리와 반복 초록 버튼 | 출결 상태가 카드 전체를 지배하고 모든 회차가 행동을 요구합니다. [A]의 경계·행동색 규칙 위반입니다. |
| **4 할 일** | ① 화면 상단 초기화 경고와 ‘실행’ 버튼 | 일상 업무보다 관리 기능이 먼저 보입니다. 파괴 가능성이 있는 작업에 가장 강한 위치를 줬습니다. |
| | ② 17개·5개·0개를 같은 폭의 3열로 배치 | 가장 긴 열만 아래로 늘어지고 빈 열이 공간을 차지합니다. 실제 데이터 밀도와 구조가 맞지 않습니다. |
| | ③ 여러 줄 제목·빨간 기한·출처·삭제 기호 | 한 항목 안에서 강조점이 너무 많습니다. 반복되는 빨강은 긴급도를 구분하지 못하고 화면 전체를 경고처럼 만듭니다. |
| **5 모바일 오늘** | ① 오른쪽 날짜·본문·버튼·하단 내비 잘림 | 390px 화면 안에 필수 정보가 들어오지 않습니다. 반응형 배치 실패입니다. |
| | ② 상단 정보와 주 이동부가 차지하는 높이 | 수업 패널은 CSS 좌표 기준 약 310px 아래에서 시작합니다. 반복 정보가 첫 화면을 차지합니다. [A]의 60px 모바일 헤더 기준과 차이가 큽니다. |
| | ③ 과목명은 잘리고 긴 학습 문장은 넘침 | 중요한 짧은 제목에는 폭을 제한하고 긴 본문에는 줄바꿈을 확보하지 못했습니다. 하단 탭도 [A]의 5개 구성이 아니라 6개 이상 보입니다. |
| **6 덱 개념** | ① 설명마다 큰 파란 사각 테두리 | 개념 강조색이 구조 전체의 경계색이 됐습니다. 무엇을 기억해야 하는지보다 상자 세 개가 먼저 보입니다. |
| | ② 작은 손글씨 설명과 큰 수식의 대비 | 긴 설명의 판독 부담이 큽니다. 손글씨 자체보다 본문 크기·행간·수식과의 비율이 문제입니다. |
| | ③ 상단 원형 점들과 하단 썸네일·긴 확인 문구 | 진행·참고·이해 확인·이동이 서로 다른 방식으로 표현됩니다. [C]의 진행바와 간결한 하단 조작 구조에 못 미칩니다. |
| **7 덱 문제** | ① 상단에 몰린 문제·선택지와 큰 빈 중앙 | 여백은 많지만 문제를 읽는 영역은 좁습니다. 문제·수식·선택지에 여백을 재배분해야 합니다. |
| | ② ‘보기를 누르면 바로 채점’ | 선택과 제출이 합쳐져 있습니다. [C]의 ‘확인/다음’ 흐름과 다르며, 오선택을 검토할 단계가 없습니다. |
| | ③ 점선 다음·실선 이전·비활성 힌트·필기·썸네일 | 같은 조작부에 서로 다른 버튼 문법이 섞였습니다. 선 모양만으로 기능 차이를 해석해야 합니다. |
| **8 덱 정답** | ① 하단 ‘다음’ 버튼 두 개와 ‘완료’ 문구 | 다음 행동의 목적지가 중복됩니다. 둘이 같은 동작인지 캡처만으로 구별되지 않습니다. |
| | ② 선택지의 초록 강조와 하단 별도 ‘정답’ 상자 | 같은 결과를 분산 표시합니다. 정답 근거보다 상태 표시가 공간과 주의를 차지합니다. |
| | ③ ‘답·풀이 다시 보기’와 ‘다시 풀기’의 분리 배치 | 결과 확인·해설·재도전의 관계가 흐립니다. 같은 단계의 행동을 한곳에 묶어야 합니다. |
| **9 덱 결과** | ① 굵은 검정 점수 상자 두 개 | 점수는 강하지만 다음에 복습할 내용은 약합니다. 결과 화면의 중심이 회복 행동보다 숫자에 머뭅니다. |
| | ② ‘기초 전부 + 응용 2/3 이상’과 0/2 표시 | 기준 문구와 분모의 관계가 모호합니다. ‘응용 정답률 ⅔ 이상’인지 다른 기준인지 명시해야 합니다. |
| | ③ ‘이 파트 처음으로·확인·다음’ 병렬 | 미달 상태에서 권장 행동이 없습니다. ‘틀린 문제 복습’을 주행동으로 두고 이동은 보조로 내려야 합니다. |

모바일 이미지는 2배 캡처입니다. 이미지상의 글자 크기를 그대로 CSS 크기로 판정하지 않았습니다. 또한 화면에 보이는 별·기호만으로 이모지 구현이라고 단정하지 않습니다.

**근거 — 전체를 관통하는 원인 5개**

1. **기준 문서부터 충돌합니다.** [A] 행동색은 `#15803D`, [B]는 알파인 그린입니다. [A/B]는 파랑 금지지만 [C]는 파랑 개념색을 요구합니다. 적용 범위를 정하지 않으면 수정 때마다 색이 되돌아갑니다.
2. **시각 언어가 세 종류입니다.** 입체 책장, 테두리 중심 관리 화면, 손글씨 학습지가 연결 규칙 없이 이어집니다. 앱과 덱의 본문 서체는 달라도 버튼·내비·진행 표시는 공통이어야 합니다.
3. **중요도를 장식으로 해결합니다.** 색·배지·테두리·그림자를 계속 더해 모두 중요해졌습니다. 제목, 본문, 보조정보의 크기와 위치부터 분리해야 합니다.
4. **공간 배분이 콘텐츠를 따르지 않습니다.** 빈 패널 옆에서 제목이 잘리고, 빈 할 일 열 옆에서 과제 목록이 길어집니다. 모바일에서는 같은 문제가 넘침으로 드러납니다.
5. **행동 단계가 정리되지 않았습니다.** 저장·학습·초기화가 경쟁하고, 덱에는 선택·채점·확인·다음이 중복됩니다. 버튼 미감보다 상태별 행동 설계를 먼저 고쳐야 합니다.

“CSS를 세 겹 덮어썼다”, “폰트가 로딩되지 않았다”는 코드 확인 전에는 원인으로 확정할 수 없습니다.

**근거 — 수정 우선순위**

작업 크기는 상대 추정입니다. 소는 국소 스타일, 중은 컴포넌트·배치 변경, 대는 상태 흐름 변경입니다.

| 순위 | 구체적 수정안 | 크기 |
|---|---|---|
| **1. 모바일 넘침 제거** | `.study-shell, .page, .panel`에 `min-width:0; max-width:100%; box-sizing:border-box`. 그리드 열은 `minmax(0,1fr)`. `.resume-title`은 `white-space:normal; overflow-wrap:anywhere`. `.week-strip`은 7열 균등 배치. 하단은 5개 항목과 ‘더보기’ 구조로 정리하고 본문에 `padding-bottom:calc(86px + env(safe-area-inset-bottom))`. **`overflow-x:hidden`으로 가리는 방식은 제외합니다.** | 중 |
| **2. 주행동 정리** | `.page-primary-action`은 화면당 하나. 오늘은 ‘이어서 학습하기’. 저장은 보조 버튼과 변경 상태 문구로 분리합니다. 덱은 ‘선택 → 확인 → 결과 → 다음’으로 만들고 `.deck-next`를 하나만 둡니다. | 대 |
| **3. 공통 외형 통일** | `.study-shell` 아래 공통 토큰을 두고 버튼 8px, 경계 `1px solid #DCE5DF`, 일반 카드 `box-shadow:none`. ‘RESUME·STATUS·CONDITION·MENU’는 삭제하거나 한국어로 통일합니다. 색의 적용 범위는 아래 아토 결정 사항으로 분리합니다. | 중 |
| **4. 책 모형 축소** | `.course-card`의 책등·접힘·광택을 제거합니다. `.course-cover`는 보조 썸네일로 두고 제목·학점·상태는 이미지 밖에 배치합니다. 오늘 수업은 `시간 / 과목명 / 장소`의 행 구조로 변경합니다. | 중 |
| **5. 글자 위계 복구** | 앱은 Pretendard, `12/16·14/18·16/20·20/25px`, 굵기 400/600으로 통일합니다. 필수 설명은 `#6B7A70`, 본문은 `#0B1A11`. `.course-name`은 2줄까지 허용하고 출처·시간만 12px로 내립니다. | 중 |
| **6. 할 일 재배치** | `.todo-board`를 기본 단일 목록으로 바꾸고 상단 필터로 유형을 전환합니다. 행은 `체크 / 제목+보조정보 / 기한 / 더보기`. `.reset-notice`는 관리 메뉴로 이동하고, 실행 직전에만 영향 개수를 표시합니다. | 중 |
| **7. 과목 화면 정렬** | `.course-header`에 과목명과 주차를 고정된 문서 순서로 둡니다. `.session-list`는 주차 아래 날짜별 행으로 구성합니다. 출결은 상태 라벨 하나로 표시하고 카드 전체 빨간 테두리를 제거합니다. 상단 공백은 발생 조건 확인 후 수선합니다. | 중 |
| **8. 덱 내용·진행 재구성** | `.deck-progress`는 연속 막대와 `현재/전체` 텍스트. `.deck-content`는 좌우 32px, 블록 간 24px를 기준으로 재배치합니다. 설명 상자는 제거하고 의미 있는 문구에만 5색을 사용합니다. 서체 크기는 제목 28px·본문 22px·보조 18px의 3단계로 시험합니다. | 중 |
| **9. 결과 화면을 복습으로 연결** | `.result-summary`에 정답 수·미응답 수·통과 기준을 구분합니다. 아래에는 틀린 개념 목록과 ‘틀린 문제 복습’ 주버튼을 둡니다. ‘처음부터’와 ‘계속 이동’은 보조 행동으로 내립니다. | 중 |

터치 영역은 44px 이상으로 확보해야 합니다. [A]의 32px 버튼 외형과 터치 영역은 분리해서 적용할 수 있습니다.

**근거 — 덱 [C] 별도 판정**

| [C] 요구 | 판정 |
|---|---|
| 16:10 가로 | **충족.** 첨부 6~9는 1024×640입니다. |
| 손글씨 본문 + 정자 수식 | **외형상 충족.** 실제 Nanum Pen Script·KaTeX 사용 여부는 확인 못 했습니다. |
| 고정 5색과 의미 | 노랑 강조·파랑 개념·초록 정답이 보입니다. 정확한 색상값은 확인 못 했습니다. 초록 ‘정답’은 지침의 ‘예제’와 의미가 다릅니다. |
| 글자 크기 3단계 | **확인 못 함.** 시각적으로 크기와 수식 비율이 불균일하며 계산된 스타일 검사가 필요합니다. |
| 상단 진행바 | **불충족.** 원형 점과 페이지 수가 있으며 연속 진행바는 없습니다. |
| 하단 힌트·확인/다음 | **불충족.** 즉시 채점, 중복 다음, 결과 화면의 모호한 확인이 섞였습니다. |

손글씨 자체를 실패 원인으로 판정하지 않습니다. 현재는 **긴 문장까지 작은 손글씨로 쓰고, 조작부에도 같은 질감을 적용한 것**이 판독성과 정돈감을 낮춥니다.

지침 자체 재검토 권고는 별개입니다. 학습 본문의 손글씨는 유지하되, 버튼·진행·점수는 Pretendard로 통일하는 안을 권고합니다. 본문까지 바꾸는 것은 [C] 변경이므로 아토 결정이 필요합니다.

**전체 판정: RED(핵심 9건).** 위 우선순위 1~9를 집계했습니다. 캡처별 27개 관찰에는 같은 원인의 반복이 포함됩니다.

- **1차 빌드:** 1~5. 모바일 잘림, 행동 중복, 입체 장식, 글자 위계부터 고칩니다. 덱의 중복 ‘다음’도 포함합니다.
- **2차 빌드:** 6~9. 할 일·회차 구조와 덱 진행·복습 흐름을 재구성합니다.
- **아토 결정 필요:** 알파인 그린 두 색의 역할, 덱 본문에 한한 파랑 예외, 덱 조작부의 서체 분리입니다. 제 권고는 **앱 행동색은 [B], 덱 교육 내용은 [C], 공통 조작부는 [A]의 구조**입니다. 학습 화면에 DM의 397px 대화 목록까지 복제할 필요는 없습니다.

상태: 캡처 검수 완료. 파일 수정 없음. 실제 동작·스타일 수치는 확인 못 했습니다.
