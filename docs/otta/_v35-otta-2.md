**RED.** 비정상 기존 값의 처리에 결함이 남았습니다.

1. `exam={}`이면 병합을 건너뛰고 완료 플래그를 저장합니다. 이후 `exam=[]`로 복구해도 시드가 재실행되지 않습니다. 미병합 항목이 있으면 완료 처리를 보류해야 합니다.
2. `exam=[null]`이면 `y.quote`에서, `summary=123`이면 `.trim()`에서 예외가 납니다. 앞부분만 메모리에 반영된 채 중단됩니다. 변경 전에 기존 필드·배열 원소의 자료형을 검증해야 합니다.