# 학습 질문 패널(v50) 오타 설계 회의 (2026-09-21) — 1차 RED(5) → 2차 RED(3) → 3차 RED(1) → 4차 GREEN · 구현 검수는 Codex 한도(16:53)로 보류

## 1차
**판정 — RED.** 기존 API로 구현은 가능합니다. 다만 브리프의 5개 항목에 수정이 필요합니다. 파일 수정·실제 전송은 하지 않았습니다.

1. **RED — API 연결은 가능하지만, 최대 4턴이라는 전제가 틀렸습니다.**
   - 같은 출처의 쿠키 인증으로 호출할 수 있습니다. 다만 `ATOM_HOSTED`는 인증 여부가 아닙니다. 목록·전송의 401과 일반 HTTP 오류를 처리하고, SSE 응답 형식을 확인해야 합니다.
   - GREEN 뒤 아톰을 추가 호출하므로 실제 최대 **5턴**입니다. 오타는 호출당 최대 600초이며, 두 번 호출되면 오타 대기만 1,200초에 접근합니다. 여기에 아톰 시간이 더해집니다. [서버 근거](C:/Users/user/본사/atom/server.py:4060)
   - `done`은 **턴 종료**입니다. 첫 `done`에서 입력 잠금을 풀면 안 됩니다. 빈 `done {}`는 잠금 거절 뒤에도 옵니다. [잠금 처리](C:/Users/user/본사/atom/server.py:4090)
   - 수정: 턴별 말풍선과 요청 전체 상태를 분리하십시오. 600초를 전체 제한으로 쓰지 마십시오. 현재 `/api/send`에는 주기적 연결 유지 신호가 없으므로 실제 접속 경로의 장시간 무응답도 검증해야 합니다.
   - 기존 그룹의 기록·아톰 세션·잠금을 공유합니다. `client`는 대화 격리 수단이 아닙니다. 패널에 공유 사실을 표시하고, 다른 창의 전송과 기록 갱신을 처리하십시오. 잠금 거절 시 초안을 보존하고 자동 재전송하지 마십시오.

2. **RED — 문맥의 출처·크기·유효기간이 빠졌습니다.**
   - `e.source`만으로는 부족합니다. 같은 iframe이 다른 출처로 이동해도 창 참조는 유지될 수 있습니다. `e.origin===location.origin`, 메시지 유형·필드 자료형·현재 덱 식별자를 함께 확인하십시오.
   - 문제 본문 600자만 제한하면 보기·제목·답변으로 크기가 커집니다. 수신 측에서도 필드별 길이, 보기 개수, 전체 문맥 상한을 적용하십시오.
   - 덱 A→B 이동, 초기 메시지 수신 누락, `#check` 진입 시 A의 문맥이 남을 수 있습니다. 덱 이동·닫기·검사 모드에서 문맥을 즉시 비우고, 로드 세대 식별자와 현재 문맥 요청/응답을 추가하십시오. 패널을 열기 전부터 수신해야 합니다.
   - 전송 순간의 문맥을 복사해 고정하십시오. `revealed=false`이면 정답·정오 판정을 전송하지 마십시오. 미응답도 오답과 구분해야 합니다. 600자 절단은 표시하고, 누락된 수식·도표를 추측하지 않도록 명시하십시오.
   - 템플릿 변경만으로 기존 덱이 갱신되지는 않습니다. 기존 덱 반영 범위와 문맥 미지원 덱의 표시도 정하십시오.

3. **RED — 역할 요청은 가능하지만, 검증 범위 보장이 없습니다.**
   - 학생 눈높이 설명과 수학 검증 요청 자체는 서버 흐름과 양립합니다. 그러나 오타 프롬프트는 서버가 만들며, `AGENTS.md`의 판정 형식을 요구합니다. 프런트 문구가 이를 대체하지 않습니다.
   - 서버는 최근 8개 메시지를 **각 1,500자**로 자릅니다. 아톰 답의 1,501번째 문자 이후에 오류가 있으면 오타에게 전달되지 않습니다. 첫 검증에는 질문 전문이 별도로 들어가지만, 최종 검증에는 같은 보장이 없습니다. [프롬프트 근거](C:/Users/user/본사/atom/server.py:2828)
   - 수정: 역할 요청을 앞에 두고, 문맥은 명확히 구분한 참고 자료로 넣으십시오. “이번 요청은 학습 설명·검증이며 후속 실행 요청은 없음”을 명시하십시오. 판정 형식은 유지해야 합니다.
   - 아톰 설명·수정 답을 1,500자 이내로 요청하되, 초과하면 화면에 **전체 답변 검증을 보장할 수 없음**을 표시하십시오. 서버 수정 없이 긴 답 전체의 검증을 보장한다는 주장은 삭제해야 합니다.

4. **RED — 배치 방향은 타당하지만, 종료·복구 상태가 없습니다.**
   - `준비 → 전송 중 → 턴 진행 → 종료`와 `인증 필요·잠금 거절·연결 불명·오류`를 구분하십시오. `status`와 `turn.role`로 현재 화자를 표시해야 합니다.
   - 서버는 연결 쓰기 오류를 삼키고 작업을 계속할 수 있습니다. 패널 닫기·요청 중단은 서버 취소가 아닙니다. [연결 처리](C:/Users/user/본사/atom/server.py:4001)
   - 수정: 패널·덱을 닫아도 진행 중 요청 상태를 유지하십시오. 다시 열면 기록을 대조하고, 연결 끊김은 완료로 표시하지 마십시오. 자동 재전송은 중복 기록을 만들 수 있습니다. 기존 API만으로 끊긴 요청의 완료 여부를 확정할 수 없는 경우는 명시해야 합니다.
   - 노트·studyunit에도 진입 버튼이 어디에 있는지 정하십시오. 모바일은 가상 키보드·안전 영역을 반영하고, 한글 조합 중 Enter 전송을 막으십시오. 문맥과 간이 마크다운은 HTML을 이스케이프해야 합니다.

5. **RED — 정상 흐름 중심의 검사만으로는 부족합니다.**
   - API: 401·404·비SSE 응답, 그룹 없음, 다른 창의 잠금 점유, `error → done {}`, 3턴·4턴·5턴 흐름을 추가하십시오.
   - SSE: UTF-8 문자와 이벤트 경계의 청크 분할, 여러 이벤트 동시 수신, 장시간 무응답, 중간 연결 종료, 오타 실패가 `done.text`로 오는 경우를 검사하십시오.
   - 문맥: 잘못된 origin/source, 과대 메시지, A→B의 늦은 메시지, 초기 수신 누락, `#check`, 정답 공개 전후를 검사하십시오.
   - UI: 답변 중 닫기·재열기·덱 전환, 기록 로드와 새 답변의 경합, 한글 조합, 모바일 키보드, HTML 삽입을 추가하십시오.
   - `/api/health`는 비인증 상태에서도 200입니다. `authed`와 실제 목록 접근을 확인해야 합니다. 실전송 1회는 접속 확인용이며, 나머지 분기 검사를 대신하지 못합니다.

전체 판정: RED(5건)
45,018

## 2차
**판정 — RED.** r2에서 남은 결함은 3건입니다.

- 상태·복구: 정상 완료와 조기 EOF를 구분하지 못합니다. 턴 사이 연결이 오류 없이 닫히면 `reader done`이 되어 완료로 처리됩니다. 전체 완료 신호가 없으면 기록 재조회로 완료를 확인하고, 그전에는 미확정 상태로 두어야 합니다.
- 문맥 세대: `gen`을 증가시키지만 메시지에 담거나 비교하지 않습니다. 같은 덱을 다시 열면 이전 메시지도 `deck`·`source` 검사를 통과할 수 있습니다. 열기마다 발급한 식별자를 덱이 회신하고 앱이 대조해야 합니다.
- 방송 중복: 자체 스트림 종료 직후 늦게 도착한 watch의 `token/done`은 다시 반영됩니다. 요청 진행 여부만으로는 중복을 막지 못합니다. 요청·턴 식별자와 이벤트 순번으로 두 경로를 중복 제거해야 합니다.

프롬프트 역할·절단 고지, 모바일·이스케이프 설계는 수용합니다. 검증 계획에는 위 3개 재현 조건을 추가해야 합니다. 실제 코드·시험 결과는 이번 판정 대상이 아닙니다.

전체 판정: RED(3건)
15,095

## 3차
**판정 — RED.** 3건 중 완료 구분 1건이 남았습니다.
- 조기 EOF 직후 GET이 성공해도 전체 완료는 증명되지 않습니다. 서버가 계속 생성 중이면 저장된 중간 턴만 반환할 수 있습니다.
- 재현: 첫 턴 저장 → 다음 턴 생성 중 연결 종료 → GET 성공 → 중간 기록으로 교체 → `closed(확정)` 오표시입니다.
- 수선 조건: 요청과 연결된 서버 종료 상태를 확인해야 합니다. 서버 변경이 없다면 「기록 동기화됨 · 완료 미확정」으로 유지해야 합니다.
- gen 왕복은 설계상 닫혔습니다. 발급·응답·수신 비교로 이전 세대 메시지를 거부합니다.
- watch 중복은 설계상 닫혔습니다. 방송 직접 렌더를 없애고 기록 id로 중복을 제거합니다. 추가 검증의 실행 결과는 확인 못 함.

전체 판정: RED(1건)
17,145

## 4차
**판정 — GREEN.** 3차 RED 1건은 설계상 닫혔습니다.

**근거**
- EOF + GET 성공에도 「기록 동기화됨 · 완료 미확정」을 유지합니다.
- 조기 EOF 이후 새 턴은 watch `turn/done` 재조회로 반영합니다.
- 완료 표시는 .51의 요청 id·`flow_end` 도입 뒤로 제한했습니다.
- 마지막 항목이 앞선 `closed(확정)` 문구를 명시적으로 폐기합니다. 구현 여부는 이번 판정 범위 밖입니다.

전체 판정: GREEN
20,322

