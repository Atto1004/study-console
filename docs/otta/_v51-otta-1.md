**판정 — RED.** 범위 집계와 객관식 선택지를 고쳐야 합니다. 계산 16문항의 표시 답·해설 결과는 맞지만, 일부 객관식은 정답이 유일하지 않습니다.

**근거**

안건 1 — 브리프 §3의 네 항목입니다.

1. 데이터 흐름: **RED**. 덱 9개의 파일 존재·문항 수·슬라이드 수는 색인과 일치합니다. 상대경로 fetch는 정적 배포에 맞습니다. 다만 `index.html:7561`의 `examWeeks({scope:"Ch.3~5",kind:"중간"},4)`는 `[1,2,3,4]`를 반환합니다. V49는 이를 사용해 오리엔테이션·Ch.2까지 ‘범위’ 진도와 이해도에 포함합니다(`10241`, `10246`). 단원별 범위 매핑을 적용하거나 ‘현재까지 진도’로 표시해야 합니다. 실제 GitHub Pages 배포 상태는 확인 못 함.
2. 저장 분리: **GREEN**. `slides_tpl.html:336~355`를 메모리에서 실행했습니다. 문제 모드는 시작·문제·결과만 남기고 `mc-slides-test-quiz`만 기록합니다. V43·V44가 읽는 학습 키와 마지막 위치 키를 건드리지 않습니다.
3. 문제 검산: **RED**. 아래 미적분 문항에 동치 선택지가 있어 단일정답 조건을 위반합니다.
4. D-14 결정과 관계: **GREEN**. V49는 별도 허브이며 시간 배분·학습 큐를 변경하지 않습니다. 읽은 학습시스템 지침 §17도 이 구분을 명시합니다.

안건 2 — 과목별 기초 3개·응용 1개를 직접 계산했습니다. 기초의 `data-ok="1"`과 16개 해설을 대조했습니다.

- 정역학: 기초 1-4 = `(28.6,42.9,85.7) N`, 2-1 = `12`, 3-3 = `(-3,6,-3)`. 응용 1-1 = 크기 `3`, 단위벡터 `(2/3,-2/3,1/3)`, 방향각 `48.2°·131.8°·70.5°`. 모두 일치합니다.
- 일반물리학2: 기초 1-1 = 전자 2개 잉여, 1-2 = `0.009 N` 척력, 5-2 = `8.85×10⁻¹¹ F`. 응용 1-2 = `2.2705×10³⁹`. 모두 일치합니다.
- 공업수학1: 기초 3-4 = `3/2+c/x²`, 5-3의 근 = `−0.2±3i`, 6-2의 근 = `2,3`. 응용 3-1 = `3cos x−2cos²x`; 원식과 초기값을 만족합니다.
- 미적분학2: 기초 1-1 = `3=√9`, 1-2 = 중심 `(-2,3,-1)`, 반지름 `2√2=√8`, 2-3 = `7√2=√98`. 각각 ①②, ①③, ①②가 동치입니다. 응용 2-1 = `√209`는 맞습니다.

**아토가 하실 일** — 아톰에게 범위 집계와 동치 오답 선택지 수정을 요청해 주세요. 파일 변경·외부 전송 없음.

전체 판정: **RED** = 범위 집계 수정 / 미적분 기초 1-1·1-2·2-3 선택지 수정.