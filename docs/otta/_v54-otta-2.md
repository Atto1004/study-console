**판정 — RED.** 기존 7건 중 5건은 닫혔습니다. A③ 저장 위치 복구와 D 색 대비는 미해결입니다. 단원 경로에도 오류가 재현됩니다.

**근거**

기존 검사 24회차·826단계·144문제는 실패 0건입니다. 추가 메모리 검사에서 입력형 재정답 XP·호감도 증가 0, 자정 뒤 하트 3·하루 건너뛴 연속일 0을 확인했습니다. 하트 0 입력형 차단, 첫 문단 `data-tutor` 보존도 통과했습니다. `.scrim` 블러 해제는 코드로 확인했습니다.

1. **RED** — `docs/tools/classroom_tpl.html:349·376`. 공수1 9/4에서 `L.pos={mode:"step",ci:"0",si:"2"}` → 일반 목차의 ‘이어서 하기’ → ‘이해했어요’를 누르면 7단계 중 3단계에서 챕터가 완료됩니다. `"2"+1`이 `"21"`이 됩니다. `#resume`의 범위 보정은 통과했지만 일반 복구에도 숫자·정수 검증을 공통 적용해야 합니다.

2. **RED** — `docs/layers/v55-layer-src.js:196·199·202`. UC 8색 배너 글자 대비를 계산했습니다. 배열 순서로 밝음은 **5.84/6.39/6.58/6.00/9.19/5.75/7.47/4.89**, 어두움은 **5.86/5.45/5.55/5.90/4.21/5.79/4.93/6.68**입니다. 어두운 5번 배너가 4.5 미달입니다. 진행 막대와 트랙도 밝음 4색·어두움 4색이 3 미달입니다. 어두운 상태 글자 초록은 2.85, 파랑은 3.23입니다. `v54-layer-src.js:75`의 일반 버튼도 어두운 바탕에서 3.23입니다. 테마별 실제 바탕 기준으로 보정해야 합니다.

3. **RED** — `v55-layer-src.js:27·30`, `knowledge/units.json:15·25·41`. 미등록 날짜로 `unitOf()`에 제목을 넣으면 ‘13.1 벡터함수’→12장, ‘Ch.8 Moments of Inertia’→Ch.4, ‘29장 전류가 만드는 자기장’→28장으로 오분류됩니다. 장 번호를 일반 키워드보다 우선해야 합니다. 현재 등록 4과목의 원본 날짜 26건은 누락·중복 0건입니다. 현재 제목의 정규식 중복 5건은 날짜 우선 규칙으로 보호됩니다.

4. **RED** — `v55-layer-src.js:119·121`. 같은 단원의 9/2·9/4 회차 사이에 지난 시험 9/3을 넣으면 시험이 9/4 뒤에 나옵니다. 선행 단원이 없는 지난 시험은 아예 사라집니다. 단원 내부에서도 시험 날짜로 나누고 `pastEx[-1]`을 출력해야 합니다.

5. **RED** — `v55-layer-src.js:125·132`. 기준일 11/1, 다음 시험을 기말로 두면 미등록 Ch.6~8이 ‘기말 뒤’에 나옵니다. `phase=final`을 항상 다음 시험 뒤에 붙이는 원인입니다. 중간·기말 종류에 따라 배치해야 합니다.

예정 회차는 4개→전부 6개→다른 과목 4개로 분리됩니다. 단원 ‘지금’과 히어로는 같은 `current()`를 사용합니다. 정역학 중간 Ch.3~5·기말 Ch.6~8과 예정 주차는 `index.html:2776·2784` 이후 문자열에 부합합니다. OT 원본 슬라이드는 확인 못 했습니다.

**아토가 하실 일** — 없음. 아톰이 위 5건을 수선한 뒤 재검수하면 됩니다.

전체 판정: **RED**. 파일 변경·외부 전송 없음.